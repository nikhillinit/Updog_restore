# Pro Subscription Bridge Configuration

# Gemini Configuration
# Get your API key from: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# OpenAI Configuration (for Stagehand's self-healing selectors)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Session Directory (Windows-compatible paths)
# This is where browser profile data and quota state are stored
PRO_BRIDGE_SESSION_DIR=./data/sessions

# Quota Configuration
# ChatGPT Pro Deep Think daily limit (Pro subscription: unlimited, but we rate-limit for stability)
CHATGPT_DEEP_THINK_DAILY_LIMIT=100

# Gemini Deep Think weekly limit (Advanced subscription limit)
GEMINI_DEEP_THINK_WEEKLY_LIMIT=50

# Browser Configuration
# Run browser in headless mode (set to false for debugging)
BROWSER_HEADLESS=false

# Browser launch timeout (milliseconds)
BROWSER_TIMEOUT_MS=30000

# Consensus Configuration
# Minimum agreement rate (0-1) for an issue to be included
CONSENSUS_MIN_AGREEMENT=0.5

# How to resolve severity conflicts: max, min, or average
CONSENSUS_SEVERITY_RESOLUTION=max

# Maximum time to wait for all providers (milliseconds)
CONSENSUS_TIMEOUT_MS=120000

# Logging
# Log level: debug, info, warn, error
LOG_LEVEL=info

# Integration Tests
# Set to 1 to run integration tests (requires valid credentials)
RUN_INTEGRATION_TESTS=0
