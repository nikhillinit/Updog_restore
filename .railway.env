# Railway Environment Configuration
NODE_ENV=production
HUSKY=0
PORT=$PORT

# Database and Redis (Railway will auto-populate these)
DATABASE_URL=$DATABASE_URL
REDIS_URL=$REDIS_URL

# CORS for Railway domain
CORS_ORIGIN=https://$RAILWAY_PUBLIC_DOMAIN

# Security settings
CSP_REPORT_ONLY=0
ENABLE_QUEUES=1

# Performance settings
BODY_LIMIT=10mb
CIRCUIT_BREAKER_THRESHOLD=5