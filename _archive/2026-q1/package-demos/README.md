# Package Demo Archive

These files were moved out of active package roots on 2026-03-17 to reduce root
lint/typecheck noise and keep maintained package entrypoints focused on `src/`.

Scope:

- `packages/agent-core` top-level demos and TypeScript examples
- `packages/memory-manager` POC demo scripts
- `packages/test-repair-agent` demo/example entrypoints

Notes:

- Archived demos are historical reference only.
- They are excluded from the normal repo lint/typecheck flow.
- Active package code remains under `packages/*/src`.
