import React from 'react';

// Simple mock components for testing without state management dependencies
export const MockInvestmentStrategyStep = () => (
  <div data-testid="investment-strategy-step">Investment Strategy Step</div>
);

export const MockExitRecyclingStep = () => (
  <div data-testid="exit-recycling-step">Exit Recycling Step</div>
);

export const MockWaterfallStep = () => (
  <div data-testid="waterfall-step">Waterfall Step</div>
);

export const MockStepNotFound = () => (
  <div data-testid="step-not-found">Step Not Found</div>
);