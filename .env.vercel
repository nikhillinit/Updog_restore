# Vercel Deployment Configuration
# Set VITE_API_BASE_URL to empty string for same-domain API calls
VITE_API_BASE_URL=

# This tells the build that API calls should use relative paths
# The frontend will make calls to /api/... on the same domain it's served from