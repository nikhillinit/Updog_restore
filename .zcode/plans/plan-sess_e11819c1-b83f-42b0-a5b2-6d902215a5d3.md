Apply red-team corrections to the consensus plan via hash-chain-preserving
errata. No code, no Git mutation, no provider/database/production action — .omx
planning artifacts only.

1. Create `.omx/plans/updog-v2-conformance-right-sized-errata-iter5.md`
   containing the 8 material corrections:
   - E1 (High): Add existing v2 test files to F1 file surface —
     tests/unit/internal-economics/v2/{derive-composite-v2,benchmark-v2,lane-isolation-v2,reserve-funding-classifier-v2,property-tests-v2,waterfall-deal-by-deal-v2,waterfall-whole-fund-v2}.test.ts
     — because F1 global refusal flips the current public success path
     (derive-composite-v2.ts:57 always returns null; placeholder receipts at
     liquidity-receipt-builder-v2.ts:78-153). F1 must inventory and update all
     success-asserting tests (AGENTS.md mandatory pre-action check).
   - E2 (High): State explicitly that F1 creates npm script
     test:internal-economics-v2 in package.json; define composition as vitest
     over tests/unit/internal-economics/** (all existing v2 tests included, not
     only named ones).
   - E3 (High): Define corpus disposition: existing 11 V2-TC-001..008/R01-03
     cases either renumbered into reserved ranges with explicit mapping table or
     kept with legacy IDs declared immutable; either way F1 records new corpus
     SHA and updates the ADR-085 addendum record.
   - E4 (Med): Clarify F1 migrates only V2-A refusal-range cases plus
     scaffolding; V2-S/E/F/D/W expectations authored in their respective PRs —
     resolves contradiction with refusal-only F1.
   - E5 (Med): Re-pin anchors: fee-recycling-enforcer-v2.ts call at line 38
     (three citations); drop routeQueryFast.ts:231 (actual version surface:
     RouterFast type field, line 26); generate-discovery-map.ts second copy at
     1028; widen opening-schema anchor to 442-491; receipt-builder ranges
     24-76/78-154; corpus-adapter anchor cite whole-file disconnection not
     line 46.
   - E6 (Med): Adopt architect iter4 residual note — replace "Mandatory
     repository specialist reviewers under the policy matrix" with
     "plan-required specialist review evidence; policy-mandatory only when
     Phoenix protected paths (.claude/PHOENIX-AGENTS-REGISTRY.md,
     PHOENIX-TOOL-ROUTING.md, DISCOVERY-MAP.md Phoenix sections) are touched."
   - E7 (Med): Add refusal-code relationship table: new codes vs existing
     23-entry registry, specifically UNSUPPORTED_V2_RECYCLING (capability gate)
     vs existing UNSUPPORTED_V2_RECYCLING_BASIS / RECYCLING_CAPACITY_EXCEEDED /
     FEE_RECYCLING_DISABLED; existing codes unchanged.
   - E8 (Med): Specify R2 output contract: new constants
     ROUTER_INDEX_SCHEMA_VERSION=2.1 / ROUTER_FAST_SCHEMA_VERSION=2.0 in
     generate-discovery-map.ts; state which output key carries schema version
     (version vs schemaVersion) and how routeQueryFast.ts type contract remains
     behavior-unchanged. Plus minor items: "Required Financial Truth" ->
     financial-truth job in ci-unified.yml (integration automatic via
     phoenix:truth glob); define phoenix-precision-guardian applicability
     trigger; note feat/v2-core-financial-model retirement after F-chain.

2. Update `.omx/plans/updog-v2-conformance-right-sized-consensus-handoff.md`:
   add errata reference under post_review_changes (scope:
   errata_addendum_with_hash_update, architecture/PR-count/order unchanged),
   record errata file SHA-256 and updated final hashes if plan/spec files are
   touched. Preferred: leave approved plan/spec files byte-identical (hashes
   stay valid); corrections live in errata file alone; F1 executor instruction =
   read plan + errata together. If any in-place edit to plan/spec proves
   unavoidable, record both old and new hashes in handoff.

3. Verify: recompute SHA-256 of all touched artifacts, confirm untouched
   approved artifacts still match recorded final hashes, report final hash
   table.

Execution of F1-F6 remains unauthorized; this step only repairs the planning
evidence.
