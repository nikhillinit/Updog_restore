---
phase: 05-test-hygiene-resurrection
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.int.ts
  - tests/integration/fund-idempotency.spec.ts
  - .planning/phases/05-test-hygiene-resurrection/05-02-FINDINGS.md
autonomous: false
requirements:
  - REQ-TEST-01
must_haves:
  truths:
    - 'The stale quarantine comment and exclude entry for
      fund-idempotency.spec.ts are removed from vitest.config.int.ts'
    - 'The test file is either actively running in the integration suite (happy
      path) OR re-excluded with a NEW comment citing the actual current failure
      mode (NOT REFL-024 cascade and NOT 6/6 timeout)'
    - 'If re-excluded, a FINDINGS.md artifact exists documenting the actual
      failure, the commits that make the old cascade comment obsolete, and a
      follow-up REFL filename reservation'
    - 'npm run validate:core exits 0'
    - 'node scripts/check-orphan-tests.mjs exits 0'
  artifacts:
    - path: 'vitest.config.int.ts'
      provides:
        'Integration test exclude list with NO stale comment referencing
        REFL-024 or 6/6 timeout cascade'
      contains: 'exclude:'
    - path: '.planning/phases/05-test-hygiene-resurrection/05-02-FINDINGS.md'
      provides:
        'Outcome record — either tests-pass-unquarantined or
        tests-fail-with-actual-error-re-quarantined'
      contains: 'Outcome:'
  key_links:
    - from: 'vitest.config.int.ts'
      to: 'tests/integration/fund-idempotency.spec.ts'
      via:
        'exclude list entry (present BEFORE this plan, absent AFTER happy path,
        PRESENT WITH NEW COMMENT AFTER re-quarantine path)'
      pattern: 'fund-idempotency'
    - from: 'tests/integration/fund-idempotency.spec.ts'
      to: 'tests/integration/global-setup.ts'
      via:
        'BASE_URL env var set by globalSetup, read by test via
        process.env.BASE_URL'
      pattern: "process\\.env\\.BASE_URL"
---

<objective>
Re-enable `tests/integration/fund-idempotency.spec.ts` in the integration test suite.
The file was quarantined on 2026-02-20 (commit `2a18a571`) with the comment
"6/6 tests timeout at 30s each, causing cascade resource exhaustion". Since then:

1. `50ae84ca` (March 2026) migrated integration tests from per-file `setupFiles`
   server spawning to a single shared `globalSetup`, eliminating the per-file
   spawn cascade that was the REFL-024 / cascade-exhaustion root cause.
2. `a2482e51` stabilized the integration lifecycle further —
   `tests/integration/global-setup.ts` now starts a single ephemeral-port
   Express server for the entire integration run, waits on `/healthz`, and
   gracefully tears down via `killProcessTree`.

The test file reads
`process.env.BASE_URL || http://localhost:${process.env.PORT || '5000'}`, which
inherits from globalSetup (line 276 of `tests/integration/global-setup.ts` sets
`process.env.BASE_URL`). Structurally, the original cascade failure mode is no
longer possible. Either (a) the test just works now, or (b) it surfaces a
previously-hidden bug.

Purpose: REQ-TEST-01. This is an explicit "free-win-or-bug-discovery" item —
both outcomes are valuable. The plan is autonomous for the happy path and
contains a HUMAN CHECKPOINT for the branch where the test surfaces a real bug,
because the checkpoint decision (fix in-phase vs re-quarantine with follow-up)
requires judgment on how deep the bug goes.

Output: a `vitest.config.int.ts` with the stale comment+exclude removed, a
`05-02-FINDINGS.md` artifact documenting the outcome, and either a passing test
or an accurately-re-quarantined test. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@vitest.config.int.ts
@tests/integration/fund-idempotency.spec.ts
@tests/integration/global-setup.ts

<interfaces>
Key contracts and invariants the executor needs.

### From vitest.config.int.ts (CURRENT state — what you will edit)

```typescript
exclude: [
  'tests/unit/**/*',
  'tests/synthetics/**/*',
  '**/*.quarantine.test.ts',
  'tests/quarantine/**/*',
  // Testcontainers tests require Docker - run via testcontainers-ci.yml instead
  'tests/integration/testcontainers-smoke.test.ts',
  'tests/integration/ScenarioMatrixCache.integration.test.ts',
  'tests/integration/cache-monitoring.integration.test.ts',
  'tests/integration/scenarioGeneratorWorker.test.ts',
  'tests/integration/migration-runner.test.ts',
  // Quarantined: 6/6 tests timeout at 30s each, causing cascade resource exhaustion
  'tests/integration/fund-idempotency.spec.ts',
],
```

Lines 47-48 (the comment and the entry) are the ones to remove in the happy
path. Do NOT touch any of the other exclude entries — they have independent
reasons (Docker-only tests, migration runner, scenario generator, scenario
matrix cache). If you notice any that LOOK stale, note them in FINDINGS.md but
do NOT remove them in this phase.

### From tests/integration/fund-idempotency.spec.ts (CURRENT state — what the test exercises)

- POST /api/funds with full Fund body → expects 201 (first) or 409 (duplicate /
  idempotency-key collision)
- DELETE /api/funds/{id} → expects 2xx (used by afterEach cleanup)
- GET /api/funds → expects a JSON array of Fund objects
- Auth: `Authorization: Bearer ${asUser()}` from `../utils/integrationAuth`
- 6 tests across 3 describe blocks:
  1. Double-submit prevention (2 tests: Promise.all concurrent requests,
     Idempotency-Key header)
  2. Inflight map cleanup (2 tests: AbortController cancel + retry, invalid data
     followed by valid)
  3. Edge cases (2 tests: 5 rapid sequential, different fund data
     differentiation)

### Known risks that could cause failures even with the cascade fix

- The `/api/funds` DELETE endpoint may not exist or may return non-2xx (the test
  was written against a hypothetical API shape; the quarantine comment says
  "missing DB teardown" which could literally mean the DELETE endpoint is
  missing).
- The POST endpoint may have changed validation — `testFundData` has 8 fields;
  if any required field was added since 2026-02-20, the 201 expectation fails
  with 400.
- The Idempotency-Key header handling may not be implemented — if it is not, the
  second request with the same key returns 201 instead of 409, and Test 2 fails.
- The concurrency semantic (one 201 + one 409 for simultaneous identical POSTs)
  assumes a server-side dedupe by name. If that dedupe was removed or relaxed,
  both may return 201 and the test fails.

### From tests/integration/global-setup.ts (CURRENT state — verifies cascade fix is real)

- Line 28: `globalSetup: ['tests/integration/global-setup.ts']` (single setup,
  not per-file)
- Line 276: `setWorkerServerEnv(baseUrl, actualPort)` sets
  `process.env.BASE_URL` so every test file inherits the correct URL.
- Lines 281-332: graceful teardown kills process tree on completion.
- The original REFL-024 per-file spawn cascade is structurally impossible under
  this setup. </interfaces> </context>

<tasks>

<task type="auto">
  <name>Task 1: Remove stale quarantine entry from vitest.config.int.ts</name>
  <files>vitest.config.int.ts</files>
  <read_first>
    - vitest.config.int.ts (the exact file you are editing — re-read to see live state)
    - tests/integration/global-setup.ts (confirm globalSetup inherits BASE_URL correctly, matching the interfaces block above)
  </read_first>
  <action>
Edit `vitest.config.int.ts`. Find lines 47-48 (verify with fresh grep before editing —
they may have shifted):

```typescript
      // Quarantined: 6/6 tests timeout at 30s each, causing cascade resource exhaustion
      'tests/integration/fund-idempotency.spec.ts',
```

Remove both lines entirely. The resulting `exclude` array should end with:

```typescript
      'tests/integration/migration-runner.test.ts',
    ],
```

Do NOT touch any of the other exclude entries. Do NOT touch anything else in the
file.

After the edit, verify:

- `grep -n 'fund-idempotency' vitest.config.int.ts` returns ZERO lines.
- `grep -n '6/6 tests timeout' vitest.config.int.ts` returns ZERO lines.
- `grep -n 'cascade resource exhaustion' vitest.config.int.ts` returns ZERO
  lines.
- The file still parses as valid TypeScript — run `npm run check` and confirm 0
  errors.

While you are in the file, glance at the other exclude entries. If any LOOKS
stale (e.g., the comment references a fixed problem or mentions a commit that
predates the global-setup migration), do NOT remove it — note it in the
FINDINGS.md produced in Task 3. The only entries in scope for Phase 5 are the
`fund-idempotency.spec.ts` pair. </action> <verify> <automated>grep -c
'fund-idempotency' vitest.config.int.ts; grep -c '6/6 tests timeout'
vitest.config.int.ts; npm run check 2>&1 | tail -5</automated> </verify>
<acceptance_criteria> - `grep -c 'fund-idempotency' vitest.config.int.ts`
returns `0` - `grep -c '6/6 tests timeout' vitest.config.int.ts` returns `0` -
`grep -c 'cascade resource exhaustion' vitest.config.int.ts` returns `0` -
`grep -c 'tests/integration/migration-runner.test.ts' vitest.config.int.ts`
returns `1` (sanity check — other exclude entries untouched) - `npm run check`
exits 0 with no new TypeScript errors (the file is still valid config) -
`git diff vitest.config.int.ts` shows ONLY the two removed lines (one comment,
one exclude entry) and nothing else </acceptance_criteria> <done>The stale
comment and exclude entry are gone; the config file parses; no other excludes
are touched.</done> </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the unquarantined test and branch on outcome</name>
  <files>tests/integration/fund-idempotency.spec.ts</files>
  <read_first>
    - vitest.config.int.ts (confirm Task 1's removal landed)
    - tests/integration/fund-idempotency.spec.ts (re-read to understand what the test expects so you can interpret the failure output correctly)
  </read_first>
  <action>
This is a human-verify checkpoint. Execute the commands below, capture the outputs, and
report back to the user for a branch decision. DO NOT proceed to Task 3 until the user
responds with `branch-A`, `branch-B: <fix>`, `branch-C: <bug>`, or `branch-D: <obsolete contract>`.

Run this command to exercise ONLY the resurrected file against the current
integration setup:

```
TZ=UTC npx vitest run -c vitest.config.int.ts tests/integration/fund-idempotency.spec.ts
```

Capture the full output. Save it to
`.planning/phases/05-test-hygiene-resurrection/05-02-run-output.txt` (scratch —
do NOT commit; it is decision-support only and will be deleted in Task 3).

Then ALSO run the full integration project to verify no collateral damage:

```
TZ=UTC npx vitest run -c vitest.config.int.ts
```

Report to the user in this exact shape:

1. **How many of the 6 tests passed / failed** — read from the "Tests: X passed,
   Y failed" line in the vitest summary.

2. **If any failures** — paste the full error message for the FIRST failure (the
   `AssertionError` or `TypeError` or `TimeoutError`). Include the HTTP status
   code if the assertion is on `.status`.

3. **Classify the failure (human judgment required) — present all four options
   to the user**:
   - **A — PASS**: All 6 tests pass. No branch; proceed to happy-path Task 3.
   - **B — TRIVIAL BUG, FIX IN-PHASE**: Failure is a small, obvious issue — e.g.
     the test asserts a response field that was renamed, the test relies on a
     header that is now optional, or the endpoint returns 200 instead of 201.
     Estimated fix ≤30 minutes of test-file edits. No server-side change needed.
     Proceed to Task 3 with fix-in-place.
   - **C — REAL BUG, PRODUCTION CODE**: Failure is in production code (the POST
     endpoint no longer handles concurrency, the DELETE endpoint is missing, the
     Idempotency-Key header is ignored). Estimated fix >30 minutes.
     Re-quarantine with new comment, write FINDINGS with follow-up plan, proceed
     to Task 3.
   - **D — STALE TEST, OBSOLETE CONTRACT**: The test tests an API shape that no
     longer exists (e.g., `/api/funds` was renamed to `/api/v2/funds`, or the
     concurrency semantic was intentionally relaxed). The test is asserting
     against an outdated contract. Re-quarantine with new comment, write
     FINDINGS with follow-up.

4. **Second integration run result** — did the full integration project pass at
   the same count it had BEFORE Task 1 (minus the 6 new fund-idempotency
   assertions, plus or minus)? If the resurrected file broke OTHER tests (e.g.,
   leaked state into siblings), this is a blocking regression — report it and
   ask for direction.

Wait for explicit human go-ahead before proceeding to Task 3. The human will
tell you which branch (A/B/C/D) and — for B — the specific fix to apply.
</action> <verify> <automated>test -f
.planning/phases/05-test-hygiene-resurrection/05-02-run-output.txt</automated>
</verify> <acceptance_criteria> - Scratch file
`.planning/phases/05-test-hygiene-resurrection/05-02-run-output.txt` exists
(proof the checkpoint was actually executed, not skipped) - User has explicitly
responded with one of: `branch-A`, `branch-B: <description>`,
`branch-C: <description>`, `branch-D: <description>` - No edits have been made
to any source file yet (checkpoint is pure observation) </acceptance_criteria>
<done>The user has classified the outcome into one of four branches and given
explicit go-ahead to proceed to Task 3.</done> <resume-signal> Type `branch-A`
(all pass, happy path), `branch-B: <description of fix>` (trivial in-phase fix),
`branch-C: <description of bug>` (re-quarantine, production bug follow-up), or
`branch-D: <description of obsolete contract>` (re-quarantine, stale test
follow-up). </resume-signal> </task>

<task type="auto">
  <name>Task 3: Finalize outcome — commit happy path OR re-quarantine with accurate comment + FINDINGS</name>
  <files>vitest.config.int.ts, tests/integration/fund-idempotency.spec.ts, .planning/phases/05-test-hygiene-resurrection/05-02-FINDINGS.md</files>
  <read_first>
    - vitest.config.int.ts (current state after Task 1 edit)
    - tests/integration/fund-idempotency.spec.ts (only if branch B — you will edit this file)
    - .planning/phases/05-test-hygiene-resurrection/05-02-run-output.txt (scratch output captured in Task 2 — read but DO NOT commit)
    - cheatsheets/INDEX.md (look up the test-placement / orphan-test guidance if branch B involves adding anything to tests/)
  </read_first>
  <action>
Branch on the decision from Task 2. Do exactly ONE of the four sub-actions below.

### Sub-action A — Happy path: all 6 tests pass

Step A.1. Create
`.planning/phases/05-test-hygiene-resurrection/05-02-FINDINGS.md` with exactly
this body (substitute values where indicated with angle brackets):

```
# Plan 05-02 Findings — REQ-TEST-01

## Outcome: HAPPY PATH — fund-idempotency.spec.ts passes unchanged

The test file was quarantined on 2026-02-20 (commit `2a18a571`) with the comment
"6/6 tests timeout at 30s each, causing cascade resource exhaustion". The cascade
was fixed by:
- `50ae84ca` — migration from per-file setupFiles to single globalSetup
- `a2482e51` — stabilize integration lifecycle and discovery

After removing the exclude entry, all 6 tests pass against the current globalSetup
single-server-spawn architecture. The quarantine was dead weight for at least 6 weeks
(possibly longer — the global-setup migration landed in March 2026).

## Results

- `TZ=UTC npx vitest run -c vitest.config.int.ts tests/integration/fund-idempotency.spec.ts`: 6/6 passed
- Full integration project run: <X passed / Y failed> (matches pre-change baseline — no collateral)
- `npm run validate:core`: exit 0
- `node scripts/check-orphan-tests.mjs`: exit 0

## Observations about other exclude entries

<paste any comments you noted from Task 1 about other exclude entries that LOOK stale
but were intentionally left alone as out-of-scope for Phase 5, OR write "none" if all
other entries looked legitimate>

## No follow-up required

The test is now actively running in the integration suite.
```

Step A.2. No code changes beyond Task 1. Confirm `git status --short` shows only
`vitest.config.int.ts` (modified) and
`.planning/phases/05-test-hygiene-resurrection/05-02-FINDINGS.md` (new).

Step A.3. Run final gates:

```
TZ=UTC npm test -- --project=integration
npm run validate:core
node scripts/check-orphan-tests.mjs
```

All three must exit 0 (or match the known-green baseline if the project-wide
integration suite has other known flakes).

### Sub-action B — Trivial fix in-phase (≤30 minutes)

Step B.1. Apply the specific fix the user named in the `resume-signal` (e.g.,
update an assertion to expect the new field name, add a missing auth header,
update the URL path). Edit `tests/integration/fund-idempotency.spec.ts` in
place.

Step B.2. Re-run ONLY the test file to confirm the fix:

```
TZ=UTC npx vitest run -c vitest.config.int.ts tests/integration/fund-idempotency.spec.ts
```

Must exit 0 with all 6 passing. If not, STOP — the fix was not actually trivial,
flip to sub-action C and ask for guidance.

Step B.3. Create
`.planning/phases/05-test-hygiene-resurrection/05-02-FINDINGS.md` with:

```
# Plan 05-02 Findings — REQ-TEST-01

## Outcome: FIXED IN-PHASE — fund-idempotency.spec.ts required a small test update

The test file was quarantined on 2026-02-20 with a stale cascade comment. The cascade
fix (globalSetup migration) was real, but the test also needed a <N>-line update to
match current API behavior.

## Fix applied

<exact description of the fix, file path, line numbers, old value -> new value>

## Why this was safe to fix in-phase

<explanation — e.g., "The assertion was against a response field renamed in commit
<hash>. The test logic is unchanged; only the field name in the expect(...) call was
updated.">

## Results

- Before fix: <N> tests failed with <error>
- After fix: 6/6 passed
- Full integration project: <X passed / Y failed>
- `npm run validate:core`: exit 0
- `node scripts/check-orphan-tests.mjs`: exit 0

## No follow-up required
```

Step B.4. Run final gates (same as sub-action A.3).

### Sub-action C — Real production bug (>30 min, re-quarantine)

Step C.1. Re-add the exclude entry to `vitest.config.int.ts`, but with a NEW,
ACCURATE comment. The new comment MUST:

- NOT reference REFL-024
- NOT say "cascade"
- NOT say "6/6 tests timeout"
- Cite the date of the re-quarantine (today, 2026-04-08)
- Describe the actual failing test name and the actual failure signature
- Reference the FINDINGS.md path
- Reference the follow-up REFL filename reservation

Example shape (use the ACTUAL observed failure, not this placeholder):

```typescript
      // Re-quarantined 2026-04-08: POST /api/funds no longer returns 409 on
      // Idempotency-Key collision (returns 201). Production-code fix out of scope
      // for Phase 5. See .planning/phases/05-test-hygiene-resurrection/05-02-FINDINGS.md
      // and docs/skills/REFL-038-fund-idempotency-regression.md (follow-up).
      'tests/integration/fund-idempotency.spec.ts',
```

Step C.2. Create
`.planning/phases/05-test-hygiene-resurrection/05-02-FINDINGS.md`:

```
# Plan 05-02 Findings — REQ-TEST-01

## Outcome: REAL BUG DISCOVERED — re-quarantined with accurate comment

The original cascade-failure comment was stale — the global-setup migration
(`50ae84ca`) fixed the REFL-024 root cause and no longer applies. After removing the
stale exclude, the test surfaced a REAL production bug that was hiding behind the
quarantine.

## Actual failure

- Test name: <exact `describe > it` path>
- Error: <exact assertion or error message>
- First observed: 2026-04-08 (this plan)
- Last known passing: <if determinable from git log; otherwise "unknown — at least since quarantine on 2026-02-20">

## Why NOT fixed in Phase 5

<e.g., "The fix is in server/routes/fund-setup.ts and requires updating the idempotency
middleware plus its unit tests. Estimated 2-4 hours. Out of scope for Phase 5 (test
hygiene).">

## Follow-up reservation

- REFL filename: `docs/skills/REFL-038-fund-idempotency-<slug>.md` (reserved — not
  written yet; to be created when the follow-up fix lands)
- Recommended phase: v1.1 spillover OR v1.2 kickoff (user decision)
- Estimated effort: <N> hours

## Invariants captured

- The cascade fix is real (`50ae84ca`, `a2482e51`) — any future comment on this file
  MUST cite the actual failure, not the old cascade.
- globalSetup at `tests/integration/global-setup.ts` inherits BASE_URL correctly via
  `process.env.BASE_URL` (verified while investigating).
```

Step C.3. Confirm `vitest.config.int.ts` again excludes the file but with the
new comment:

```
grep -A 3 -B 1 'fund-idempotency' vitest.config.int.ts
```

Output must include the new date (2026-04-08) and the FINDINGS.md path, and must
NOT include "cascade" or "6/6".

Step C.4. Run final gates (same as sub-action A.3). The test file is excluded
again, so the integration suite should match its pre-plan baseline.

### Sub-action D — Stale contract (>30 min, re-quarantine)

Same shape as sub-action C, but the FINDINGS.md "Actual failure" section frames
the problem as "test asserts against an obsolete contract" rather than
"production code regression", and the follow-up reservation is for either
updating the test or deleting it entirely once a product decision is made about
whether the contract should live or die.

Use this exact comment shape in vitest.config.int.ts:

```typescript
      // Re-quarantined 2026-04-08: test asserts against an obsolete contract
      // (<brief shape of the obsolete thing>). Product decision required before
      // the test can be updated or deleted. See
      // .planning/phases/05-test-hygiene-resurrection/05-02-FINDINGS.md.
      'tests/integration/fund-idempotency.spec.ts',
```

Final gates same as sub-action A.3.

### All branches — post-branch cleanup

Regardless of which branch was taken:

Step Z.1. Delete the scratch file if created:

```
rm -f .planning/phases/05-test-hygiene-resurrection/05-02-run-output.txt
```

Step Z.2. Confirm final `git status --short` shows only:

- `vitest.config.int.ts` (modified — either stripped-to-clean in A, or
  re-excluded-with-new-comment in C/D)
- `.planning/phases/05-test-hygiene-resurrection/05-02-FINDINGS.md` (new)
- `tests/integration/fund-idempotency.spec.ts` (modified — ONLY in sub-action B)

Step Z.3. Run `node scripts/check-orphan-tests.mjs` — must exit 0.

Step Z.4. Run `npm run validate:core` — must exit 0. </action> <verify>
<automated>npm run validate:core && node scripts/check-orphan-tests.mjs && test
-f .planning/phases/05-test-hygiene-resurrection/05-02-FINDINGS.md && grep -c
'Outcome:'
.planning/phases/05-test-hygiene-resurrection/05-02-FINDINGS.md</automated>
</verify> <acceptance_criteria> - File
`.planning/phases/05-test-hygiene-resurrection/05-02-FINDINGS.md` exists -
FINDINGS.md contains an `Outcome:` line with one of: HAPPY PATH, FIXED IN-PHASE,
REAL BUG, STALE CONTRACT - `npm run validate:core` exits 0 -
`node scripts/check-orphan-tests.mjs` exits 0 - Sub-action A assertions (only
apply if sub-action A): `grep -c 'fund-idempotency' vitest.config.int.ts`
returns `0` - Sub-action B assertions (only apply if sub-action B):
`grep -c 'fund-idempotency' vitest.config.int.ts` returns `0`,
`TZ=UTC npx vitest run -c vitest.config.int.ts tests/integration/fund-idempotency.spec.ts`
exits 0 - Sub-action C/D assertions (only apply if sub-action C/D):
`grep -c 'fund-idempotency' vitest.config.int.ts` returns `1`,
`grep -c 'Re-quarantined 2026-04-08' vitest.config.int.ts` returns `1`,
`grep -c 'cascade' vitest.config.int.ts` returns `0`,
`grep -c '6/6 tests timeout' vitest.config.int.ts` returns `0`, FINDINGS.md
contains a "Follow-up reservation" section with a reserved REFL filename -
Scratch file `05-02-run-output.txt` is NOT present in `git status --short`
(deleted if it was ever created) - `git status --short` shows only the allowed
files for the chosen branch </acceptance_criteria> <done>The plan is closed with
an unambiguous outcome: either the test is live and green, the test is live and
green after a small in-phase fix, or the test is re-excluded with an accurate
non-stale comment and a FINDINGS.md that captures the real failure mode and
follow-up plan.</done> </task>

</tasks>

<threat_model>

## Trust Boundaries

No new trust boundaries. The plan edits a test-runner config file, possibly a
test file, and creates a planning-doc artifact. Nothing touches production code,
auth, data handling, or external services.

## STRIDE Threat Register

| Threat ID  | Category          | Component              | Disposition | Mitigation Plan                                                                                                                                                                                                                                          |
| ---------- | ----------------- | ---------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-02-01 | Tampering         | `vitest.config.int.ts` | accept      | Config file is under version control; any unauthorized change would be visible in `git diff`. No new attack surface introduced — we are removing (or rewriting) an exclude comment, not adding a new code path.                                          |
| T-05-02-02 | Denial of Service | integration test suite | accept      | If the resurrected test leaks state or hangs, it could slow CI. Mitigated by running it locally first (Task 2) and branching to re-quarantine if it causes collateral damage. Global `testTimeout: 30000` in vitest.config.int.ts caps any runaway test. |

No threats require mitigation. Phase 5 is test hygiene; no new code paths, no
new data handling, no dependency changes. </threat_model>

<verification>
- `vitest.config.int.ts` no longer contains the stale comment "6/6 tests timeout" or
  "cascade resource exhaustion".
- Either `grep -c 'fund-idempotency' vitest.config.int.ts` returns 0 (happy path / in-phase
  fix) OR the only occurrence is a new date-stamped comment that cites the actual failure
  and references FINDINGS.md.
- `.planning/phases/05-test-hygiene-resurrection/05-02-FINDINGS.md` exists with an
  `Outcome:` line that matches one of the four branches.
- `npm run validate:core` exits 0.
- `node scripts/check-orphan-tests.mjs` exits 0.
- `npm run check` exits 0 (no TypeScript errors introduced in the config file).
- The full integration test project matches its pre-plan pass/fail baseline (no
  collateral damage from the resurrected file — or from a re-quarantine that silently
  changed exclude-list semantics).
- `git status --short` shows only the expected files for the chosen branch.
</verification>

<success_criteria>

1. `tests/integration/fund-idempotency.spec.ts` is either actively running in
   the integration suite (branches A and B) OR excluded with a new date-stamped
   comment that cites the actual failure mode (branches C and D). In no case
   does the stale "cascade resource exhaustion" comment survive.
2. If re-excluded,
   `.planning/phases/05-test-hygiene-resurrection/05-02-FINDINGS.md` exists and
   documents the actual failure, the commits that obsolete the old cascade
   comment, and a follow-up REFL filename reservation.
3. `npm run validate:core` exits 0.
4. `node scripts/check-orphan-tests.mjs` exits 0.
5. The integration project-wide pass/fail count matches the pre-plan baseline
   (no collateral damage).
6. No scratch files (`05-02-run-output.txt`) are committed. </success_criteria>

<output>
After completion, create `.planning/phases/05-test-hygiene-resurrection/05-02-SUMMARY.md`
following the summary template. The summary MUST reference the FINDINGS.md outcome (A/B/C/D)
and MUST NOT duplicate its contents — FINDINGS.md is the operational record, SUMMARY.md
is the plan-level close-out.
</output>
