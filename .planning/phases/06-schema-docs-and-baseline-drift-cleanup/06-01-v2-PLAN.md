---
phase: 06-schema-docs-and-baseline-drift-cleanup
plan: 01-v2
type: execute
wave: 1
depends_on:
  - 06-01-INTROSPECT-FINDINGS.md
files_modified:
  - shared/schema.ts
  - shared/schema-lp-reporting.ts
  - shared/schema-lp-sprint3.ts
  - shared/schema/shares.ts
  - server/app.ts
  - server/routes/cohort-analysis.ts
  - server/routes/portfolio-optimization.ts
  - server/services/portfolio-optimization-service.ts
  - shared/types/database.ts
  - tests/integration/cache-monitoring.integration.test.ts
  - tests/integration/ScenarioMatrixCache.integration.test.ts
  - tests/unit/portfolio-optimization/portfolio-optimization-service.test.ts
  - tests/unit/schema/portfolio-optimization-schema.test.ts
requirements:
  - REQ-DRIFT-01
autonomous: false
---

<objective>
Execute the remaining audit-first portion of REQ-DRIFT-01 after the original
`06-01` plan was invalidated by live Neon evidence. This v2 plan is the
canonical execution contract for the remaining phantom-table cleanup work.

Safe-now work already approved by the Ralph follow-up to the deliberate PRD:

- preserve `06-01-INTROSPECT-FINDINGS.md`
- delete stale untracked `migrations/schema.ts`
- delete stale untracked `migrations/relations.ts`
- remove the Notion cluster from `shared/schema.ts`
- delete `server/services/notion-service.ts`

Everything else remains audit-first until it clears the same safety bar:

1. direct SQL confirms live-DB absence,
2. repo search shows no active runtime consumer that must survive,
3. route / startup / worker / migration dependencies are absent or removed
   coherently,
4. related tests can be updated or removed coherently,
5. the cluster can ship as an atomic, reviewable diff without touching unrelated
   dirty work.

`jobOutbox` stays keep-as-is. No migration re-emit is allowed for it unless new
evidence contradicts the current findings doc. </objective>

<context>
- Findings source of truth:
  `.planning/phases/06-schema-docs-and-baseline-drift-cleanup/06-01-INTROSPECT-FINDINGS.md`
- Approved deliberate PRD:
  `.omx/plans/prd-phase-06-01-next-step.md`
- Verified schema scope: all table definitions must be evaluated across
  `shared/*.ts`, `shared/schema/*.ts`, and `schema/src/*.ts` (12 files total),
  not just the original 3-file scope.
</context>

<clusters>

### Completed safe-now cluster

- **Generated artifact cleanup**
  - `migrations/schema.ts`
  - `migrations/relations.ts`
- **Notion cluster**
  - `notionConnections`
  - `notionSyncJobs`
  - `notionPortfolioConfigs`
  - `notionDatabaseMappings`
  - `server/services/notion-service.ts`
  - status: executed in the Ralph safe-now slice on 2026-04-10
  - residual note: `shared/notion-schema.ts` may now be dead, but was left out
    of the approved safe-now bucket to avoid widening scope mid-turn

### Remaining audit-first clusters

1. **Cohort cluster**
   - `cohortDefinitions`
   - `sectorTaxonomy`
   - `sectorMappings`
   - `companyOverrides`
   - `investmentOverrides`
   - `server/routes/cohort-analysis.ts`
   - `server/app.ts` mount removal
   - Reason: mounted public route and client query hooks require perimeter
     verification.
   - audit result (2026-04-10): **defer / keep surface intact**
   - evidence:
     - `server/app.ts:28` imports `cohortAnalysisRouter`
     - `server/app.ts:181` mounts
       `app.use('/api/cohorts', cohortAnalysisRouter)`
     - `client/src/hooks/useCohortAnalysis.ts` issues live fetches to
       `/api/cohorts/*`
     - `client/src/components/cohorts/CohortDefinitionSelector.tsx` and
       `client/src/components/cohorts/SectorMappingUI.tsx` consume the hook
   - implication: deleting this cluster now would break an active public API and
     live client workflows; it is not a safe-delete candidate.

2. **Portfolio-optimization cluster**
   - `scenarioMatrices`
   - `optimizationSessions`
   - `server/routes/portfolio-optimization.ts`
   - `server/services/portfolio-optimization-service.ts`
   - `shared/types/database.ts`
   - 4 directly coupled tests from the findings doc
   - Reason: additional runtime/cache consumers surfaced in
     `shared/core/optimization/ScenarioMatrixCache.ts`,
     `server/services/CacheStatsService.ts`, and
     `server/services/CacheInvalidationService.ts`.
   - audit result (2026-04-10): **defer / keep surface intact**
   - evidence:
     - `server/services/portfolio-optimization-service.ts` still owns live
       `jobOutbox`, `scenarioMatrices`, and `optimizationSessions` behavior
     - `server/routes/portfolio-optimization.ts` still exposes the route
       surface, even if it is not mounted in `server/app.ts`
     - `shared/core/optimization/ScenarioMatrixCache.ts`,
       `server/services/CacheStatsService.ts`, and
       `server/services/CacheInvalidationService.ts` still consume
       `scenarioMatrices`
     - `tests/integration/cache-monitoring.integration.test.ts` and
       `tests/unit/schema/portfolio-optimization-schema.test.ts` still assert
       the schema
   - implication: this is an internal/runtime dependency cluster, not dead code.

3. **LP cluster**
   - all 15 LP tables from `shared/schema-lp-reporting.ts` and
     `shared/schema-lp-sprint3.ts`
   - Reason: live DB absence is proven, but repo/runtime imports still need a
     coherent removal plan before deletion.
   - audit result (2026-04-10): **defer / keep surface intact**
   - evidence:
     - `server/routes.ts:104-105` imports and mounts `lp-api`
     - `server/routes/lp-api.ts`, `server/routes/lp-capital-calls.ts`,
       `server/routes/lp-documents.ts`, and `server/routes/lp-notifications.ts`
       all import the LP schema files
     - `server/workers/capital-call-status-worker.ts`,
       `server/services/lp-calculator.ts`,
       `server/services/lp-notification-service.ts`,
       `server/services/pdf-generation/data-fetchers.ts`,
       `server/queues/report-generation-queue.ts`,
       `server/middleware/requireLPAccess.ts`, `server/db-schema.ts`, and
       `server/db/pool.ts` all depend on LP schema exports
   - implication: the findings doc's earlier "routes are not mounted" assumption
     is false. This is an active mounted product surface with missing live-DB
     backing, which makes it a schema-drift incident, not a safe-delete target.

4. **Newly surfaced phantoms**
   - `shares`
   - `shareAnalytics`
   - `sensitivityRuns`
   - `snapshotVersions`
   - Reason: all have active server/runtime consumers and need dead-code audits,
     not blind deletion.
   - audit result (2026-04-10): **defer / keep surfaces intact**
   - evidence:
     - `shares` / `shareAnalytics`
       - `server/routes.ts:113` mounts
         `app.use('/api/shares', sharesRoutes.sharesRouter)`
       - `server/routes/shares.ts` imports `shares` and `shareAnalytics`
       - `client/src/pages/shared-dashboard.tsx` fetches `/api/shares/:shareId`
       - `tests/e2e/report-sharing.spec.ts` uses `/api/shares`
     - `sensitivityRuns`
       - `server/services/sensitivity-run-service.ts` imports and mutates
         `sensitivityRuns`
       - `client/src/hooks/useSensitivityRuns.ts` and multiple sensitivity
         panels depend on it
       - `server/migrations/20260406_create_sensitivity_runs_v1.up.sql` exists
     - `snapshotVersions`
       - `server/services/snapshot-version-service.ts` is an active service
       - `server/routes/portfolio/versions.ts` uses that service
       - `server/workers/version-pruning-worker.ts` and related tests depend on
         it
   - implication: these are active supported surfaces with missing live-DB
     backing; they require follow-up remediation, not deletion.

</clusters>

<audit_results>

All four remaining audit-first clusters were reviewed on 2026-04-10.

- **No additional clusters were promoted to safe-delete.**
- **All remaining clusters stay deferred.**
- The strongest new finding is that the **LP cluster is actively mounted** via
  `server/routes.ts`, so the old assumption that it was merely an unmounted
  staged surface is incorrect.
- The cohort, shares, sensitivity, and snapshot-version surfaces are also active
  client/server/runtime paths, so deleting them now would create product
  regressions rather than reduce dead code.

</audit_results>

<tasks>

<task type="audit">
  <name>Task 1: cohort cluster perimeter audit</name>
  <deliverables>
    - prove whether `/api/cohorts` is still a live public surface
    - catalog all runtime callers of the cohort route and the five cohort-adjacent phantom tables
    - either promote the cluster to safe-delete or leave it deferred with evidence
  </deliverables>
  <status>completed-2026-04-10</status>
  <verdict>defer / active public API</verdict>
</task>

<task type="audit">
  <name>Task 2: portfolio-optimization runtime audit</name>
  <deliverables>
    - trace all remaining imports and runtime consumers of `scenarioMatrices` and `optimizationSessions`
    - determine whether the cache services and shared optimization code can be removed atomically
    - keep `shared/types/database.ts` aligned with the final verdict
  </deliverables>
  <status>completed-2026-04-10</status>
  <verdict>defer / active internal runtime dependency cluster</verdict>
</task>

<task type="audit">
  <name>Task 3: LP cluster import and ownership audit</name>
  <deliverables>
    - catalog every active import of the LP schema files
    - determine whether the LP cluster is dormant code or a staged-but-unmounted feature
    - only permit deletion if all imports can be removed coherently in one pass
  </deliverables>
  <status>completed-2026-04-10</status>
  <verdict>defer / active mounted LP surface with schema drift</verdict>
</task>

<task type="audit">
  <name>Task 4: dead-code audit for newly surfaced phantoms</name>
  <deliverables>
    - `shares` / `shareAnalytics`: prove whether `/api/shares` and `client/src/pages/shared-dashboard.tsx` still define a supported product surface
    - `sensitivityRuns`: reconcile `server/services/sensitivity-run-service.ts` and `server/migrations/20260406_create_sensitivity_runs_v1.up.sql`
    - `snapshotVersions`: reconcile `server/services/snapshot-version-service.ts` and its related tests
    - classify each as keep, rewrite, or delete
  </deliverables>
  <status>completed-2026-04-10</status>
  <verdict>defer / active supported surfaces</verdict>
</task>

</tasks>

<verification>
- Always begin with targeted repo searches for the candidate cluster.
- Before deleting any remaining audit-first cluster:
  - run `npm run check`
  - run `npm run validate:core`
  - if schema-affecting deletes are included, run `TZ=UTC npm run phoenix:truth`
- Record the exact evidence that promoted or deferred each cluster.
- Do not widen scope beyond the audited cluster in the same commit.
</verification>

<stop_conditions>

- Stop if any cluster still has active runtime imports, mounted routes, worker
  dependencies, or migration coupling that cannot be removed atomically.
- Stop if the proof for a cluster depends on stale generated artifacts instead
  of SQL truth or live repo evidence.
- Stop if unrelated dirty files would be required to complete the cluster.
  </stop_conditions>

<notes>
- `jobOutbox` is explicitly out of scope for deletion and should not be re-emitted.
- The original `06-01-PLAN.md` is superseded by the findings doc and this v2 plan.
</notes>
