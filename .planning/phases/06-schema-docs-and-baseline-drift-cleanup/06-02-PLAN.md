---
phase: 06-schema-docs-and-baseline-drift-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/codebase/CONCERNS.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - REQ-DRIFT-02
  - REQ-DRIFT-03
must_haves:
  truths:
    - 'A grep for `\b(374|132)\b` across `.planning/PROJECT.md`,
      `.planning/codebase/CONCERNS.md`, `.planning/codebase/CONVENTIONS.md`,
      `.planning/codebase/INTEGRATIONS.md`, `.planning/REQUIREMENTS.md`, and
      `.planning/ROADMAP.md` returns ZERO matches.'
    - 'A grep for `~400` (the stale explicit-any baseline number) across the
      same 6 files returns ZERO matches; only the live number `363` appears,
      sourced from `.baselines/eslint-output.json`.'
    - 'A grep for hardcoded phoenix counts (`262/262`,
      `\d{2,3}/\d{2,3}\s+truth`, `phoenix.*\d{2,3}/\d{2,3}`) across the 3
      forward-looking files (`.planning/ROADMAP.md`,
      `.planning/REQUIREMENTS.md`, `.planning/PROJECT.md`) returns ZERO matches.
      Hits in `STATE.md`, `*SUMMARY.md`, `*CONTEXT.md`, `*PLAN.md`,
      `CHANGELOG.md`, and `docs/archive/**` are deferred-by-policy and remain.'
    - 'CLAUDE.md at the project root is explicitly noted in REQUIREMENTS.md and
      ROADMAP.md as NOT being a drift target — the original ROADMAP phrasing was
      wrong.'
    - 'The replacement text for the 3 forward-looking phoenix prescriptions
      points to running `npm run phoenix:truth` for the live count, with NO
      embedded number.'
    - '`npm run check` exits 0 (prose-only edits — should be a no-op).'
    - '`npm run phoenix:truth` exits 0 with the same live count it had before
      this plan (Phase 6 touches no calc paths in either plan).'
    - '`npm run validate:core` exits 0.'
  artifacts:
    - path: '.planning/PROJECT.md'
      provides:
        '5 hit-points updated: line 35 (M9 target outcomes), line 110 (validated
        infra row), line 187 (REQ-DRIFT-03 description), lines 286-287 (Logging
        constraint). All `374`, `132`, and `~400` instances replaced with `39`,
        `29`, `363` and a `.baselines/*.json` source citation.'
      contains_not: '374'
    - path: '.planning/codebase/CONCERNS.md'
      provides:
        '3 hit-points updated: line 62 (~400 → 363), line 66 (374 → 39), line 68
        (132 → 29). All three rewrites cite the source `.baselines/*.json` file.'
      contains_not: '374'
    - path: '.planning/codebase/CONVENTIONS.md'
      provides:
        '2 hit-points updated: line 14 (~400 → 363), line 110 (374 → 39).'
      contains_not: '374'
    - path: '.planning/codebase/INTEGRATIONS.md'
      provides:
        '1 hit-point updated: lines 126-127 (374 → 39 with `.baselines/*.json`
        citation).'
      contains_not: '374'
    - path: '.planning/REQUIREMENTS.md'
      provides:
        'REQ-DRIFT-02 statement at lines 54-61 rewritten to remove the `262/262`
        example. REQ-DRIFT-03 statement at lines 63-71 rewritten to retarget the
        four `.planning/` doc files instead of `CLAUDE.md`, with an explicit
        note that CLAUDE.md at project root is NOT a drift target.'
      contains_not: '374'
    - path: '.planning/ROADMAP.md'
      provides:
        'REQ-DRIFT-03 line 102 rewritten to retarget the `.planning/` doc files.
        Phase 6 success criterion 4 at lines 140-142 rewritten to drop the
        `262/262` example. Phase 6 success criterion 3 at lines 136-139 either
        left intact (it states the correct numbers 39/29/363) or annotated to
        match REQ-DRIFT-03 retargeting.'
      contains_not: '374'
  key_links:
    - from: '.planning/PROJECT.md'
      to: '.baselines/console-prod-baseline.json'
      via: 'inline source citation in updated lines'
      pattern: 'console-prod-baseline\.json'
    - from: '.planning/codebase/CONCERNS.md'
      to: '.baselines/eslint-file-disable-baseline.json'
      via: 'inline source citation'
      pattern: 'eslint-file-disable-baseline\.json'
    - from: '.planning/REQUIREMENTS.md REQ-DRIFT-03'
      to:
        '.planning/codebase/{CONCERNS,CONVENTIONS,INTEGRATIONS}.md +
        .planning/PROJECT.md'
      via: 'rewritten target list, retargeted away from CLAUDE.md'
      pattern: 'codebase doc baseline numbers'
---

<objective>
Pure prose substitution to fix two distinct documentation drifts surfaced by
M9 scoping:

**REQ-DRIFT-02 (3 actionable files):** Remove hardcoded Phoenix truth-case
counts from FORWARD-LOOKING planning prescriptions. Today, `ROADMAP.md:141`
(Phase 6 success criterion 4), `REQUIREMENTS.md:54-61` (REQ-DRIFT-02 itself),
and `PROJECT.md:184-185` (REQ-DRIFT-02 echo) all embed `262/262` as an example
live count — but the requirement they describe is the no-hardcoded-numbers rule,
so they CONTAIN THE VERY NUMBER THEY FORBID. Replacement: point to
`npm run phoenix:truth` for the live count, drop the embedded number. Historical
recaps in `STATE.md`, `*SUMMARY.md`, `*CONTEXT.md`, `*PLAN.md`, `CHANGELOG.md`,
and `docs/archive/**` are EXPLICITLY EXCLUDED from this requirement (per
research §"REQ-DRIFT-02 Hit List" deferred-by-policy).

**REQ-DRIFT-03 (6 files, 13 hit-points):** Update stale baseline numbers (`374`
→ `39`, `132` → `29`, `~400` → `363`). Critical finding from research
§"REQ-DRIFT-03 Target Correction": **CLAUDE.md at the project root does NOT
contain `374`, `132`, or `~400` anywhere.** The ROADMAP and REQUIREMENTS.md
phrasing is incorrect — the stale numbers actually live in
`.planning/PROJECT.md` (5 hits), `.planning/codebase/CONCERNS.md` (3 hits),
`.planning/codebase/CONVENTIONS.md` (2 hits),
`.planning/codebase/INTEGRATIONS.md` (1 hit), and additionally in
`.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` themselves (the
requirement statements that describe the drift). This plan rewrites all 13
hit-points AND retargets the REQ-DRIFT-03 statement in REQUIREMENTS.md and
ROADMAP.md to point at the correct files. CLAUDE.md is explicitly mentioned as
NOT a drift target so the next session does not hunt for non-existent
occurrences.

Critical finding from research §"Tripwire Script Audit":
`scripts/guardrails/console-ratchet.mjs` and
`scripts/guardrails/eslint-disable-ratchet.mjs` read `.baselines/*.json` at
runtime — they do NOT hardcode `374` or `132`. This plan has ZERO tripwire
impact and CANNOT break the pre-push hook. It is a pure prose edit.

This plan is **autonomous**: no human checkpoint, no introspect, no live-DB
read, no code edits, no test deletes. All edits are deterministic
search-and-replace using exact line-by-line replacement text from 06-RESEARCH.md
§"REQ-DRIFT-02 Hit List" and §"REQ-DRIFT-03 Target Correction".

Output: 6 edited files with the stale numbers gone, the forward-looking
prescriptions pointing at `npm run phoenix:truth`, and the REQ-DRIFT-03
retargeting statement in place. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/INTEGRATIONS.md
@.planning/phases/06-schema-docs-and-baseline-drift-cleanup/06-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Exact line numbers and current text. Verified by planner grep on 2026-04-08. -->
<!-- Executor should NOT search — use these as the authoritative hit list. -->

REQ-DRIFT-03 hit list (all 13 hit-points across 6 files):

```
.planning/PROJECT.md:35       "374→39, 132→29, ~400→363"  (M9 target outcomes paragraph)
.planning/PROJECT.md:110-111  "(374 / 132 baselines)"     (Validated infra row)
.planning/PROJECT.md:187      'CLAUDE.md says "374 / 132 baselines"' (REQ-DRIFT-03 description)
.planning/PROJECT.md:286-287  "374 disallowed-`console` baseline or the 132 file-level"
                              (Logging constraint section)
.planning/codebase/CONCERNS.md:62-63  "~400 pre-existing `any` types"
.planning/codebase/CONCERNS.md:66-67  "374 disallowed calls"
.planning/codebase/CONCERNS.md:68-69  "132 file-level disables"
.planning/codebase/CONVENTIONS.md:14  "~400 pre-existing baselines"
.planning/codebase/CONVENTIONS.md:110 "(baseline 374 disallowed calls — new code cannot add to it)"
.planning/codebase/INTEGRATIONS.md:126-127  "(baseline 374 disallowed calls)"
.planning/REQUIREMENTS.md:63-71  REQ-DRIFT-03 entire block (4 mentions of CLAUDE.md to retarget)
.planning/ROADMAP.md:102  "REQ-DRIFT-03 — Update CLAUDE.md baseline numbers (374→39, 132→29, ~400→363)"
```

REQ-DRIFT-02 hit list (3 actionable files; rest are deferred-by-policy):

```
.planning/ROADMAP.md:140-142  "npm run phoenix:truth stays at 262/262 (or whatever the live count is on the day Phase 6 lands)"
                              (Phase 6 success criterion 4)
.planning/REQUIREMENTS.md:54-61  REQ-DRIFT-02 entire block — contains "vs the current 262/262"
.planning/PROJECT.md:184-185  REQ-DRIFT-02 echo — "current live count is 262/262"
```

Replacement values (live numbers — verified by planner against research):

- console baseline: 39 (source: .baselines/console-prod-baseline.json)
- file-level eslint-disable baseline: 29 (source:
  .baselines/eslint-file-disable-baseline.json)
- explicit-any baseline: 363 (source: .baselines/eslint-output.json)
- phoenix truth count: NEVER hardcode — point at `npm run phoenix:truth`

EXPLICITLY OUT OF SCOPE (deferred-by-policy per research):

- .planning/PROJECT.md:152 (M8 historical recap "Phoenix truth cases: 258 →
  262")
- .planning/STATE.md:231 (Phase 3 close paragraph "262/262 across 6 test files")
- .planning/STATE.md:236 (Phase 2 close paragraph)
- .planning/phases/03-todo-report-remediation/03-01-archive-stale-docs-PLAN.md:168,186,201
- .planning/STATE.md:205 (M9 scoping paragraph that quotes the stale numbers as
  discovery context)
- All `*-SUMMARY.md`, `*-CONTEXT.md`, `*-PLAN.md` under `.planning/phases/`
- CHANGELOG.md, docs/archive/**, archive/**, docs/PHOENIX-SOT/evidence-ledger.md
- CLAUDE.md at project root (does NOT contain the stale numbers — confirmed by
  planner grep)

These deferred files are forensic records frozen at execution time and MUST NOT
be edited by this plan. </interfaces> </context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQ-DRIFT-03 in .planning/codebase/ files (CONCERNS, CONVENTIONS, INTEGRATIONS) — 6 hit-points</name>
  <files>
    .planning/codebase/CONCERNS.md,
    .planning/codebase/CONVENTIONS.md,
    .planning/codebase/INTEGRATIONS.md
  </files>
  <read_first>
    - .planning/phases/06-schema-docs-and-baseline-drift-cleanup/06-RESEARCH.md (§"REQ-DRIFT-03 Target Correction" — the full per-line replacement table)
    - .planning/codebase/CONCERNS.md lines 60-72 (the lint baselines section)
    - .planning/codebase/CONVENTIONS.md lines 12-16 (the No `any` paragraph) and lines 106-112 (the Console gate paragraph)
    - .planning/codebase/INTEGRATIONS.md lines 122-128 (the Logging paragraph)
  </read_first>
  <action>
Step 1 — Edit `.planning/codebase/CONCERNS.md`. Three replacements:

(a) At lines 62-63, find the current text:

```
- **`@typescript-eslint/no-explicit-any`** — `warn` (target: `error`); ~400
  pre-existing `any` types as of `eslint.config.js:181`
```

Replace with:

```
- **`@typescript-eslint/no-explicit-any`** — `warn` (target: `error`); 363
  pre-existing `any` types per `.baselines/eslint-output.json` (counted at the
  `eslint.config.js:181` rule)
```

(b) At lines 66-67, find:

```
- **`no-console`** — `warn` with `allow: ['warn','error']`; **374 disallowed
  calls** at baseline (`scripts/guardrails/console-ratchet.mjs`)
```

Replace with:

```
- **`no-console`** — `warn` with `allow: ['warn','error']`; **39 disallowed
  calls** at baseline per `.baselines/console-prod-baseline.json` (enforced by
  `scripts/guardrails/console-ratchet.mjs`)
```

(c) At lines 68-69, find:

```
- **`eslint-disable` directives** — **132 file-level disables** at baseline
  (`scripts/guardrails/eslint-disable-ratchet.mjs`)
```

Replace with:

```
- **`eslint-disable` directives** — **29 file-level disables** at baseline
  per `.baselines/eslint-file-disable-baseline.json` (enforced by
  `scripts/guardrails/eslint-disable-ratchet.mjs`)
```

Confirmation grep for CONCERNS.md:

```
grep -nE "\b(374|132)\b|~400" .planning/codebase/CONCERNS.md
```

Must return ZERO matches.

Step 2 — Edit `.planning/codebase/CONVENTIONS.md`. Two replacements:

(a) At line 14, find:

```
  ~400 pre-existing baselines (`eslint.config.js:181`); the policy goal is
```

Replace with:

```
  363 pre-existing baselines per `.baselines/eslint-output.json` (rule at `eslint.config.js:181`); the policy goal is
```

(b) At line 110, find:

```
  (baseline 374 disallowed calls — new code cannot add to it).
```

Replace with:

```
  (baseline 39 disallowed calls per `.baselines/console-prod-baseline.json` — new code cannot add to it).
```

Confirmation grep:

```
grep -nE "\b(374|132)\b|~400" .planning/codebase/CONVENTIONS.md
```

Must return ZERO matches.

Step 3 — Edit `.planning/codebase/INTEGRATIONS.md`. One replacement:

At lines 126-127, find:

```
- Console-call ratchet at `scripts/guardrails/console-ratchet.mjs` (baseline 374
  disallowed calls)
```

Replace with:

```
- Console-call ratchet at `scripts/guardrails/console-ratchet.mjs` (baseline 39
  disallowed calls per `.baselines/console-prod-baseline.json`)
```

Confirmation grep:

```
grep -nE "\b(374|132)\b|~400" .planning/codebase/INTEGRATIONS.md
```

Must return ZERO matches.

Step 4 — Cross-file confirmation grep for the entire codebase doc directory:

```
grep -rnE "\b(374|132)\b|~400" .planning/codebase/
```

Must return ZERO matches across all three edited files.

Step 5 — Atomic commit:

```
git add .planning/codebase/CONCERNS.md .planning/codebase/CONVENTIONS.md .planning/codebase/INTEGRATIONS.md
git commit -m "docs(06-02): update stale baseline numbers in .planning/codebase/

REQ-DRIFT-03: replace 374 → 39, 132 → 29, ~400 → 363 in:
- .planning/codebase/CONCERNS.md (3 hit-points: any/console/eslint-disable)
- .planning/codebase/CONVENTIONS.md (2 hit-points: any/console)
- .planning/codebase/INTEGRATIONS.md (1 hit-point: console)

Each rewrite cites the source .baselines/*.json file so future drift is
self-checking. Per Phase 6 research §REQ-DRIFT-03 Target Correction.
"
```

  </action>
  <verify>
    <automated>grep -cE "\b(374|132)\b|~400" .planning/codebase/CONCERNS.md .planning/codebase/CONVENTIONS.md .planning/codebase/INTEGRATIONS.md | grep -vE ":0$" | wc -l | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "\b(374|132)\b" .planning/codebase/CONCERNS.md` returns 0
    - `grep -cE "~400" .planning/codebase/CONCERNS.md` returns 0
    - `grep -cE "\b(374|132)\b" .planning/codebase/CONVENTIONS.md` returns 0
    - `grep -cE "~400" .planning/codebase/CONVENTIONS.md` returns 0
    - `grep -cE "\b(374|132)\b" .planning/codebase/INTEGRATIONS.md` returns 0
    - `grep -c "39" .planning/codebase/CONCERNS.md` returns at least 1
    - `grep -c "29" .planning/codebase/CONCERNS.md` returns at least 1
    - `grep -c "363" .planning/codebase/CONCERNS.md` returns at least 1
    - `grep -c "39" .planning/codebase/CONVENTIONS.md` returns at least 1
    - `grep -c "39" .planning/codebase/INTEGRATIONS.md` returns at least 1
    - `grep -c "console-prod-baseline.json" .planning/codebase/CONCERNS.md` returns at least 1
    - `grep -c "eslint-file-disable-baseline.json" .planning/codebase/CONCERNS.md` returns at least 1
    - `grep -c "eslint-output.json" .planning/codebase/CONCERNS.md` returns at least 1
    - `git log --oneline -1` shows a `docs(06-02): update stale baseline numbers in .planning/codebase/` commit
  </acceptance_criteria>
  <done>All 6 hit-points across the 3 .planning/codebase/ doc files updated with live numbers and source citations; zero stale `374`, `132`, or `~400` remain in the codebase doc directory.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQ-DRIFT-03 + REQ-DRIFT-02 in .planning/PROJECT.md — 6 hit-points</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/phases/06-schema-docs-and-baseline-drift-cleanup/06-RESEARCH.md (§"REQ-DRIFT-03 Target Correction" replacement table for PROJECT.md hits)
    - .planning/PROJECT.md lines 30-40 (M9 target outcomes paragraph at line 35)
    - .planning/PROJECT.md lines 105-115 (validated infra row at line 110-111)
    - .planning/PROJECT.md lines 180-195 (Active M9 backlog section, REQ-DRIFT-02 at 184-185 and REQ-DRIFT-03 at 186-190)
    - .planning/PROJECT.md lines 280-295 (Constraints section, Logging at 286-287)
  </read_first>
  <action>
Step 1 — Edit `.planning/PROJECT.md` line 35 (REQ-DRIFT-03 hit, M9 outcomes).
Find:
```
  CLAUDE.md baseline numbers reflect reality (374→39, 132→29, ~400→363).
```
Replace with:
```
  Codebase doc baseline numbers reflect reality (console 39, eslint-disable 29, explicit-any 363) per `.baselines/console-prod-baseline.json`, `.baselines/eslint-file-disable-baseline.json`, and `.baselines/eslint-output.json`.
```

Step 2 — Edit `.planning/PROJECT.md` lines 110-111 (REQ-DRIFT-03 hit, validated
infra row). Find:

```
- ✓ Console + eslint-disable ratchets prevent debt regression (374 / 132
  baselines) — 2026-02-17
```

Replace with:

```
- ✓ Console + eslint-disable ratchets prevent debt regression (current totals 39 / 29 per `.baselines/*.json`) — 2026-02-17
```

Step 3 — Edit `.planning/PROJECT.md` lines 184-185 (REQ-DRIFT-02 hit). Find:

```
- [ ] **REQ-DRIFT-02**: Remove hardcoded Phoenix truth case counts from docs;
      replace with pointers to `npm run phoenix:truth` for the live count. Two
      historical snapshots disagree (118/118 vs 107/107); current live count is
      262/262.
```

Replace with:

```
- [ ] **REQ-DRIFT-02**: Remove hardcoded Phoenix truth case counts from docs;
      replace with pointers to `npm run phoenix:truth` for the live count.
      Historical snapshots disagree (118/118 vs 107/107); always run
      `npm run phoenix:truth` for the authoritative current count instead of
      committing a number.
```

Step 4 — Edit `.planning/PROJECT.md` lines 186-190 (REQ-DRIFT-03 hit, the
self-describing requirement). Find:

```
- [ ] **REQ-DRIFT-03**: Update CLAUDE.md baseline numbers to match reality.
      CLAUDE.md says "374 / 132 baselines" for console / eslint-disable but
      `.baselines/console-prod-baseline.json` is **39** and
      `.baselines/eslint-file-disable-baseline.json` is **29**. The `~400 any`
      claim should become `363` (per `.baselines/eslint-output.json`).
```

Replace with:

```
- [ ] **REQ-DRIFT-03**: Update stale baseline numbers in `.planning/PROJECT.md`
      and `.planning/codebase/{CONCERNS,CONVENTIONS,INTEGRATIONS}.md` to match
      reality. Those four files said `374 / 132 baselines` for console /
      eslint-disable but `.baselines/console-prod-baseline.json` is **39** and
      `.baselines/eslint-file-disable-baseline.json` is **29**. The `~400 any`
      claim should become `363` (per `.baselines/eslint-output.json`).
      **Note:** CLAUDE.md at the project root does NOT contain `374`, `132`,
      or `~400` — the original ROADMAP phrasing that named CLAUDE.md was wrong.
```

Step 5 — Edit `.planning/PROJECT.md` lines 286-287 (REQ-DRIFT-03 hit,
Constraints / Logging row). Find:

```
- **Logging:** Pino only (per ADR-019). New code cannot increase the 374
  disallowed-`console` baseline or the 132 file-level `eslint-disable` baseline.
```

Replace with:

```
- **Logging:** Pino only (per ADR-019). New code cannot increase the
  disallowed-`console` or file-level `eslint-disable` baselines (current totals
  39 and 29 per `.baselines/console-prod-baseline.json` and
  `.baselines/eslint-file-disable-baseline.json`).
```

Step 6 — Confirmation greps. PROJECT.md should still contain ONE historical
recap that legitimately mentions stale numbers via the M8 close paragraph at
line 152 ("Phoenix truth cases: 258 → 262 ...") AND any reference to "39", "29",
"363" added by the rewrites. The check is: zero `374`, `132`, or `~400` in
actively-rewritten regions:

```
grep -nE "\b(374|132)\b" .planning/PROJECT.md
```

Must return ZERO matches across the entire file.

```
grep -nE "~400" .planning/PROJECT.md
```

Must return ZERO matches.

NOTE: PROJECT.md:152 ("Phoenix truth cases: 258 → 262 ...") is a historical
M8-close recap. It is NOT a REQ-DRIFT-02 hit — it is a frozen-in-time number
describing what Plan 02-05 added. Per research §"REQ-DRIFT-02 Hit List"
classification, that line is `historical-record` and is NOT touched. Verify it
is still present:

```
grep -c "Phoenix truth cases: 258" .planning/PROJECT.md
```

Must return at least 1.

Step 7 — Atomic commit:

```
git add .planning/PROJECT.md
git commit -m "docs(06-02): retarget REQ-DRIFT-03 + clean baseline numbers in PROJECT.md

REQ-DRIFT-02: rewrite REQ-DRIFT-02 statement at lines 184-185 to drop the
hardcoded \`262/262\` example and point at \`npm run phoenix:truth\`.

REQ-DRIFT-03: replace 374 → 39, 132 → 29, ~400 → 363 in 4 hit-points and
retarget the REQ-DRIFT-03 statement at lines 186-190 from CLAUDE.md (which
does NOT contain these numbers) to .planning/PROJECT.md +
.planning/codebase/{CONCERNS,CONVENTIONS,INTEGRATIONS}.md.

Historical recap at line 152 (\"Phoenix truth cases: 258 → 262\") is left
intact — it is a frozen-in-time M8 close record, classified as
historical-record per research §REQ-DRIFT-02 Hit List.
"
```

  </action>
  <verify>
    <automated>test "$(grep -cE '\b(374|132)\b' .planning/PROJECT.md)" = "0" && test "$(grep -cE '~400' .planning/PROJECT.md)" = "0" && test "$(grep -c 'Phoenix truth cases: 258' .planning/PROJECT.md)" -ge "1"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "\b(374|132)\b" .planning/PROJECT.md` returns 0
    - `grep -cE "~400" .planning/PROJECT.md` returns 0
    - `grep -c "current live count is 262/262" .planning/PROJECT.md` returns 0
    - `grep -c "262/262" .planning/PROJECT.md` returns 0 (the only 262/262 hit was the one removed; the historical line 152 says "258 → 262" not "262/262")
    - `grep -c "Phoenix truth cases: 258" .planning/PROJECT.md` returns at least 1 (historical recap preserved)
    - `grep -c "console-prod-baseline.json" .planning/PROJECT.md` returns at least 1
    - `grep -c "eslint-file-disable-baseline.json" .planning/PROJECT.md` returns at least 1
    - `grep -c "eslint-output.json" .planning/PROJECT.md` returns at least 1
    - `grep -c "CLAUDE.md at the project root does NOT contain" .planning/PROJECT.md` returns at least 1 (retargeting note present)
    - `grep -c "npm run phoenix:truth" .planning/PROJECT.md` returns at least 1 (REQ-DRIFT-02 replacement present)
    - `git log --oneline -1` shows a `docs(06-02): retarget REQ-DRIFT-03 + clean baseline numbers in PROJECT.md` commit
  </acceptance_criteria>
  <done>All 5 REQ-DRIFT-03 hit-points + 1 REQ-DRIFT-02 hit-point in PROJECT.md updated; historical M8 recap at line 152 preserved; retargeting note explicit about CLAUDE.md not being a drift target.</done>
</task>

<task type="auto">
  <name>Task 3: Update REQ-DRIFT-02 + REQ-DRIFT-03 statements in .planning/REQUIREMENTS.md and .planning/ROADMAP.md</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/phases/06-schema-docs-and-baseline-drift-cleanup/06-RESEARCH.md (§"REQ-DRIFT-02 Hit List" + §"REQ-DRIFT-03 Target Correction")
    - .planning/REQUIREMENTS.md lines 50-72 (REQ-DRIFT-02 at 54-61 + REQ-DRIFT-03 at 63-71)
    - .planning/ROADMAP.md lines 95-145 (Phase 6 section: REQ-DRIFT-03 at line 102, success criterion 3 at 136-139, success criterion 4 at 140-142)
  </read_first>
  <action>
Step 1 — Edit `.planning/REQUIREMENTS.md` lines 54-61 (REQ-DRIFT-02 block —
the requirement that contains the very number it forbids). Find:
```
- [ ] **REQ-DRIFT-02**: Remove hardcoded Phoenix truth case counts from docs;
      replace with pointers to `npm run phoenix:truth` for the live count. Two
      historical snapshots disagree (118/118 vs 107/107 vs the current 262/262);
      CLAUDE.md memory note "Phoenix truth case count is in drift" already warns
      against quoting numbers from docs. Acceptance: a grep for hardcoded
      `\d+/\d+` truth-case counts across `docs/`, `cheatsheets/`, `README*`, and
      `CLAUDE.md` returns only the live-command pointer (or the count is
      captured in a generated file that the live command refreshes).
```
Replace with:
```
- [ ] **REQ-DRIFT-02**: Remove hardcoded Phoenix truth case counts from
      forward-looking planning docs; replace with pointers to
      `npm run phoenix:truth` for the live count. Historical snapshots
      disagree (118/118 vs 107/107); the CLAUDE.md memory note "Phoenix truth
      case count is in drift" already warns against quoting numbers from docs.
      Scope: forward-looking prescriptions in `.planning/ROADMAP.md`,
      `.planning/REQUIREMENTS.md`, and `.planning/PROJECT.md` only. Historical
      records in `*-SUMMARY.md`, `*-CONTEXT.md`, `*-PLAN.md`, `STATE.md`,
      `CHANGELOG.md`, and `docs/archive/**` are deferred-by-policy and remain.
      Acceptance: a grep for `phoenix.*\d{2,3}/\d{2,3}` and `\d{2,3}/\d{2,3}\s+truth`
      across the 3 forward-looking files returns only the live-command pointer.
```

Step 2 — Edit `.planning/REQUIREMENTS.md` lines 63-71 (REQ-DRIFT-03 block —
retargets the entire requirement). Find:

```
- [ ] **REQ-DRIFT-03**: Update `CLAUDE.md` baseline numbers to match the actual
      `.baselines/` files. Current claims are stale by ~10x: CLAUDE.md says "374
      / 132 baselines" but `.baselines/console-prod-baseline.json` is **39** and
      `.baselines/eslint-file-disable-baseline.json` is **29**. The claim of
      "~400 baselined `any` violations" should become **363** per
      `.baselines/eslint-output.json`. Acceptance: CLAUDE.md numbers match what
      `node -e "console.log(require('./.baselines/console-prod-baseline.json').total)"`
      and the equivalent for the eslint-disable baseline return on the day of
      the commit.
```

Replace with:

```
- [ ] **REQ-DRIFT-03**: Update stale baseline numbers in `.planning/PROJECT.md`
      and `.planning/codebase/{CONCERNS,CONVENTIONS,INTEGRATIONS}.md` to match
      the actual `.baselines/` files. Those four files said "374 / 132
      baselines" for console / eslint-disable but
      `.baselines/console-prod-baseline.json` is **39** and
      `.baselines/eslint-file-disable-baseline.json` is **29**. The claim of
      "~400 baselined `any` violations" should become **363** per
      `.baselines/eslint-output.json`. **Note:** CLAUDE.md at the project root
      does NOT contain `374`, `132`, or `~400` anywhere — the original ROADMAP
      phrasing that named CLAUDE.md was wrong and is being corrected in this
      same plan. Acceptance: the four `.planning/` doc files contain `39`,
      `29`, and `363` (or whatever the live baselines return on the day of the
      commit) AND zero occurrences of `374`, `132`, or `~400` remain in those
      four files.
```

Step 3 — Edit `.planning/ROADMAP.md` line 102 (REQ-DRIFT-03 short description).
Find:

```
- REQ-DRIFT-03 — Update CLAUDE.md baseline numbers (374→39, 132→29, ~400→363)
```

Replace with:

```
- REQ-DRIFT-03 — Update `.planning/` doc baseline numbers (374→39, 132→29, ~400→363) in PROJECT.md, CONCERNS.md, CONVENTIONS.md, INTEGRATIONS.md (CLAUDE.md is NOT a target — original phrasing was wrong)
```

Step 4 — Edit `.planning/ROADMAP.md` lines 140-142 (Phase 6 success criterion 4
— REQ-DRIFT-02 hit). Find:

```
4. `npm run check` exits 0; `npm run validate:core` exits 0;
   `npm run phoenix:truth` stays at 262/262 (or whatever the live count is on
   the day Phase 6 lands).
```

Replace with:

```
4. `npm run check` exits 0; `npm run validate:core` exits 0;
   `npm run phoenix:truth` exits 0 with the live count unchanged from the
   pre-phase baseline (run `npm run phoenix:truth` for the live count — do not
   commit a number).
```

Step 5 — Verify `.planning/ROADMAP.md` lines 136-139 (Phase 6 success criterion
3 — already states the correct numbers 39 / 29 / 363). Read the current lines:

```
grep -n "console-prod-baseline.json\|eslint-file-disable-baseline.json\|eslint-output.json" .planning/ROADMAP.md
```

The success criterion at lines 136-139 already cites the correct live numbers
from the right files. NO change needed there. Confirm by inspection.

Step 6 — Confirmation greps for both files:

```
grep -nE "\b(374|132)\b" .planning/REQUIREMENTS.md
grep -nE "~400" .planning/REQUIREMENTS.md
```

Both must return ZERO matches.

```
grep -nE "\b(374|132)\b" .planning/ROADMAP.md
```

Must return ZERO matches. NOTE: line 102 (the short REQ-DRIFT-03 description)
intentionally contains `374→39` etc as drift-history reference. The grep
includes that line. Resolution: the rewrite at Step 3 uses arrows `→` which the
regex `\b(374|132)\b` DOES match. To preserve the historical note while
satisfying the acceptance, REPHRASE the Step 3 line to NOT contain bare 374
or 132. Use this alternative replacement instead of Step 3's:

```
- REQ-DRIFT-03 — Update stale `.planning/` doc baseline numbers (console, eslint-disable, explicit-any) to match `.baselines/*.json` reality, in PROJECT.md, CONCERNS.md, CONVENTIONS.md, INTEGRATIONS.md. CLAUDE.md is NOT a target — original phrasing was wrong.
```

Use THIS version of Step 3, not the arrow version. Re-confirm:

```
grep -nE "\b(374|132)\b" .planning/ROADMAP.md
```

Must return ZERO matches.

Step 7 — Phoenix-count drift post-check across the 3 forward-looking files:

```
grep -nE "phoenix.*[0-9]{2,3}/[0-9]{2,3}|[0-9]{2,3}/[0-9]{2,3}\s+truth|262/262" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/PROJECT.md
```

Must return ZERO matches. (Hits in `.planning/STATE.md` and
`.planning/phases/**` are deferred-by-policy and remain.)

Step 8 — Atomic commit:

```
git add .planning/REQUIREMENTS.md .planning/ROADMAP.md
git commit -m "docs(06-02): retarget REQ-DRIFT-02 + REQ-DRIFT-03 in REQUIREMENTS.md and ROADMAP.md

REQ-DRIFT-02: rewrite REQ-DRIFT-02 statement in REQUIREMENTS.md to drop the
hardcoded \`262/262\` example and document the deferred-by-policy scope (only
3 forward-looking files; historical records left intact). Rewrite ROADMAP.md
Phase 6 success criterion 4 to drop the embedded number.

REQ-DRIFT-03: retarget REQ-DRIFT-03 in REQUIREMENTS.md and ROADMAP.md from
CLAUDE.md (which does NOT contain 374, 132, or ~400) to .planning/PROJECT.md
and .planning/codebase/{CONCERNS,CONVENTIONS,INTEGRATIONS}.md. Adds an
explicit note that CLAUDE.md at project root is NOT a drift target so the
next session does not waste time hunting non-existent occurrences.
"
```

  </action>
  <verify>
    <automated>test "$(grep -cE '\b(374|132)\b' .planning/REQUIREMENTS.md)" = "0" && test "$(grep -cE '\b(374|132)\b' .planning/ROADMAP.md)" = "0" && test "$(grep -cE '~400' .planning/REQUIREMENTS.md .planning/ROADMAP.md)" = "0" && test "$(grep -cE 'phoenix.*[0-9]{2,3}/[0-9]{2,3}|262/262' .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/PROJECT.md | grep -vE ':0$' | wc -l)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "\b(374|132)\b" .planning/REQUIREMENTS.md` returns 0
    - `grep -cE "~400" .planning/REQUIREMENTS.md` returns 0
    - `grep -cE "\b(374|132)\b" .planning/ROADMAP.md` returns 0
    - `grep -cE "~400" .planning/ROADMAP.md` returns 0
    - `grep -cE "262/262" .planning/REQUIREMENTS.md` returns 0
    - `grep -cE "262/262" .planning/ROADMAP.md` returns 0
    - `grep -c "CLAUDE.md at the project root does NOT contain" .planning/REQUIREMENTS.md` returns at least 1
    - `grep -c "CLAUDE.md is NOT a target" .planning/ROADMAP.md` returns at least 1
    - `grep -c "deferred-by-policy" .planning/REQUIREMENTS.md` returns at least 1
    - `grep -c "npm run phoenix:truth" .planning/ROADMAP.md` returns at least 2 (the original Phoenix-specific paragraph + the rewritten success criterion 4)
    - `grep -cE "phoenix.*[0-9]{2,3}/[0-9]{2,3}|[0-9]{2,3}/[0-9]{2,3}\s+truth" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/PROJECT.md` returns 0
    - `git log --oneline -1` shows a `docs(06-02): retarget REQ-DRIFT-02 + REQ-DRIFT-03` commit
  </acceptance_criteria>
  <done>REQUIREMENTS.md and ROADMAP.md REQ-DRIFT-02 and REQ-DRIFT-03 statements rewritten; CLAUDE.md retarget note explicit; deferred-by-policy scope documented; zero hardcoded baseline numbers OR phoenix counts remain in the 3 forward-looking files.</done>
</task>

<task type="auto">
  <name>Task 4: Cross-file verification + run all phase gates</name>
  <files>(no source edits — verification only)</files>
  <read_first>
    - .planning/phases/06-schema-docs-and-baseline-drift-cleanup/06-RESEARCH.md (§"Validation Architecture" — the gate matrix for Plan 06-02)
  </read_first>
  <action>
Step 1 — Final cross-file grep for stale baseline numbers across ALL 6
edited files:

```
grep -rnE "\b(374|132)\b|~400" .planning/PROJECT.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/codebase/CONCERNS.md .planning/codebase/CONVENTIONS.md .planning/codebase/INTEGRATIONS.md
```

MUST return ZERO matches. If any match appears, identify which task missed it,
fix it, and amend the corresponding commit (or land a follow-up commit).

Step 2 — Final cross-file grep for hardcoded phoenix counts in the 3
forward-looking files:

```
grep -nE "phoenix.*[0-9]{2,3}/[0-9]{2,3}|[0-9]{2,3}/[0-9]{2,3}\s+truth|262/262" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/PROJECT.md
```

MUST return ZERO matches. NOTE: PROJECT.md:152 contains "Phoenix truth cases:
258 → 262" which is a historical recap and should NOT match the regex
(`258 → 262` does not match `\d/\d` or `phoenix.*\d/\d`). If it accidentally
matches, the regex needs tightening — surface and resolve.

Step 3 — Verify the deferred-by-policy files were NOT touched. List the
deferred-by-policy file paths and confirm they are NOT in `git diff --name-only`
for this plan's commits:

```
git log --since="1 hour ago" --pretty=format: --name-only | sort -u | grep -E "\.planning/(STATE\.md|phases/|CHANGELOG)|docs/archive|^archive/|docs/PHOENIX-SOT" || echo "GOOD: no deferred-by-policy files touched"
```

Expected: `GOOD: no deferred-by-policy files touched`. If a deferred file shows
up, it was edited by mistake — revert that edit before continuing.

Step 4 — Verify CLAUDE.md at project root was NOT touched (it is NOT a drift
target, it should not be in the diff):

```
git log --since="1 hour ago" --pretty=format: --name-only | grep -x "CLAUDE.md" || echo "GOOD: CLAUDE.md untouched"
```

Expected: `GOOD: CLAUDE.md untouched`.

Step 5 — Run the type check (should be a no-op since edits are prose only):

```
npm run check
```

Must exit 0.

Step 6 — Run validate:core:

```
npm run validate:core
```

Must exit 0.

Step 7 — Run lint:

```
npm run lint
```

Must exit 0.

Step 8 — Run the tripwire ratchets (should pass — they read .baselines/\*.json
at runtime, not the prose docs we edited):

```
node scripts/guardrails/console-ratchet.mjs
node scripts/guardrails/eslint-disable-ratchet.mjs
```

Both must exit 0.

Step 9 — Run phoenix:truth as the Phase 6 regression check (Plan 06-02 touches
no calc paths so the count must be unchanged):

```
TZ=UTC npm run phoenix:truth 2>&1 | tee 06-02-phoenix.log
```

Must exit 0. Read the `Tests:` passed line and record it in the SUMMARY (do NOT
hardcode the number into this plan or any committed file).

Step 10 — Pre-push baseline simulation:

```
npm run baseline:check
```

Must exit 0.

Step 11 — Cleanup the scratch log:

```
rm -f 06-02-phoenix.log
git status --short
```

`git status --short` should show ZERO untracked files. The only changes should
be the 4 commits authored by Tasks 1-3. </action> <verify> <automated>test
"$(grep -rcE '\b(374|132)\b|~400' .planning/PROJECT.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/codebase/CONCERNS.md .planning/codebase/CONVENTIONS.md .planning/codebase/INTEGRATIONS.md | grep -vE ':0$'
| wc -l)" = "0" && test
"$(grep -cE 'phoenix.*[0-9]{2,3}/[0-9]{2,3}|262/262' .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/PROJECT.md | grep -vE ':0$'
| wc -l)" = "0" && npm run check && npm run validate:core && npm run lint &&
node scripts/guardrails/console-ratchet.mjs && node
scripts/guardrails/eslint-disable-ratchet.mjs && npm run
baseline:check</automated> </verify> <acceptance_criteria> - Cross-file grep
`\b(374|132)\b` across all 6 edited files returns 0 matches - Cross-file grep
`~400` across all 6 edited files returns 0 matches - Cross-file grep for
phoenix-count regex across the 3 forward-looking files returns 0 matches -
`git log --since="1 hour ago" --pretty=format: --name-only | sort -u` shows ONLY
the 6 expected files (PROJECT.md, ROADMAP.md, REQUIREMENTS.md, CONCERNS.md,
CONVENTIONS.md, INTEGRATIONS.md) — no STATE.md, no phases/**, no archive/**, no
CLAUDE.md - `npm run check` exits 0 - `npm run validate:core` exits 0 -
`npm run lint` exits 0 - `node scripts/guardrails/console-ratchet.mjs` exits 0 -
`node scripts/guardrails/eslint-disable-ratchet.mjs` exits 0 -
`npm run baseline:check` exits 0 - `TZ=UTC npm run phoenix:truth` exits 0 with
the live count unchanged from the day this plan started - `git status --short`
shows zero untracked files - At least 3 atomic commits landed by Tasks 1-3
(codebase/, PROJECT.md, REQUIREMENTS.md+ROADMAP.md) </acceptance_criteria>
<done>All 6 files cleaned of stale baseline numbers, all 3 forward-looking files
cleaned of hardcoded phoenix counts, zero deferred-by-policy files touched,
CLAUDE.md untouched, all phase gates green, scratch log cleaned, ready for
SUMMARY.</done> </task>

</tasks>

<threat_model>

## Trust Boundaries

This plan edits prose only — no trust boundaries are added, modified, or
removed. No code, no schema, no tests, no config.

## STRIDE Threat Register

| Threat ID  | Category               | Component                                               | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                             |
| ---------- | ---------------------- | ------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-02-01 | Tampering              | `.planning/REQUIREMENTS.md` REQ-DRIFT-03 self-reference | mitigate    | The old text contains the very word "CLAUDE.md" that needs to be replaced. Task 3's replacement is scoped to the REQ-DRIFT-03 block ONLY (lines 63-71); it does NOT use a global `s/CLAUDE.md/.../g` substitution that would damage other CLAUDE.md mentions. Verified by per-task acceptance grep on the surrounding file. |
| T-06-02-02 | Information Disclosure | `06-02-phoenix.log` scratch file                        | accept      | The scratch log contains test output only, no PII. Task 4 step 11 explicitly removes it before plan close.                                                                                                                                                                                                                  |
| T-06-02-03 | Denial of Service      | N/A                                                     | accept      | Plan 06-02 has zero tripwire impact per research §"Tripwire Script Audit" — `console-ratchet.mjs` and `eslint-disable-ratchet.mjs` read `.baselines/*.json` at runtime, not the prose docs being edited. The plan cannot break the pre-push hook.                                                                           |
| T-06-02-04 | Repudiation            | N/A                                                     | accept      | Each edit is a separate atomic commit per task; `git revert <sha>` cleanly rolls back any single task.                                                                                                                                                                                                                      |
| T-06-02-05 | Spoofing               | N/A                                                     | accept      | No auth surface touched.                                                                                                                                                                                                                                                                                                    |
| T-06-02-06 | Elevation of Privilege | N/A                                                     | accept      | No auth surface touched.                                                                                                                                                                                                                                                                                                    |

</threat_model>

<verification>
- Zero matches for `\b(374|132)\b` across `.planning/PROJECT.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/codebase/CONCERNS.md`, `.planning/codebase/CONVENTIONS.md`, `.planning/codebase/INTEGRATIONS.md`.
- Zero matches for `~400` across the same 6 files.
- The replacement live numbers (`39`, `29`, `363`) appear at least once in each target file with an inline `.baselines/*.json` source citation.
- Zero matches for `262/262` or `phoenix.*\d{2,3}/\d{2,3}` across the 3 forward-looking files (`.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/PROJECT.md`).
- `.planning/PROJECT.md:152` historical recap ("Phoenix truth cases: 258 → 262 ...") is preserved untouched.
- `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` both contain explicit notes that CLAUDE.md at project root is NOT a drift target (resolving the original ROADMAP phrasing error).
- CLAUDE.md at project root is NOT in the diff for this plan (untouched).
- No file under `.planning/STATE.md`, `.planning/phases/**`, `.planning/CHANGELOG.md`, `docs/archive/**`, `archive/**`, or `docs/PHOENIX-SOT/**` is in the diff (deferred-by-policy preserved).
- `npm run check`, `npm run validate:core`, `npm run lint`, `npm run baseline:check`, both guardrail ratchets all exit 0.
- `npm run phoenix:truth` exits 0 with the live count unchanged from the start of the plan.
- At least 3 atomic commits landed (one per task that performed edits).
- `git status --short` at plan close shows zero untracked scratch artifacts.
</verification>

<success_criteria>

1. Zero `374`, `132`, or `~400` remain in any of the 6 edited files (verified by
   grep).
2. The replacement numbers `39`, `29`, `363` appear at least once in the right
   files with `.baselines/*.json` source citations.
3. Zero hardcoded phoenix truth counts (`262/262`, `\d{2,3}/\d{2,3}\s+truth`,
   `phoenix.*\d{2,3}/\d{2,3}`) remain in the 3 forward-looking files.
4. REQ-DRIFT-03 statements in REQUIREMENTS.md and ROADMAP.md retarget CLAUDE.md
   to the correct .planning/ doc files AND explicitly note CLAUDE.md is NOT a
   drift target.
5. REQ-DRIFT-02 statements in REQUIREMENTS.md and PROJECT.md drop the embedded
   `262/262` example and document the deferred-by-policy scope.
6. Historical recap at `.planning/PROJECT.md:152` ("Phoenix truth cases: 258 →
   262") is preserved untouched.
7. Zero deferred-by-policy files (STATE.md, phases/**, CHANGELOG.md, archive/**,
   PHOENIX-SOT/\*\*) are in the diff.
8. CLAUDE.md at project root is NOT in the diff.
9. `npm run check`, `npm run validate:core`, `npm run lint`,
   `npm run baseline:check`, both guardrail ratchets all exit 0.
10. `npm run phoenix:truth` count is unchanged from the day this plan started.
11. At least 3 atomic commits landed (one per editing task). </success_criteria>

<output>
After completion, create
`.planning/phases/06-schema-docs-and-baseline-drift-cleanup/06-02-SUMMARY.md`
following the summary template. The SUMMARY MUST capture:

- The 6 files edited and a one-line summary of each edit per file (count of
  hit-points changed, before/after representative example).
- The retargeting note: "CLAUDE.md at project root is NOT a drift target — the
  original ROADMAP/REQUIREMENTS phrasing was wrong" — confirm both
  REQUIREMENTS.md and ROADMAP.md now contain this note.
- Confirmation that PROJECT.md:152 historical recap ("Phoenix truth cases: 258 →
  262") was preserved.
- The list of deferred-by-policy files that were intentionally NOT touched.
- The post-plan grep results (all four post-checks: `\b(374|132)\b` count,
  `~400` count, phoenix-count regex count, deferred-file diff list).
- The list of atomic commit SHAs landed by this plan (expect ~3).
- The pre/post phoenix:truth `Tests:` line (read from the actual log output — do
  NOT hardcode the number).
- Any surprises encountered (e.g., a hit point that was already cleaned by a
  prior commit, or a hit point that surfaced in a file the planner didn't list).
  </output>
