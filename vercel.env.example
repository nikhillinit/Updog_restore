# Vercel Environment Variables
# Add these in Vercel Dashboard → Settings → Environment Variables

# REQUIRED: Memory configuration for build process
# Prevents OOM errors during builds, especially with large bundles
# Add this in Vercel Dashboard → Settings → Environment Variables
NODE_OPTIONS=--max-old-space-size=4096

# REQUIRED: Node.js version (should match package.json engines)
# Set in Vercel Dashboard → Settings → General → Node.js Version
# Recommended: 20.x

# Production environment variables (replace with your actual values)
# DATABASE_URL=your-production-database-url
# REDIS_URL=your-production-redis-url
# SESSION_SECRET=your-production-secret

# Optional: Feature flags
# ENABLE_WEBSOCKETS=false
# ENABLE_WORKERS=false