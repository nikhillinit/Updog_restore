/**
 * Press On v2 mock fixtures · barrel re-export.
 *
 * Modules under ./mock-data/ are split by domain so each stays under the
 * codacy file-size threshold and reads as a single responsibility.
 * Swap for real selectors when wiring to the data layer.
 */
export * from './mock-data/companies';
export * from './mock-data/scenarios';
export * from './mock-data/lps';
export * from './mock-data/operating';
