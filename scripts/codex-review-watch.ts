#!/usr/bin/env tsx

console.error(
  '[codex-review-watch] Retired: the local review agent entrypoint is no longer wired into app tooling.'
);
console.error('Use current PR review workflows and repo verification commands instead.');
process.exit(1);
