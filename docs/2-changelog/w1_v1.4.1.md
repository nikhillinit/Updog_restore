# Changelog - Week 1, 05-08-2026, V. 1.4.1

**Release Date**: Week 1, 05-08-2026 at 21:18 **Version**: 1.4.1 (previously
1.4.0) **Object**: feat(audit): WS1 surface contract matrix - 470-row
release-proof surface inventory, validators, G1 tooling **Code review**:
`docs/3-code-review/CR_w1_v1.4.1.md` (Codex loop, 5 rounds -> APPROVED)

## Changes

Implements F_1.2.0 child plan A (WS1, gate G1): the checked-in surface contract
matrix that defines "every v1.4 surface" for the release-proof program. Audit
tooling and tests only — zero product runtime change.

- **Matrix artifacts** (`audit/surface-contract-matrix/`): `matrix.json` (470
  rows: 400 http-api incl. 9 listener-qualified, 43 client-route, 12 worker-job,
  9 dormant-ui, 3 scheduler, 1 event-handler, 1 websocket, 1 vercel-function),
  `source-inventory.json` (exact-set row ids + source-to-row mappings +
  source-hash gate), curated dispositions (`listener-dispositions.json`,
  `dormant-inventory.json`, `dormant-candidates.json`, `orphans.json`,
  `runtime-exclusions.json`, `auth-overrides.json`, `condition-overrides.json`,
  `definition-overrides.json`), `requirements.json` (master-plan mandatory
  classification families), `boot-proofs.json`, generated `MATRIX.md` (G1 review
  pack).
- **Tooling** (`scripts/`): `seed-matrix.mjs` (KG + registry + listener
  extraction, merge-not-clobber with field-level ownership, byte-identical
  double-seed), `inspect-runtime.mjs` (multi-profile hermetic runtime
  instrumentation of `makeApp()`/`createServer()` with layer tagging),
  `boot-proof.mjs` (per-deployment built-artifact boot proof with
  method/status-exact probes), `classify-pass.mjs`, `validate-matrix.mjs` (incl.
  shared `validateRowIntegrity`), `render-matrix.mjs`, `approve-matrix.mjs`
  (sole approval mutation path; `--close-g1` runs the full tentative
  closed-state validation before writing).
- **CI gate**: `tests/unit/audit/` (23 tests) — exact-set equality,
  source-hash + membership gates, fingerprint recomputation, discovery re-runs,
  render determinism, merge-engine fixtures, real-app inspector test,
  phase-downgrade check (ci-unified prior-ref plumbing).
- **Boot findings recorded**: `railway-api` FAILED (built `dist/index.js` starts
  no listener — WS5-owned), `railway-worker` FAILED (Redis-less environment),
  `vercel-api`/`vercel-web` proven, others unproven.
- G1 review (approving 470 proposed rows, off-row dispositions, 5 persona roles)
  remains deliberately open — it is the user's gate, closed later via
  `approve-matrix.mjs --close-g1`.
