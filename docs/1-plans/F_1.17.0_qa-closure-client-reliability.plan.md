# QA Closure Client Reliability Implementation Plan

**Governing policy:**
`docs/governance/solo-internal-change-and-production-policy.md` (resolve from
`origin/main`). This plan grants no merge, production, schema, or release
authority. Merge, deploy, production, and release remain separate owner actions.

Reviewed checkout: `origin/main` on 2026-09-24. This plan covers the unstarted
client half of September 2026 QA closure after prior server-scope and
position-value work. Repository verification found all ten client items still
require work, although C9's direct `/help` route and several C10 route files are
already compliant and must remain unchanged.

## Overview

Implement C1-C10 in three reviewable PRs, ordered by financial correctness and
mutation safety, merged in this order:

1. **PR 1 - mutation and error-boundary correctness:** C3, C8.
2. **PR 1b - durable create receipts:** C2 (server receipts, one additive
   migration, client intent keys).
3. **PR 2 - query, state, accessibility, and readable-text closure:** C1, C4-C7,
   C9, C10.

PR 1 goes first because C3 currently sends hard-coded optimistic-lock version
`1` for both preview and commit
(`client/src/components/portfolio/tabs/ReallocationTab.tsx:25`, `:65`, `:125`).
The shortest correct repair reuses the existing allocations query and
`apiRequest` error channel; it adds no service, state manager, retry layer, or
new schema, but does require a narrow cross-stack row-version contract repair
and a canonical lock order inside the existing shared allocation writer. PR 2
stays mostly client-facing. C5 adds one narrow server-side exact-baseline lookup
because the existing client report list is capped at 50; the remaining items
share the same unit/build gates.

Settled decisions remain closed:

- Team access keeps universal safe reads and strict writes through existing
  authorization helpers.
- No rebrand: primary remains charcoal `#292929`; readable muted copy uses
  `text-presson-textMuted` (`#5A5A5A`) (`DESIGN.md:58`, `DESIGN.md:65-70`). Do
  not remap the Tailwind charcoal scale.
- LP portal remains feature-flagged and out of scope.
- `ReallocationTab` is repaired, not removed.
- Internal-tool scale is about five users: no new framework, service, generic
  retry layer, or design abstraction.

## Problem Statement

### Verified defects

| Item | Checkout finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Disposition                                                                  |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| C1   | Pipeline deal query has no fund guard and can call an unscoped URL (`client/src/pages/pipeline.tsx:425-453`); Add/Import controls and modal mounts remain available without a fund (`client/src/pages/pipeline.tsx:555-565`, `:775-779`, `:841-850`). Existing empty surface is `pipeline-empty-state` (`client/src/pages/pipeline.tsx:134-163`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Fix.                                                                         |
| C2   | Add deal, add company, and import confirm issue writes without caller-owned idempotency headers (`client/src/components/pipeline/AddDealModal.tsx:119-146`; `client/src/components/portfolio/tabs/AddCompanyDialog.tsx:87-104`; `client/src/components/pipeline/ImportDealsModal.tsx:156-169`). `apiRequest` already merges caller headers (`client/src/lib/queryClient.ts:105-116`). The server rejects reuse after a changed request fingerprint (`server/middleware/idempotency.ts:289`), while form values can change after failure (`client/src/components/portfolio/tabs/AddCompanyDialog.tsx:116`). All three server routes already require a key (`server/routes/deal-pipeline.ts:153-158`, `:677-682`; `server/routes/portfolio-companies.ts:289-295`).                                                                                                                                    | Fix client plus durable server receipts (PR 1b).                             |
| C3   | Preview and commit both hard-code version `1` (`ReallocationTab.tsx:25`, `:65`, `:125`). Latest allocations carry `allocation_version` per company (`client/src/components/portfolio/tabs/types.ts:12-24`), while active writers lock and increment individual company rows (`server/services/allocation-write-service.ts:100-145`; `server/services/fund-moic-input-service.ts:163-197`) and new rows start at version 1 (`server/storage.ts:154-184`). Active integration coverage preserves mixed versions after a partial write (`tests/integration/allocation-scenario-apply.test.ts:225-245`). The reallocation route instead enforces all-row equality (`server/routes/reallocation.ts:258-277`, `:449-470`) and commit omits conflict details (`server/routes/reallocation.ts:618-623`). The quarantined all-equal expectation is obsolete (`tests/unit/reallocation-api.test.ts:495-530`). | Fix first; cross-stack client plus route repair.                             |
| C4   | Performance timeseries and breakdown queries expose loading/refetch only; their independent error states are ignored (`client/src/components/performance/PerformanceDashboard.tsx:156-175`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Fix.                                                                         |
| C5   | Current-baseline empty-alert copy always claims acceptable variance (`client/src/pages/variance-tracking.tsx:1770-1781`). Server `lastAnalysisDate` comes from the latest report for any baseline (`server/routes/variance.ts:1017-1044`), while the client report list is capped at the newest 50 (`client/src/hooks/useVarianceData.ts:126-137`; `server/services/variance-tracking/calculation-service.ts:177-189`). A valid older report for the current baseline can therefore be invisible even though reports expose `baselineId` (`shared/variance-validation.ts:436`).                                                                                                                                                                                                                                                                                                                     | Fix with an exact-baseline server query; do not scan the capped client list. |
| C6   | A passkey verification failure writes global `error` (`client/src/pages/shared-dashboard.tsx:115-138`), and global error rendering replaces the passkey form (`client/src/pages/shared-dashboard.tsx:251-270`). Verification has separate 400/404 failures and a five-attempt 429 path (`server/routes/shares.ts:78`, `:699`); all must remain inline. Form label/input/error associations are incomplete (`client/src/pages/shared-dashboard.tsx:282-299`).                                                                                                                                                                                                                                                                                                                                                                                                                                        | Fix.                                                                         |
| C7   | Scenario comparison overflow wrapper is not a named keyboard-focusable region (`client/src/components/fund-results/ScenarioComparisonTable.tsx:195-197`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Fix.                                                                         |
| C8   | Raw reserve/forecast calculation details can cross the route boundary: aggregator embeds the database message (`server/services/metrics-aggregator.ts:511-527`), route serializes it (`server/routes/dual-forecast.ts:61-75`), client hook/readiness mapping carries it (`client/src/hooks/useDualForecast.ts:32-37`; `client/src/pages/fund-model-results/use-readiness-rollup.ts:36-44`; `client/src/pages/fund-model-results/readiness-rollup.ts:118-122`), and rollup renders it (`client/src/pages/fund-model-results/FundReadinessRollup.tsx:89-95`).                                                                                                                                                                                                                                                                                                                                         | Fix at server serialization boundary; keep raw detail in server log only.    |
| C9   | Setup gating enables only dashboard (`client/src/components/layout/navigation-config.ts:147-149`), disabling Help in both consumers of the shared map. Direct `/help` is already unprotected (`shared/routes/app-route-definitions.ts:8-34`) and mounted (`client/src/app/app-routes.tsx:102-110`); do not change routing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Fix navigation only; retain verified route behavior.                         |
| C10  | Readable copy still uses low-contrast `text-charcoal-400/500` on the named result, scenario, MOIC, sensitivity, variance, and public-share surfaces. The inventory also includes readable copy in `client/src/pages/fund-model-results/states.tsx:35` and `client/src/components/scenarios/CreateMethodologyScenarioModal.tsx:262`. Icons such as variance empty-state glyphs are decorative (`variance-tracking.tsx:838`, `:880`, `:1417`, `:1771`, `:1909`, `:1938`). The general accessibility suite scans only selected regions and critical findings (`tests/e2e/accessibility.spec.ts:32-44`), so it is not a complete source inventory.                                                                                                                                                                                                                                                      | Replace text only; retain icons/decorative marks.                            |

No item is dropped as fully fixed. C9 direct routing and C10 already-tokenized
files are explicitly preserved rather than rewritten.

## Solution Architecture

### PR 1 - C3, C8

#### C3 row-scoped optimistic-lock repair

Use `allocation_version` per proposed company from `useLatestAllocations`, keyed
by company ID. Do not require one fund-wide version: active writers lock and
increment individual company rows
(`server/services/allocation-write-service.ts:100-145`;
`server/services/fund-moic-input-service.ts:163-197`), new rows start at version
1 (`server/storage.ts:154-184`), and active integration coverage preserves mixed
versions after a partial write
(`tests/integration/allocation-scenario-apply.test.ts:225-245`). The quarantined
all-equal expectation is replaced, not restored
(`tests/unit/reallocation-api.test.ts:495-530`).

Required flow:

1. Load portfolio companies and latest allocations in parallel with existing
   hooks. Build a `company_id -> allocation_version` map from the allocations
   response (`client/src/components/portfolio/tabs/types.ts:12-24`). Enable
   preview only when every selected company has a positive version. Mixed
   versions elsewhere in the fund are valid; do not guess, take a maximum, or
   fall back to `1`.
2. Extend each proposed allocation with `expected_version`. The preview and
   commit payloads carry that value per company; the successful preview freezes
   the exact proposed rows and expected versions that commit will use. Remove
   the scalar `current_version` contract. Reject duplicate `company_id` values
   in the preview and commit request schemas with one Zod `.refine` (400 before
   any transaction); the current schema accepts duplicates
   (`server/routes/reallocation.ts:37-48`), which would break the
   one-update/one-audit-per-company invariant. Reuse the duplicate message shape
   of the shared writer's guard
   (`server/services/allocation-write-service.ts:79-97`).
3. Validate preview conflicts only for proposed company IDs. On conflict, return
   `details.current_versions` as company/version objects from the route, sorted
   by ascending `company_id`: `{ company_id: number, current_version: number }`.
   The existing `ApiError` already preserves non-5xx `details`
   (`client/src/lib/queryClient.ts:130-160`), so map that field in the two
   reallocation hooks without changing `queryClient.ts`.
4. In the commit transaction, select only proposed company IDs with
   `ORDER BY id FOR UPDATE`, then compare each locked row with its
   `expected_version`. Lock order must not follow request order. A stale row
   aborts the transaction before any update. The existing shared writer locks
   rows one at a time in request order (`updateCompanyAllocation` at
   `server/services/allocation-write-service.ts:100-111`, looped at `:226-236`),
   so opposite-order overlapping requests across the two writers can deadlock
   instead of yielding one success and one 409. Sort a copy of `updates` by
   `company_id` for that lock/update loop inside `applyAllocationUpdates`; keep
   the caller's order for the audit payload (`:184`) so both callers
   (`server/routes/allocations.ts:440`;
   `server/services/allocation-scenario-service.ts:1477`) are fixed once. The
   single-row MOIC input writer
   (`server/services/fund-moic-input-service.ts:163-197`) cannot form a lock
   cycle and stays unchanged.
5. Update only proposed rows and increment each row's own version by one. Leave
   all unproposed rows and their versions untouched. Return `new_versions` as an
   ascending-`company_id` array of
   `{ company_id: number, new_version: number }`; preserve `updated_count` as
   the number of changed proposed rows.
6. Write one existing-schema `reallocation_audit` row per proposed company,
   using that row's expected/resulting versions and exact allocation delta in
   `changes_json`. Return `audit_ids` as an ascending-`company_id` array of
   `{ company_id: number, audit_id: string }`, where `audit_id` is the UUID of
   that company's audit row. Do not invent a fund revision or bump every fund
   row; a true fund revision would require a dedicated column and every writer
   to adopt it. The current audit table has scalar version columns and a UUID
   primary key (`migrations/0001_certain_miracleman.sql:311-319`), so
   per-company audit rows keep those columns truthful without a migration.
7. On preview or commit 409: clear stale preview, refetch latest allocations,
   keep selected companies and reason, then require a fresh preview. Display
   refresh/re-preview copy; never retry commit automatically or silently
   overwrite.
8. Fence late preview responses. Today preview resets only when
   `selectedCompanies` changes (`ReallocationTab.tsx:41-44`), and an in-flight
   `onSuccess` still stores its result after an edit, reset, or fund switch. At
   `mutate()`, capture a fingerprint of `fundId` plus the sorted normalized
   proposed rows and expected versions in a ref. `onSuccess` discards the
   response unless the current fingerprint still matches; stored preview keeps
   its fingerprint, and commit stays disabled unless it equals the fingerprint
   of the displayed inputs. Edit, reset, and fund switch therefore always
   require a fresh preview.

This is an intentional cross-stack repair: the PR description must name the
client/server request, transaction, response, and audit contract change. No
schema migration or fund-wide revision is in scope. **UNVERIFIED:** external
consumers of the current scalar reallocation success/audit response are not
visible in this checkout; implementation must confirm that deployment consumers
accept the row-scoped response before changing it.

#### C8 stable forecast error envelope

Keep raw errors in `routeLog.error` (`server/routes/dual-forecast.ts:54-56`).
For HTTP responses:

- Preserve stable error codes and status mapping (`INSUFFICIENT_DATA` remains
  404; other calculation failures remain 500).
- Replace calculation `message` with generic user-safe copy.
- Remove raw `details` from the generic 500 response.
- Do not serialize table names, SQL, parameters, provider details, or exception
  messages.

No client error-redaction layer: `getErrorMessage` already suppresses every 5xx
body (`client/src/lib/http-response.ts:10-11`), and the hook test pins the
generic behavior (`tests/unit/hooks/useDualForecast.test.tsx:200`). Update the
held-reference route test that currently expects raw text
(`tests/unit/routes/dual-forecast-route.test.ts:181`). Fixing the route boundary
once covers every consumer and avoids inconsistent string filtering.

### PR 1b - C2 durable create receipts

C2 is a server-side P1, not a client-only fix. Failure window: the business
`COMMIT` succeeds, then the acknowledgment or the Redis cache write is lost; the
same pinned key has no receipt, so the retry repeats the mutation. The generic
middleware caches only 200/201 in Redis with a default 300-second TTL and stores
nothing without a Redis client (`server/middleware/idempotency.ts:211-217`,
`:267`), and its own test proves a same-key retry after a 500 runs the handler
again (`tests/unit/middleware/idempotency-dedupe.test.ts:275-293`). Today
`createDeal` commits the deal before a separate activity insert
(`server/services/deal-pipeline-service.ts:219-233`) and `confirmImport` inserts
row by row with no transaction (`:636-656`). PR 1b persists a completed receipt
in the same PostgreSQL transaction as each mutation.

#### One state machine, two storage adapters

Reuse `runIdempotentCommand` and `replayIdempotentCommandIfPresent`
(`server/lib/idempotent-command.ts:66-102`) as the single state machine: it
already owns the canonical request hash, changed-payload rejection
(`409 IDEMPOTENCY_KEY_REUSE`), replay, and reload after a lost insert race. Its
row type for these routes is the stored `{ responseStatus, responseBody }`, so
replay returns the original status and body. The hashed request binds the parsed
body, `fundId`, the acting user ID, an operation literal (`deal_create`,
`deal_import`, or `company_create`), and a contract-version literal. Each
adapter's `insert(requestHash)` runs one `db.transaction`:

1. Claim the key with `INSERT ... ON CONFLICT DO NOTHING RETURNING`. A
   concurrent same-key request waits on the unique index, then gets no row;
   return `null` so the helper reloads and replays the committed receipt. If the
   first transaction rolled back, the waiting insert succeeds instead.
2. Run the mutation with the transaction handle.
3. Write the completed response status and body into the claimed receipt.
4. Commit. A deferred constraint trigger (`DEFERRABLE INITIALLY DEFERRED`)
   rejects any receipt whose key, hash, and response are not all present or all
   absent at commit, so no pending receipt can ever be visible. A deferred row
   trigger keeps the `NEW` row captured at the INSERT event, so validating `NEW`
   would reject the completed receipt. The trigger function must instead re-read
   the current row by primary key when it fires and validate that; use the same
   function for both adapters.

Adapters:

- **Company create (receipt on the business row).** Add nullable
  `create_idempotency_key varchar(128)`, `create_request_hash char(64)`, and
  `create_response jsonb` to `portfoliocompanies`, with the all-null-or-all-set
  rule above and a partial unique index on
  `(fund_id, create_idempotency_key) WHERE create_idempotency_key IS NOT NULL`.
  The inserted company row claims the key in step 1; step 3 stores its create
  response. Keep the three columns out of the public model: omit them from
  `insertPortfolioCompanySchema` (`shared/schema.ts:561`, which today omits only
  `id` and `createdAt`), and project them out wherever full rows leave the
  server (`server/storage.ts:672`, `server/routes/portfolio-companies.ts:259`).
  The stored `create_response` is that public projection. Types: keep the
  exported `PortfolioCompany` name as the public type,
  `Omit<PortfolioCompanyRow, 'createIdempotencyKey' | 'createRequestHash' | 'createResponse'>`,
  and add `PortfolioCompanyRow` for the full stored row, in both alias sites
  (`shared/schema.ts:669`, `shared/schema/portfolio.ts:205`). `IStorage`
  (`server/storage.ts:99`) and every response path keep returning the public
  `PortfolioCompany`; only the adapter reads `PortfolioCompanyRow`. No unsafe
  casts. Update the inferred stored-company type in
  `server/services/portfolio-time-machine-read.ts:7` so the historical path does
  not carry receipt fields. No new table and no new row, so release-canary
  residue is unchanged (the canary creates a company through this route,
  `tests/smoke/release-canaries.spec.ts:720-735`). Companies have no hard-delete
  route (the four pinned rows are GET, GET by ID, PATCH, and POST); rows leave
  only through fund deletion or canary purge, which also remove the fund scope a
  retry would need, so no separate retention rule is required.
- **Deal create and import (command table).** Add `deal_pipeline_commands`
  (`fund_id` FK cascade, `operation`, `idempotency_key varchar(128)`,
  `request_hash char(64)` hex check, `created_by`, `response_status`,
  `response_body jsonb`, `created_at`; unique
  `(fund_id, operation, idempotency_key)`), following
  `migrations/0059_task_update_commands.sql`. Keep receipts after a deal is
  deleted: the receipt, not the deal, is the idempotency history. `createDeal`
  runs its deal and activity inserts inside the claimed transaction.
  `confirmImport` runs its loop inside it with a nested `tx.transaction`
  (savepoint) per row, so a single row failure is still caught and reported in
  `failedRows` without aborting the rest; the stored response is the full import
  summary. Register `deal_pipeline_commands` under the `mutationReceipt` residue
  group now (`server/services/canary-residue-service.ts:83`) and in the canary
  purge script as the 0060 precedent did
  (`scripts/release/purge-canary-runs.mjs`). The canary creates no deal
  commands, so the reserved vector stays 44/5/5.

Route wiring: add the three POST paths to
`server/lib/database-backed-idempotency-routes.ts` so the generic middleware
bypasses them (`server/middleware/idempotency.ts:272`); keep
`requireIdempotencyKey`. The route handlers in `server/routes/deal-pipeline.ts`
and `server/routes/portfolio-companies.ts` call the adapters and send the
returned status/body, adding `Idempotency-Replay: true` on replay.

Schema: one additive journaled migration at the next free journal index,
following the 0060 precedent (drift-patch header,
`migrations/meta/_journal.json` entry, a `scripts/prod-schema-manifests/`
manifest, and the `prod-schema-clone`, manifest-sentinel, and G3
forward-compatibility test updates). Never `db:push`. The production schema
apply must happen before any release that serves PR 1b code; that apply and the
release remain separate owner actions this plan does not grant.

#### C2 client intent keys

Each write component owns a ref containing the key and normalized payload
fingerprint:

- Build the normalized payload exactly as sent to the endpoint, then generate
  `crypto.randomUUID()` when a validated submit/confirm begins and the ref is
  empty or its fingerprint differs. The key belongs to the submitted payload,
  not to the modal session.
- Pass `{ headers: { 'Idempotency-Key': key } }` as `apiRequest`'s fourth
  argument; no shared wrapper is needed because headers are already supported
  (`client/src/lib/queryClient.ts:105-116`).
- Classify each failure. A definite rejection is an `ApiError` with a received
  4xx status other than 408 and other than `409 IDEMPOTENCY_RACE_UNRESOLVED`;
  only then may a later edit rotate the key. Everything else (network failure,
  timeout, 408, unresolved race, 5xx) is an unknown outcome: the server may have
  committed. On an unknown outcome, freeze the original key and exact body, make
  the fields and reset read-only, and offer only an explicit retry that resends
  that body with that key. Resolve when the retry returns success (including a
  replay) or a definite rejection; only a definite rejection re-enables edits
  under a new key. Closing while unresolved invalidates the list query so the
  user sees any created row; it does not start a new intent silently.
  `useFundDraftSync`'s `uncertain` status is the precedent
  (`client/src/hooks/useFundDraftSync.ts:169`); do not import the fund-draft
  settlement module.
- Keep the key after a definite rejection only while the normalized submitted
  payload is unchanged. If any submitted field changes after a definite
  rejection, rotate the key before the next request; an unchanged retry reuses
  it.
- Clear only on success, definite rejection followed by an edit, or explicit
  reset/close/new-file reset of a resolved intent. Existing reset seams are
  add-deal success/reset (`AddDealModal.tsx:137-148`, `:178-183`), add-company
  close (`AddCompanyDialog.tsx:79-85`) and success
  (`AddCompanyDialog.tsx:97-104`), and import close
  (`ImportDealsModal.tsx:221-233`).
- Add the key only to import confirm (`/api/deals/opportunities/import`), not
  read-only preview (`ImportDealsModal.tsx:137-153`).

### PR 2 - C1, C4-C7, C9, C10

#### Query and state repairs

- **C1:** add `enabled: fundId != null` to the deal query. With no fund, render
  a fund-selection prompt, hide or disable Add/Import write actions, and do not
  mount them as open modals. Do not call `/api/deals/opportunities` or open a
  write modal without `fundId` (`client/src/pages/pipeline.tsx:555-565`,
  `:775-779`, `:841-850`). Do not add a new no-fund component; reuse the
  existing empty/prompt surface. Keep the stages query unchanged unless its
  existing global taxonomy contract proves fund-scoped during implementation.
- **C4:** capture `error`/`isError` for each performance query. Render an inline
  error and retry for the active timeseries tab or breakdown tab independently;
  one successful dataset remains usable if the other fails. Header refresh may
  continue refetching both (`PerformanceDashboard.tsx:210-213`).
- **C5:** do not derive current-baseline analysis state by scanning
  `useVarianceReports`; it receives only the newest 50 reports
  (`client/src/hooks/useVarianceData.ts:126-137`;
  `server/services/variance-tracking/calculation-service.ts:177-189`). Add
  `getLatestVarianceReportForBaseline(fundId, baselineId)` in the calculation
  service, filtering by both IDs and ordering by `createdAt DESC`. In the
  dashboard route, query the default baseline explicitly with
  `{ isDefault: true, limit: 1 }` rather than searching the newest-five baseline
  list (`server/services/variance-tracking/baseline-service.ts:280-294`), then
  call the exact report lookup. Keep `summary.lastAnalysisDate` as the existing
  response field but source it only from that matching report; return `null`
  when no default baseline or no matching report exists. Recent baselines,
  reports, risk level, and trend remain fund-wide. The client uses dashboard
  `defaultBaseline` and `summary.lastAnalysisDate` for this copy:
  1. no dashboard `defaultBaseline` -> `No baseline`;
  2. baseline exists but exact-baseline `summary.lastAnalysisDate` is null ->
     `Not analyzed`;
  3. exact-baseline `summary.lastAnalysisDate` is non-null ->
     acceptable-variance reassurance. Keep the response shape unchanged
     (`shared/variance-validation.ts:679-703`). Historical/open-incidents copy
     remains unchanged.
- **C6:** every verification `POST` failure, including 400, invalid-passkey 401,
  404, network failure, and five-attempt 429, maps to `passkeyError` and stays
  inline on the mounted form (`server/routes/shares.ts:78`, `:699`). Only
  initial GET/load failures use global share-load `error`. Focus the password
  input after verification failure and add stable input/error IDs, `htmlFor`,
  `aria-describedby`, and `role="alert"`.

#### Accessibility and navigation repairs

- **C7:** add `tabIndex={0}`, `role="region"`, descriptive `aria-label`, and
  existing charcoal focus-visible ring utilities to the overflow wrapper at
  `ScenarioComparisonTable.tsx:195-197`. Do not alter table semantics.
- **C9:** while `needsSetup`, enable `dashboard` and `help` in
  `isNavigationItemEnabled`. Desktop and mobile already use this shared function
  (`client/src/components/layout/sidebar.tsx:137-151`;
  `client/src/app/app-layout.tsx:123-140`). Do not modify route definitions:
  `/help` is already direct and unprotected
  (`shared/routes/app-route-definitions.ts:33`).

#### C10 targeted readable-muted sweep

Replace only readable copy using `text-charcoal-400/500` with
`text-presson-textMuted`; do not replace icons, spinners, separator dots,
disabled-control colors, chart marks, or other decorative uses. Do not touch the
Tailwind palette.

Exact source scope with verified readable occurrences:

- Model results: `client/src/pages/fund-model-results.tsx`,
  `client/src/pages/fund-model-results/states.tsx`,
  `client/src/pages/fund-model-results/SectionRenderer.tsx`,
  `client/src/pages/fund-model-results/result-section-cards.tsx`,
  `client/src/pages/fund-model-results/lifecycle-cards.tsx`,
  `client/src/pages/fund-model-results/scenario-section.tsx`,
  `client/src/features/analytics-parity/QuarterlyReviewTrace.tsx`.
- Scenarios: `client/src/pages/fund-scenario-workspace.tsx`,
  `client/src/components/scenarios/CreateMethodologyScenarioModal.tsx`,
  `client/src/components/scenarios/ScenarioActionList.tsx`,
  `client/src/components/scenarios/ScenarioFactsSeedPicker.tsx`,
  `client/src/components/fund-results/ScenarioComparisonTable.tsx`,
  `client/src/components/fund-results/CrossSetScenarioComparisonTable.tsx`,
  `client/src/components/fund-results/ScenarioSetsSummary.tsx`,
  `client/src/components/fund-results/CurrentPlanAcceptancePanel.tsx`.
- MOIC: `client/src/pages/fund-model-results-moic-analysis.tsx`,
  `client/src/components/moic/MoicBasisDisclosure.tsx`.
- Sensitivity: `client/src/components/backtesting/BacktestingWorkspace.tsx`,
  `client/src/components/sensitivity/_shared/SummaryCard.tsx`,
  `client/src/components/sensitivity/OneWayPanel.tsx`,
  `client/src/components/sensitivity/TwoWayPanel.tsx`,
  `client/src/components/sensitivity/StressPanel.tsx`.
- Variance and public share: `client/src/pages/variance-tracking.tsx`,
  `client/src/pages/shared-dashboard.tsx`.

`client/src/pages/model-results.tsx`,
`client/src/pages/fund-model-results-reports.tsx`,
`client/src/pages/fund-model-results-internal-analysis.tsx`, and
`client/src/pages/fund-model-results-analysis.tsx`, and
`client/src/pages/sensitivity-analysis.tsx` have no scoped raw
`text-charcoal-400/500` or `text-gray-400/500` readable-copy occurrence on this
checkout; retain their existing token/default classes. Re-verify with `rg`
before editing and do not manufacture churn.

Expected visual-baseline effect: muted readable copy becomes darker and more
consistent on light surfaces. Layout, typography, spacing, primary actions,
charts, and icon colors do not change. Re-run the complete scoped `rg` over the
named files as mechanical acceptance, then run existing rendered axe and
screenshot checks. The general route axe suite is supplemental, not proof of
complete readable-copy coverage, because it scans only selected regions and
critical findings (`tests/e2e/accessibility.spec.ts:32-44`). **UNVERIFIED:**
exact screenshot deltas remain unverified until those rendered checks run.

## Implementation Details

### Task 1 - C3 allocation version and conflict recovery

**Production files**

- `client/src/components/portfolio/tabs/ReallocationTab.tsx`
- `client/src/hooks/useReallocationPreview.ts`
- `client/src/hooks/useReallocationCommit.ts`
- `client/src/types/reallocation.ts`
- `server/routes/reallocation.ts`
- `server/services/allocation-write-service.ts` (sorted lock order only)

**Tests that fail before and pass after**

- Create `tests/unit/components/portfolio/reallocation-tab.test.tsx`: each
  selected company supplies its own `expected_version`; preview and commit use
  the same frozen row-scoped payload; 409 refetches, clears preview, retains
  edits, and blocks commit until re-preview; unrelated mixed fund versions do
  not block a selected-row write; a preview response that resolves after an
  edit, reset, or fund switch is discarded and commit stays disabled until a
  fresh preview matches the displayed inputs.
- Extend `tests/unit/routes/reallocation.contract.test.ts` only for canonical
  input, duplicate `company_id` rejection (400, no transaction), authorization,
  and fund-scope boundaries. Its mocked transaction is not safety evidence.
- Create an active PostgreSQL/Testcontainers suite at
  `tests/integration/reallocation.pg.test.ts` and add that exact path to
  `TESTCONTAINERS_TEST_PATHS` in `tests/config/testcontainers-test-paths.mjs`.
  `tests/api/**` is excluded from the Testcontainers runner
  (`vitest.config.testcontainers.ts:39-43`), so do not place this transaction
  gate there. The suite must prove:
  - a partial commit updates only proposed rows and preserves mixed versions on
    untouched rows;
  - a stale per-row version returns 409 with company/version details and leaves
    all values, versions, and audit rows unchanged;
  - a forced mid-transaction audit failure rolls back every row update and
    inserts no partial audit row;
  - reversed request order still acquires locks in sorted company-ID order and
    concurrent overlapping reallocations complete without deadlock, with one
    success and one stale conflict;
  - concurrent reallocation versus the existing allocation writer has one winner
    and one optimistic-lock conflict, with no lost update
    (`tests/api/allocations.test.ts:270-373`), including cross-writer reversed
    overlap (`[A,B]` through `applyAllocationUpdates` against `[B,A]` through
    reallocation) and two reversed `applyAllocationUpdates` calls, each with no
    deadlock error;
  - the shared writer's audit payload keeps caller order after the lock-order
    sort; and
  - returned `audit_ids` identify exactly one audit row per proposed company,
    with expected/resulting versions and deltas only for those companies.

**Acceptance check**

Fresh per-company allocation versions are visible before preview; no request
contains a hard-coded `1`; stale preview cannot commit; a 409 always requires
refetch and re-preview; a partial reallocation advances only each proposed row
by one, leaves unproposed rows unchanged, and records exact row-scoped audit
evidence. No test asserts that all fund rows share one version.

### Task 2 - C2 durable create receipts (PR 1b)

**Production files**

- `server/lib/idempotent-command.ts` (reuse; change only if an adapter needs a
  stored status field the current row type cannot carry)
- `server/lib/database-backed-idempotency-routes.ts`
- `server/services/deal-pipeline-service.ts`
- `server/routes/deal-pipeline.ts`
- `server/routes/portfolio-companies.ts`
- Company-create persistence path (`server/storage.ts:690-694` or a small
  transaction-aware insert beside it), the public-row projections at
  `server/storage.ts:672` and `server/routes/portfolio-companies.ts:259`,
  `insertPortfolioCompanySchema` in `shared/schema.ts`, and
  `server/services/portfolio-time-machine-read.ts`
- `server/services/canary-residue-service.ts` and
  `scripts/release/purge-canary-runs.mjs` (register `deal_pipeline_commands`)
- `shared/schema/portfolio.ts` and one new `deal_pipeline_commands` schema
  export, plus the new journaled migration, journal entry, and prod-schema
  manifest
- `client/src/components/pipeline/AddDealModal.tsx`
- `client/src/components/portfolio/tabs/AddCompanyDialog.tsx`
- `client/src/components/pipeline/ImportDealsModal.tsx`

**Tests that fail before and pass after**

- Create `tests/integration/pipeline-create-commands.pg.test.ts` and add it to
  `TESTCONTAINERS_TEST_PATHS`. For each of the three routes, through the real
  route and a real PostgreSQL:
  - **lost acknowledgment:** a test-only Express hook makes the first response
    write fail after the handler's transaction commits; the same-key retry
    returns the original status and body with `Idempotency-Replay: true`, and
    exactly one durable business row (or one set of imported rows) plus one
    receipt exists;
  - **same-key concurrency:** two simultaneous same-key requests produce one
    mutation and two identical responses;
  - **changed payload:** the same key with a different body, a different actor,
    or a different operation returns `409 IDEMPOTENCY_KEY_REUSE` with no new
    row;
  - **rollback:** a forced failure inside the mutation leaves no business row
    and no receipt, and a later same-key retry creates once;
  - **import savepoints:** one failing import row is reported in `failedRows`
    while every other row and the receipt commit;
    - **receipt integrity:** for both adapters, a claimed receipt completed
      later in the same transaction commits, and a claimed receipt never
      completed fails at commit.
    - **public model:** client-supplied receipt fields are stripped by the
      create request parse (Zod `.omit()` strips, it does not reject) and never
      persisted; the stored receipt columns hold only server-generated values;
      create, list, detail, and replay responses never contain them.
- **Release-canary residue:** a canary-principal company create through the
  route with a key changes reconciled residue by `portfolioCompany +1`,
  `total +1`, and `mutationReceipt +0`, and
  `tests/integration/release-canary-residue-characterization.test.ts` still
  records 40/4/2 unchanged; once F_1.16.0 has landed, its v2 composition tests
  still derive 44/5/5.
- Update the existing Redis-replay assertions for these routes in
  `tests/unit/routes/deal-pipeline.contract.test.ts`, the portfolio-company
  route tests, and
  `tests/unit/middleware/database-backed-idempotency-routes.test.ts`; add a
  transaction/savepoint passthrough to the `db` mocks in
  `tests/unit/services/deal-pipeline-service.test.ts:78` and
  `tests/unit/routes/deal-pipeline.contract.test.ts:83`. Grep every dependent
  assertion before changing shared mocks. Run `tests/api/deal-pipeline.test.ts`
  as a live-route regression.
- Extend `tests/unit/components/pipeline/add-deal-modal.test.tsx` and
  `tests/unit/components/portfolio/add-company-dialog.test.tsx`, and create
  `tests/unit/components/pipeline/import-deals-modal.test.tsx`. Each captures
  the fourth `apiRequest` argument and proves one UUID per unchanged normalized
  payload, reuse on retry, rotation only after a definite rejection plus edit, a
  new UUID after a resolved success/reset/close, no header on import preview,
  and that after a lost response the retry resends the original body and key
  exactly while edit and reset stay blocked.

**Acceptance check**

A committed create whose acknowledgment is lost is replayed from its durable
receipt with the original status and body; no retry, concurrent duplicate, or
changed-payload reuse creates a second row; the release-canary residue vector is
unchanged; and the client never turns an unknown outcome into a new intent.

### Task 3 - C8 stable user-safe dual-forecast errors

**Production files**

- `server/routes/dual-forecast.ts`

**Test that fails before and passes after**

- Extend `tests/unit/routes/dual-forecast-route.test.ts` with hostile SQL/table
  text for both a `MetricsCalculationError` and an unexpected `Error`; assert
  stable code/status plus generic message, and assert hostile text is absent
  from the full JSON body. Update the held-reference expectation that currently
  accepts raw text (`tests/unit/routes/dual-forecast-route.test.ts:181`). Keep
  the existing generic client behavior pinned by
  `tests/unit/hooks/useDualForecast.test.tsx:200`.

**Acceptance check**

Fund readiness blocker shows generic forecast-unavailable copy and stable code;
server logger receives original error; response contains no raw SQL, table name,
query parameters, or exception detail.

### Task 4 - C1 fund-gated pipeline deals query

**Production file**

- `client/src/pages/pipeline.tsx`

**Test that fails before and passes after**

- Extend `tests/unit/pages/pipeline-empty-state.test.tsx` with mutable mocked
  fund context: null fund renders the fund-selection prompt, hides or disables
  Add/Import actions, does not mount an open write modal, and never calls the
  deals endpoint; selecting a fund enables exactly one fund-scoped request and
  the write actions.

**Acceptance check**

No `/api/deals/opportunities` read occurs without `fundId`; no Add/Import write
action can open without `fundId`; selected-fund empty state and existing
add/import paths still work.

### Task 5 - C4 independent performance query failure states

**Production file**

- `client/src/components/performance/PerformanceDashboard.tsx`

**Test that fails before and passes after**

- Extend `tests/unit/components/performance/performance-dashboard.test.tsx` with
  timeseries-only failure/retry and breakdown-only failure/retry cases.

**Acceptance check**

Failure in one query does not mask usable data from the other; retry invokes
only the failed query's refetch control; global refresh still refetches both.

### Task 6 - C5 truthful variance reassurance

**Production file**

- `client/src/pages/variance-tracking.tsx`
- `server/services/variance-tracking/calculation-service.ts`
- `server/routes/variance.ts`

**Tests that fail before and pass after**

- Extend `tests/unit/services/variance-tracking.test.ts` to prove
  `getLatestVarianceReportForBaseline` filters by both `fundId` and
  `baselineId`, orders newest first, and returns no match rather than falling
  back to another baseline
  (`tests/unit/services/variance-tracking.test.ts:911-936`).
- Extend `tests/unit/api/variance-tracking-api.test.ts` so the dashboard keeps
  fund-wide recent reports/risk/trend but sources `summary.lastAnalysisDate`
  from the exact default-baseline lookup; include a default baseline older than
  the newest-five baseline list, a newer report for another baseline, and an
  older matching report
  (`tests/unit/api/variance-tracking-api.test.ts:1539-1602`).
- Extend `tests/unit/pages/variance-tracking.test.tsx` with the three exact
  current-scope states: no baseline, baseline with null exact-baseline
  `lastAnalysisDate`, and baseline with non-null exact-baseline
  `lastAnalysisDate`; cover no current alerts in each relevant state.

**Acceptance check**

Acceptable-variance reassurance appears only when the server finds a report for
the exact current default baseline ID, even if that report is older than the 50
reports returned by `useVarianceReports`; a baseline with reports only for other
baselines renders `Not analyzed`, and no baseline renders `No baseline`.

### Task 7 - C6 passkey inline verification errors and form accessibility

**Production file**

- `client/src/pages/shared-dashboard.tsx`

**Test that fails before and passes after**

- Extend `tests/unit/pages/shared-dashboard.test.tsx`: verification POST 400,
  401, 404, 429, and network failure keep the form mounted, display inline
  `role="alert"`, have label/input/error ID linkage, and return focus to the
  password input; initial GET/load failures still render the global access
  error.

**Acceptance check**

No verification POST failure replaces passkey view; input retains entered value
unless existing product behavior explicitly clears it; only initial GET/load
failures use the global access-error card; keyboard/screen-reader relationships
are complete.

### Task 8 - C7 keyboard-focusable scenario table region

**Production file**

- `client/src/components/fund-results/ScenarioComparisonTable.tsx`

**Test that fails before and passes after**

- Extend `tests/unit/components/fund-results/ScenarioComparisonTable.test.tsx`
  to assert region role/name, `tabIndex=0`, and focus-visible ring classes.

**Acceptance check**

Keyboard focus can enter the horizontal-scroll region and receives a visible
charcoal focus indication; table headers/cells remain unchanged.

### Task 9 - C9 Help remains available during setup

**Production file**

- `client/src/components/layout/navigation-config.ts`

**Tests that fail before and pass after**

- Extend `tests/unit/components/layout/navigation-config.test.tsx`: Help and
  Dashboard enabled with `needsSetup`; Portfolio remains disabled.
- Extend `tests/unit/components/layout/sidebar-navigation.test.tsx`: Help is a
  link in desktop and mobile setup-incomplete states.
- Retain `tests/unit/app/route-perimeter-governance.test.tsx` direct `/help`
  route coverage (`tests/unit/app/route-perimeter-governance.test.tsx:157-171`).

**Acceptance check**

Setup-incomplete user can open Help from both navigation surfaces and by direct
URL; all other setup-gated destinations remain disabled/redirected.

### Task 10 - C10 readable muted text token

**Production files**

Only exact files listed in the C10 scope above, after re-running a complete
scoped `rg`. This includes `client/src/pages/fund-model-results/states.tsx` and
`client/src/components/scenarios/CreateMethodologyScenarioModal.tsx`. Do not
edit already-compliant route files and do not replace decorative/icon classes.

**Acceptance evidence**

- Run scoped `rg` over the complete named C10 file list for
  `text-charcoal-400/500` and `text-gray-400/500`; inspect every match and
  replace readable copy only. This is a one-time mechanical acceptance check,
  not a permanent allowlist test that would require decorative-class
  maintenance.
- Run existing route/component tests for changed behavior-bearing files:
  `tests/unit/pages/fund-model-results.test.tsx`,
  `tests/unit/pages/fund-scenario-workspace.test.tsx`,
  `tests/unit/pages/fund-model-results-moic-analysis.test.tsx`,
  `tests/unit/pages/sensitivity-analysis.test.tsx`,
  `tests/unit/pages/variance-tracking.test.tsx`,
  `tests/unit/pages/shared-dashboard.test.tsx`, and
  `tests/unit/components/fund-results/ScenarioComparisonTable.test.tsx`.
- Run the existing rendered axe and screenshot checks for affected surfaces;
  treat `tests/e2e/accessibility.spec.ts:32-44` as supplemental because its
  route scan is not a complete readable-copy inventory.

**Acceptance check**

Named routes contain no scoped low-contrast readable-copy classes; icons and
decorative states retain current colors; rendered checks show only readable
muted-text contrast changes. No new source-guard test is added.

## Technical Considerations

### Financial and mutation correctness

- Allocation version is row-scoped. Active writers lock and increment individual
  company rows (`server/services/allocation-write-service.ts:100-145`;
  `server/services/fund-moic-input-service.ts:163-197`), so mixed fund versions
  are valid. Every multi-row writer locks in ascending `company_id` order:
  reallocation locks only proposed IDs sorted, and `applyAllocationUpdates`
  sorts its lock loop. Each compares expected versions before updating.
- Commit remains strict-write authorized through existing route middleware; no
  authorization change.
- Idempotency key identifies the normalized submitted payload, not the modal
  session or click attempt. Reuse it only while that payload is unchanged.
- Do not edit `client/src/lib/queryClient.ts` for C3. PR 1 now changes
  `server/services/allocation-write-service.ts`; the classifier treats
  `server/services/**` as financial (`scripts/ci/classify-change-paths.mjs:22`),
  so hosted CI adds `phoenix:truth` plus internal-economics V2
  (`.github/workflows/ci-unified.yml:121`) for PR 1 as well as PR 2. No
  calculation-specific `calc-gate` is otherwise warranted.

### Error and privacy boundary

- C8 response code/message are public UI inputs; raw exception is log-only.
- C6 verification POST errors are form validation/auth feedback, not share-load
  terminal errors. Only initial GET/load failures use the global access-error
  state; do not broaden global error handling to POST verification responses.

### Accessibility and design

- Use existing charcoal focus utilities and `text-presson-textMuted`; no blue,
  opacity workaround, palette remap, or new token.
- C10 uses a complete scoped `rg` plus rendered checks, not a permanent source
  allowlist. Raw scale colors remain valid for icons/decorative states under
  `DESIGN.md`.

### Surface-contract matrix

Every changed server route source is pinned:

- PR 1 changes `server/routes/reallocation.ts` and
  `server/routes/dual-forecast.ts` (`source-inventory.json:576`, `:627`). Their
  source mappings cover exactly three rows: dual forecast plus reallocation
  preview and commit (`source-inventory.json:3414-3419`, `:4559-4567`).
  `server/services/allocation-write-service.ts` is not a pinned matrix source,
  so the lock-order change adds no demoted row.
- PR 1b changes `server/routes/deal-pipeline.ts` (14 rows) and
  `server/routes/portfolio-companies.ts` (4 rows) in `source-inventory.json`
  `source_to_rows`: 18 expected demoted rows. The service, schema, migration,
  and idempotency-route-list files are not pinned sources.
- PR 2 C5 changes `server/routes/variance.ts` (`source-inventory.json:636`). Its
  file-level source mapping covers 15 rows, including variance dashboard
  (`source-inventory.json:3020-3035`). Do not hide that governance cost or split
  the route solely to avoid review.

Do not run the README's `approve-matrix.mjs --fresh` reset path; it requires a
full independent G1 review and owner closure (`README.md:75-80`). Before PR 1
uses the maintenance path, update `audit/surface-contract-matrix/README.md` to
define the sanctioned non-fresh sequence: retain its clean committed-source SHA,
dependency, knowledge-graph, boot-proof, two-seed byte comparison, and classify
controls; omit only `approve-matrix.mjs --fresh`; approve the demoted rows from
a scoped review file without `--close-g1`, preserving the historical
`g1_closure`; then run final validation and render. Use that documented sequence
after each PR's atomic green source commit. The merge preserves human fields and
manual evidence (`matrix-schema.mjs:2133-2146`) but deliberately demotes an
approved row when its defining-source hashes change
(`matrix-schema.mjs:2184-2194`). Because the closed phase is retained,
`validate-matrix.mjs` must not run between reseed and reapproval. Approval-state
preservation is not a success criterion.

Required review flow per PR:

1. Confirm reseed changed only expected source bindings/artifacts and demoted
   the rows bound to changed route files. PR 1 expected review scope is exactly
   `api:GET:/api/funds/:fundId/dual-forecast`,
   `api:POST:/api/funds/:fundId/reallocation/preview`, and
   `api:POST:/api/funds/:fundId/reallocation/commit`. PR 2 expected scope is the
   15 `server/routes/variance.ts` rows listed in
   `source-inventory.json:3020-3035`; unexpected demotions stop the repair.
2. Do not run `approve-matrix.mjs --init-review`: it validates the current
   matrix first and refuses a closed matrix that contains demoted rows
   (`approve-matrix.mjs:896`, `:960`). Instead the independent reviewer writes a
   scoped review file under the review root (`approve-matrix.mjs:181`) that
   lists only the rows demoted by that PR, each with
   `reviewed_fields.decision_status = approved` and updated evidence. This is
   the shape the closed-repair regression uses
   (`tests/unit/audit/surface-contract-matrix-approve.test.ts:178-218`).
3. The reviewer verifies, for every demoted row, the changed endpoint semantics,
   source hashes, authorization, reachability, and coverage. For PR 1 that means
   the three named rows, not a claim that prior approval survived reseed.
4. Run
   `approve-matrix.mjs --review-file <scoped file> --approver <id> --evidence <ref> --dry-run`,
   then the same command without `--dry-run` once the owner authorizes it. Never
   pass `--close-g1`: it rejects an already-closed phase
   (`approve-matrix.mjs:1115`). Without it, the tool validates the complete
   candidate before an atomic write, approves only the listed rows, leaves every
   other row unchanged, and keeps `g1_closure` byte-identical
   (`tests/unit/audit/surface-contract-matrix-approve.test.ts:268-296`). If any
   row outside the scoped file is still unresolved, the tool refuses the repair
   (`surface-contract-matrix-approve.test.ts:298`); stop and investigate.
   Reviewer identity, approval, merge, and release remain separate
   owner-governed actions; this plan grants none of them.
5. After the scoped approval, run `validate-matrix.mjs`, then
   `render-matrix.mjs`; final validation must prove the closed matrix has no
   demoted or otherwise unresolved rows and an unchanged `g1_closure`.

Expected governed files are `source-inventory.json`, `matrix.json`, `MATRIX.md`,
and the scoped review file for that PR. Review any additional generated delta
before including it.

Serialize matrix ownership. All three PRs regenerate the same governed files, so
never merge independently regenerated matrix artifacts or hand-resolve their
conflicts. The chain is PR 1, then PR 1b, then PR 2: each starts its matrix work
only from `main` after its predecessor merges (or from a rebase onto that merged
state), then reseeds. If `main` gains any matrix change before a PR merges,
rebase and rerun the full non-fresh sequence from reseed.

### Validation gates

Run from repository root with Node 22 and UTC:

1. Targeted tests named under each task.
2. PR 1 and PR 1b: `TZ=UTC npm run test:testcontainers`; the C3 transaction
   suite and the C2 receipt suite must be selected through
   `TESTCONTAINERS_TEST_PATHS`.
3. `TZ=UTC npm run check` - zero new errors.
4. `TZ=UTC npm run test:unit`.
5. PR 1 and PR 1b, because test routing changes: `TZ=UTC npm test` before push.
6. All three PRs change `server/services/**` (PR 1 the allocation writer, PR 1b
   the deal service and canary residue registry, PR 2 C5):
   `TZ=UTC npm run phoenix:truth` and
   `TZ=UTC npm run test:internal-economics-v2`; retain exact-head hosted
   `Financial Truth` CI evidence for both commands on each PR.
7. `npm run build:web`.
8. PR 1b schema: migration, journal, prod-schema manifest, `prod-schema-clone`,
   manifest-sentinel, and G3 forward-compatibility tests pass; the migration is
   additive only.
9. Each PR: sanctioned non-fresh matrix reseed, deterministic second-seed
   comparison, classify, scoped independent review, and reapproval of every row
   demoted by that PR; only then run final validate/render checks. Do not treat
   demotion as failure or claim prior approval was preserved.

Do not substitute a local build for hosted CI, merge approval, deployment, or
production proof.

## Files to Modify/Create

### PR 1 production

- `client/src/components/portfolio/tabs/ReallocationTab.tsx`
- `client/src/hooks/useReallocationPreview.ts`
- `client/src/hooks/useReallocationCommit.ts`
- `client/src/types/reallocation.ts`
- `server/routes/reallocation.ts`
- `server/services/allocation-write-service.ts`
- `server/routes/dual-forecast.ts`

### PR 1 tests and governed generated evidence

- `tests/unit/components/portfolio/reallocation-tab.test.tsx` (new)
- `tests/unit/routes/reallocation.contract.test.ts`
- `tests/integration/reallocation.pg.test.ts` (new PostgreSQL/Testcontainers
  suite)
- `tests/config/testcontainers-test-paths.mjs`
- `tests/unit/routes/dual-forecast-route.test.ts`
- `audit/surface-contract-matrix/source-inventory.json` (generated)
- `audit/surface-contract-matrix/matrix.json` (generated)
- `audit/surface-contract-matrix/MATRIX.md` (generated)
- Scoped review file for the three PR 1 rows (independent reviewer; path under
  the review root)
- `audit/surface-contract-matrix/README.md` (document sanctioned non-fresh
  maintenance sequence before first use)

### PR 1b production, tests, and governed evidence

- Production and test files listed in Task 2.
- `tests/integration/pipeline-create-commands.pg.test.ts` (new
  PostgreSQL/Testcontainers suite) and its `TESTCONTAINERS_TEST_PATHS` entry.
- Governed matrix artifacts and a scoped review file for the 18 PR 1b rows.

### PR 2 production

- C1/C4-C7/C9 files named in Tasks 4-9.
- C5 includes `server/services/variance-tracking/calculation-service.ts` and
  `server/routes/variance.ts`; the shared dashboard response shape is unchanged.
- C10 exact source list under Solution Architecture, after scoped
  re-verification.

### PR 2 tests

- Existing test files named in Tasks 4-9.
- Existing route/component tests named in Task 10.
- Governed matrix artifacts and scoped review file listed under PR 1,
  regenerated separately for PR 2's `server/routes/variance.ts` change.

No runtime configuration, dependency, provider, deployment, or LP portal file.
PR 1b adds one additive schema migration; the only configuration edit is the
Testcontainers path registry.

## Type Definitions

- Add required `expected_version` to each `ProposedAllocation`; remove scalar
  `current_version` from preview/commit requests; `proposed_allocations` must
  have unique `company_id` values.
- Define `new_versions` exactly as
  `Array<{ company_id: number; new_version: number }>` and `audit_ids` exactly
  as `Array<{ company_id: number; audit_id: string }>`; both are sorted by
  ascending `company_id`.
- Define wire field `details.current_versions` exactly as
  `Array<{ company_id: number; current_version: number }>` sorted by ascending
  `company_id`; map once to a typed `currentVersions` field in reallocation hook
  errors.
- `ApiError.details` remains `unknown`; validate with existing record/array
  checks before reading. Do not edit `queryClient.ts` or add a generic
  response-body type system.
- No shared financial-contract change; PR 1b adds the schema described under PR
  1b.

## Backward Compatibility

- C3 is a cross-stack contract repair: row-scoped requests replace the scalar
  version, success returns `new_versions`/`audit_ids`, and each proposed company
  receives one existing-schema audit row. Unproposed allocation values and
  versions remain unchanged. **UNVERIFIED:** external consumers of the current
  scalar response are not visible in this checkout; confirm them before
  implementation and stop if they require a compatibility adapter.
- C3 409 responses put deterministic company/version objects in
  `details.current_versions` for both preview and commit. Success returns
  deterministic company/version and company/audit-UUID objects; no fund-wide
  revision is represented.
- C2 adds required headers already expected by current server routes; payloads
  are unchanged. The three create routes move from Redis replay to durable
  receipts: changed-payload reuse now returns `409 IDEMPOTENCY_KEY_REUSE`
  instead of the generic middleware's 422, and replay no longer expires. The
  migration is additive (new nullable columns and one new table).
- `applyAllocationUpdates` keeps its request/response contract and audit payload
  order; only its internal lock order changes.
- C5 keeps the dashboard response shape but narrows `summary.lastAnalysisDate`
  semantics to the exact current default baseline. Fund-wide recent-report,
  risk, and trend fields remain unchanged.
- C8 intentionally narrows error disclosure while preserving stable codes and
  status classes.
- C9 keeps `/help` path and route protection status unchanged.
- C10 changes color tokens only; no content, layout, or interaction change.

## Test Impact

Expected new/expanded regression coverage:

- Optimistic-lock version derivation, conflict recovery, and partial-commit
  coherence.
- Idempotency-key lifetime for three write intents, and durable co-transactional
  receipts covering lost acknowledgment, same-key concurrency, changed payload,
  and unchanged release-canary residue.
- User-safe forecast error envelope.
- Fund-gated pipeline query.
- Independent performance errors/retries.
- Server-backed exact-baseline analysis state, including a matching report older
  than the client list's 50-report window.
- Passkey inline error semantics and focus.
- Keyboard-focusable overflow region.
- Setup-safe Help navigation.
- Complete scoped readable-muted `rg` check plus rendered axe/screenshot
  evidence; no permanent source guard.

The quarantined `tests/unit/reallocation-api.test.ts:495-530` all-equal
expectation is obsolete and must not be restored. The registered
`tests/integration/reallocation.pg.test.ts` Testcontainers suite is the C3
transaction/audit gate; contract tests remain boundary evidence only.

## Documentation Impact

PR 1 must update `audit/surface-contract-matrix/README.md` before using the
non-fresh maintenance path, placing scoped approval (scoped review file, no
`--close-g1`, `g1_closure` preserved) before final validation and render. No
other user-facing documentation or changelog is required unless repository
release policy requires it at implementation time. This plan is the
implementation record. Matrix fingerprint/render artifacts are governed
evidence, not feature documentation.

Expected screenshot/audit evidence should state: contrast-only visual shifts on
named routes; no rebrand; no Tailwind charcoal-scale remap; no LP portal change.

## To-dos

### PR 1 - mutation and error boundary

- [ ] C3 add failing active client and route tests.
- [ ] C3 derive and pin expected version per selected company.
- [ ] C3 return `details.current_versions`, refetch, clear stale preview, and
      require re-preview.
- [ ] C3 reject duplicate `company_id` values in both request schemas.
- [ ] C3 lock proposed IDs in sorted order; update and audit only those rows.
- [ ] C3 sort the lock loop inside `applyAllocationUpdates`; keep audit payload
      order; test cross-writer reversed overlap without deadlock.
- [ ] C3 fence late preview responses by fund plus input fingerprint.
- [ ] C3 run active PostgreSQL/Testcontainers coverage for rollback, stale
      versions, concurrency, lock order, and exact audit rows through
      `npm run test:testcontainers`.

- [ ] C8 add hostile-detail route tests and generic response envelope.
- [ ] Run targeted tests, common gates, `TZ=UTC npm run phoenix:truth`, and
      `TZ=UTC npm run test:internal-economics-v2`; retain exact-head hosted
      `Financial Truth` CI evidence.
- [ ] Update matrix README with the sanctioned non-fresh maintenance sequence.
- [ ] Run non-fresh matrix reseed; confirm exactly three PR 1 rows demote;
      obtain scoped independent review and approval without `--close-g1`; only
      then run final validate/render. Do not claim approval preservation.

### PR 1b - durable create receipts

- [ ] Start only from `main` after PR 1 merges.
- [ ] Add the additive migration: company-row receipt columns with the deferred
      all-or-none trigger and partial unique index, and the
      `deal_pipeline_commands` table; journal, manifest, and schema tests.
- [ ] Add the two adapters over `runIdempotentCommand`; bind actor and operation
      in the hash; register the three paths as database-backed.
- [ ] Register `deal_pipeline_commands` as `mutationReceipt` residue and in
      canary purge.
- [ ] Add the Testcontainers receipt suite: lost acknowledgment, same-key
      concurrency, changed payload, rollback, import savepoints, receipt
      integrity, and release-canary company residue unchanged.
- [ ] Add client intent-key and unknown-outcome tests for all three writes.
- [ ] Run common gates, `TZ=UTC npm test`, `TZ=UTC npm run phoenix:truth`, and
      `TZ=UTC npm run test:internal-economics-v2`; retain exact-head hosted
      `Financial Truth` CI evidence.
- [ ] Non-fresh matrix reseed; confirm exactly 18 PR 1b rows demote; scoped
      review packet for the owner.
- [ ] Owner applies the production schema before any release that serves PR 1b
      code (separate owner action).

### PR 2 - client reliability and accessibility

- [ ] C1 gate deals query and test no-fund request suppression.
- [ ] C4 add independent query error/retry blocks and tests.
- [ ] C5 query latest report by exact default-baseline ID server-side; keep
      fund-wide recent report/risk/trend semantics; add service, route, and
      client truth-table tests including a matching report older than the
      newest 50.
- [ ] C6 keep form mounted for every verification POST failure; link accessible
      inline error and restore focus.
- [ ] C7 make overflow wrapper a named focusable region.
- [ ] C9 enable Help during setup in shared navigation config; retain direct
      route behavior.
- [ ] C10 re-run complete scoped `rg`; change readable text only; include the
      two omitted files and no permanent source guard.
- [ ] C10 review expected contrast-only screenshot shifts.
- [ ] Run targeted tests, common gates, `TZ=UTC npm run phoenix:truth`, and
      `TZ=UTC npm run test:internal-economics-v2`; retain exact-head hosted
      `Financial Truth` CI evidence.
- [ ] Start PR 2 matrix work only from `main` after PR 1b merges; rebase and
      restart from reseed if `main` gains any matrix change.
- [ ] Run separate non-fresh matrix reseed for `server/routes/variance.ts`;
      independently review and approve all 15 demoted rows from a scoped review
      file, without `--close-g1`, before final validate/render.

## Review disposition

- **Round 1 P1 C3 fund-wide version model - addressed:** Replaced with
  per-company expected versions, sorted proposed-row locks, proposed-row-only
  updates, and truthful per-company audit rows; no fund revision or all-row
  bump.
- **Rounds 1-2 P1 C3 transaction evidence - addressed:** The active suite is
  `tests/integration/reallocation.pg.test.ts`, registered in
  `TESTCONTAINERS_TEST_PATHS`, and required by
  `TZ=UTC npm run test:testcontainers`; mocked route tests remain boundary-only.
- **Rounds 1-2 P1 C5 baseline identity - addressed:** Dashboard
  `lastAnalysisDate` now comes from a server-side latest-report query filtered
  by exact fund and default-baseline IDs, so correctness does not depend on the
  newest-50 client list. Service, route, and client tests cover a newer report
  for another baseline and an older matching report.
- **Round 4 P1 matrix repair sequence - addressed:** scoped review file plus
  `approve-matrix.mjs` without `--close-g1`, matching the closed-repair
  regression; `--init-review` and `--close-g1` are not used.
- **Rounds 1-3 P1 matrix refresh - addressed:** PR 1 adds the sanctioned
  non-fresh maintenance sequence to the matrix README. Both PRs accept
  deliberate approval demotion, complete scoped independent review and approval
  without `--close-g1`, then run final validation/render; no closed-phase
  validation occurs before reapproval and no approval-preservation claim
  remains.
- **Round 1 P2 C2 idempotency - addressed:** Keys remain only while the
  normalized submitted payload is unchanged; any post-attempt edit rotates the
  key and is tested.
- **Round 1 P2 C1 no-fund controls - addressed:** Added a fund-selection prompt
  and disabled/hidden Add/Import actions and modal opens until `fundId` exists.
- **Round 1 P2 C6 verification errors - addressed:** All verification POST
  failures stay inline; only initial GET/load failures use the global
  access-error state.
- **Round 1 scope C8 client behavior - addressed:** Kept the existing 5xx
  redaction behavior; the held-reference route test now verifies generic output
  and absence of raw detail instead of requiring a client redaction layer.
- **Round 1 P2 C10 inventory/guard - addressed:** Added the two omitted files,
  replaced the permanent inventory test with scoped `rg`, and retained rendered
  axe/screenshot evidence as supplemental checks.
- **Round 1 P2 PR 1 financial gates - superseded 2026-09-25:** The generic
  `queryClient.ts` edit stays removed, but PR 1 now changes
  `server/services/allocation-write-service.ts`, so PR 1 carries the
  `phoenix:truth`, internal-economics V2, and hosted `Financial Truth` gates.
- **Round 2 P2 C3 wire shapes - addressed:** `details.current_versions`,
  `new_versions`, and `audit_ids` now have exact object fields, company
  association, and ascending-`company_id` ordering.
- **Round 3 P2 PR 2 financial truth - addressed:** C5's
  `server/services/variance-tracking/calculation-service.ts` change requires
  local `phoenix:truth` and `test:internal-economics-v2` runs plus exact-head
  hosted `Financial Truth` CI evidence for both commands.

### Stop condition

Two separate stops per PR:

1. **Implementation and review packet complete (agent stop):** fresh targeted
   evidence plus `TZ=UTC npm run test:testcontainers` for PR 1 and PR 1b,
   `TZ=UTC npm run check`, `TZ=UTC npm run test:unit`, required PR 1 and PR 1b
   `TZ=UTC npm test`, PR 1b schema tests, `TZ=UTC npm run phoenix:truth` and
   `TZ=UTC npm run test:internal-economics-v2` for every PR, and
   `npm run build:web` pass; exact-head hosted `Financial Truth` CI records both
   financial commands; the non-fresh reseed demoted exactly the expected rows;
   the scoped review file and `approve-matrix.mjs --dry-run` output are ready
   for the owner. The agent stops here.
2. **Owner-authorized matrix reapproval (owner action):** the owner authorizes
   the scoped `approve-matrix.mjs` run without `--close-g1`; final
   `validate-matrix.mjs` and `render-matrix.mjs` pass with no unresolved rows
   and a byte-identical, still-closed `g1_closure`. PR 1b reaches this stop only
   after PR 1's reapproved matrix state is on `main`, and PR 2 only after PR
   1b's.
3. **Final-head evidence (before merge):** commit the approval artifacts, push,
   and require hosted `CI Gate Status` and `Financial Truth` green on that new
   head. Stop-1 hosted evidence covers an earlier head and is not merge evidence
   once the approval commit lands.

No authority claim extends beyond local implementation evidence.

### Owner review 2026-09-25 (exact head `4a9940e50`)

- **Item 1 lock order in every writer - addressed:** verified that
  `applyAllocationUpdates` locks rows one at a time in request order. It now
  sorts its lock loop by `company_id` (audit payload order unchanged), fixing
  both callers once; the Testcontainers suite covers cross-writer reversed
  overlap. PR 1 therefore gains the `server/services/**` Financial Truth gates;
  the writer is not a pinned matrix source, so PR 1 row scope stays three.
- **Item 2 late preview responses - addressed:** preview is bound to a
  fund-plus-input fingerprint captured at `mutate()`; stale completions are
  discarded, and commit requires a fingerprint match with the displayed inputs.
- **Item 3 unknown mutation outcomes - addressed with a stated ceiling:** only a
  definite 4xx rejection allows key rotation; unknown outcomes pin the original
  key and body until an explicit retry resolves them. The generic middleware's
  Redis replay window (default 300 s, no storage without Redis) bounds the
  guarantee; moving the three routes to database-backed idempotency is an owner
  decision.
- **Item 4 duplicate `company_id` - addressed:** one schema `.refine` rejects
  duplicates with 400 before any transaction.
- **Item 5 matrix ownership - addressed:** PR 2 matrix work starts only from PR
  1's merged matrix state and restarts from reseed on any intervening matrix
  change; the stop condition now separates the agent's implementation and
  review-packet stop from owner-authorized reapproval, and no longer says
  "G1-closed" for a phase that stays closed and unchanged.

### Codex re-review 2026-09-25 (head `693b71785`)

- **P1 committed-then-500 duplicate - partially addressed (owner chose atomic
  writes; see the next round for the residual ceiling):** verified that the
  generic middleware caches only 200/201, that `createDeal` commits before its
  activity insert, and that `confirmImport` has no transaction. Both now run in
  one transaction (savepoint per import row); company create is already a single
  insert. A registered Testcontainers suite proves a 500 leaves no row and a
  same-key retry creates once. Durable database receipts were considered and not
  chosen; the lost-201 Redis-window gap remains the stated ceiling.
- **P2 final-head CI after approval commit - addressed:** stop 3 requires hosted
  `CI Gate Status` and `Financial Truth` on the head that contains the approval
  artifacts.

### Codex re-review 2026-09-25 (head `863e2c323`)

- **P1 residual commit-acknowledgment loss - open ceiling, not addressed:**
  confirmed that a commit whose acknowledgment is lost yields an uncached 500
  and a re-executing pinned-key retry; the trigger tests prove rollback before
  commit only. The C2 text now states both residual gaps (lost commit
  acknowledgment and the lost-201 Redis window) and that closing them needs a
  co-transactional durable receipt. Owner decision: accept this ceiling
  explicitly, or add database-backed receipts for the three routes.
- **P2 transaction-incompatible unit mocks - addressed:** both `db` mocks that
  exercise create/import gain a transaction and savepoint passthrough, both
  files are listed, and `tests/api/deal-pipeline.test.ts` runs unchanged.

### Owner decision 2026-09-25 (after head `39eab3238`)

- **C2 promoted to server-side P1, new PR 1b:** the lost-acknowledgment window
  (business commit succeeds, acknowledgment or cache write is lost, the retry
  repeats the mutation) is closed by persisting a completed receipt in the same
  PostgreSQL transaction as each mutation. The earlier "no server changes" and
  atomic-writes-only designs and the stated residual ceiling are withdrawn.
- **Storage split preserves F_1.16.0's 44/5/5:** company create stores its
  receipt on the business row (nullable key, hash, and response; all or none;
  partial unique per fund), because the release canary creates a company through
  that route; deal create and import use a new `deal_pipeline_commands` table
  registered as `mutationReceipt` residue, which the canary never touches. One
  state machine (`runIdempotentCommand`) drives two storage adapters.
- **Required regressions:** lost acknowledgment, same-key concurrency,
  changed-payload rejection, and release-canary company creation leaving the
  residue vector unchanged.
- **Scope cost stated:** PR 1b demotes 18 matrix rows, adds one additive
  journaled migration, and needs an owner-authorized production schema apply
  before release. `39eab3238`'s mock passthrough work carries into PR 1b's test
  list.

### Codex re-review 2026-09-25 (head `3fde896f7`)

- **P1 deferred trigger sees the INSERT-time row - addressed:** the all-or-none
  trigger re-reads the current row by primary key when it fires, shared by both
  adapters; tests prove claim-then-complete commits and claim-only fails.
- **P1 receipt columns leak into the public company model - addressed:** the
  columns are omitted from `insertPortfolioCompanySchema`, projected out of
  storage and route responses, excluded from the historical inferred type, and
  the stored create response is the public projection; tests cover rejection and
  absence.
- **P2 PR 2 predecessor - addressed:** PR 2 matrix work starts after PR 1b.
- **P2 Type Definitions contradiction - addressed:** now states PR 1b adds the
  schema; no shared financial-contract change.

### Codex re-review 2026-09-25 (head `52a050e52`)

- **P2 Zod strips rather than rejects - addressed:** the public-model test now
  requires stripping and non-persistence of client-supplied receipt fields, not
  a 400; no extra guard is added.
- **P2 public TypeScript model - addressed:** `PortfolioCompany` stays the
  public type as an `Omit` of the new full `PortfolioCompanyRow`; `IStorage` and
  response paths use the public type, only the adapter reads the full row, and
  no unsafe cast is allowed.
