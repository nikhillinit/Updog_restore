# Bootstrap Save Confirmation Implementation Plan

**Governing policy:**
`docs/governance/solo-internal-change-and-production-policy.md` (resolve from
`origin/main`). This plan is a client-only defect repair with no production,
schema, or release authority. Merge, deploy, and production remain separate
owner actions.

Source: OMX consensus planning bundle
`~/.codex/task-artifacts/next-priority-bootstrap-20260924T165416Z/.omx/plans/`
(`prd.md`, `test-spec.md`, Architect APPROVE, Critic APPROVE, 2026-09-24).
Reviewed checkout `origin/main` at `9e552f0ac` (#1575). Candidate under reuse:
the local, unmerged commit
`fix(client): distinguish live bootstrap saves from recovery`, whose parent is
`eb9352108` (#1572). The candidate is referenced by subject only; the repository
is public and its SHA stays out of tracked docs.

## Overview

The Fund Basics bootstrap save shows a false "Save not confirmed" alert while a
live `save_draft` is still in flight, and a genuinely lost response leaves the
mounted shell without the recovery alert it needs. This plan finishes the
existing local candidate on refreshed main: keep its module-local live-transport
registry that suppresses the false alert, and add the one missing store
transition in the two direct callers so that a real unknown outcome reaches the
global `draftSyncStatus` in the same mounted UI. Seven source and test files, no
new module, no wire, schema, or public-API change.

## Problem Statement

Two reproduced same-mount gaps (test-spec, 2026-09-24):

1. **False uncertainty during a live save.** The recovery effect in
   `client/src/hooks/useFundDraftSync.ts` (around line 311) treats any pending
   `save_draft` as a command recovered from a reload and reports `uncertain`.
   The bootstrap path in `client/src/pages/FundBasicsStep.tsx` starts that
   command in the same session, so the user sees "Save not confirmed" for a save
   that then succeeds. The candidate already fixes this with an in-memory
   registry of live saves consulted by the effect.
2. **Real uncertainty never reaches the shell.** With the registry in place, the
   effect stays quiet while the transport runs. When the transport then rejects
   with `FundWorkflowUncertainError`, the registry entry is removed in
   `finally`, but registry cleanup is not store state and the effect does not
   rerun. The two direct callers' `uncertain` arms (`FundBasicsStep.tsx` around
   line 252, `client/src/components/workspace/FundWorkspace.tsx` around
   line 389) only write component-local error text. Global `draftSyncStatus`
   stays `idle` or `saving`, so the shell's `draft-uncertain` alert and its
   "Check save status" action never render, and the workspace dialog reports an
   unknown outcome while the store still says `saving`.

The autosave path in the hook already performs the missing transition
(`setStatus('uncertain')` in its outcome switch), which is why only the two
direct callers are wrong. The hook's `status` is the store's `draftSyncStatus`
(selected at hook line 58), so a caller's store write is what the FundSetup
shell renders.

## Solution Architecture

Option A from the consensus PRD. Caller-owned status is the settled contract of
F_1.15.0: `saveDraftAndSettle` owns transport, journal settlement, and the
session/fund/command-key supersession fence; callers own status,
`draftServerReady`, dialog text, and navigation. The repair therefore stays in
the callers:

- Keep the candidate's registry in
  `client/src/services/fund-draft-settlement.ts` (`isDraftSaveInFlight`, exact
  identity match on session, key, operation, target fund, body signature, and
  expected ETag; entry added before transport, removed in `finally`) and the
  guarded effect in the hook, unchanged.
- In both direct callers' `uncertain` outcome arms, write the store's
  `uncertain` status synchronously, before the existing local error update,
  inside the same synchronous outcome callback. No status write for
  `superseded`, no `pendingCommand` clearing, no key/body/ETag replacement, no
  fetch over an unresolved write, and the callback never moves across an
  `await`.

Rejected alternatives, recorded so TRIP-2 does not reopen them: moving the
status write into the helper (Option B) widens settlement into presentation and
changes the closed outcome contract for every caller; making the registry
observable and rerunning the effect on transport end (Option C) adds a
subscription lifecycle and must undo the verified-ref early return. Revisit
centralization only on another demonstrated mismatch.

## Implementation Details

### 1. Reuse the candidate

**Files**: `client/src/services/fund-draft-settlement.ts`,
`client/src/hooks/useFundDraftSync.ts`,
`tests/unit/pages/fund-basics-bootstrap.test.tsx`, `TODOS.md`

**Current state**: the candidate's parent is an ancestor of `origin/main`, and
the commits between them are documentation-only, so it cherry-picks cleanly. It
adds the registry and `isDraftSaveInFlight` to the settlement module, guards the
hook's recovery effect with it, adds a `BootstrapWithDraftSync` test harness
plus a held-save regression to the bootstrap suite, and deletes the TODOS.md
entry.

**Modifications**:

- Create one isolated worktree on a branch named without the substring `command`
  (the checkout path leaks into 17 sanitizer tests), cherry-pick the candidate,
  run `npm ci` in the worktree (managed worktrees ship empty `node_modules` and
  otherwise resolve the parent's React copy). Do not rebase or reset the
  original checkout.
- Registry identity matching and the `finally` cleanup stay as the candidate
  wrote them.
- The cherry-pick brings the TODOS.md deletion in immediately. Checkpoint
  commits before acceptance passes must not describe that deletion as closure;
  the deletion is only correct once the mounted-uncertainty acceptance in
  section 3 is green.
- Fold in the dead export `STORAGE_UNAVAILABLE_MESSAGE` at
  `client/src/hooks/useFundDraftSync.ts:30`. A by-name search across the
  repository (excluding `node_modules`, `.worktrees`, `.claude`) finds no
  importer; the #1573 audit deferred it to the next hook touch, which this is.
  Delete it in the same hook change; no behavior effect.

### 2. Direct caller status transitions

**File**: `client/src/pages/FundBasicsStep.tsx`

**Current state**: the `uncertain` arm (around line 252) sets `bootstrapError`
to `UNCERTAIN_SAVE_MESSAGE` and returns the stage to `idle`. The `saved` arm
already writes `synced` to the store before navigating.

**Modifications**:

- In the `uncertain` arm, write the store's `uncertain` status first, then the
  existing local error and stage updates. Keep the exhaustive `switch` and every
  other arm unchanged.
- Presentation after the fix: while the outcome is unknown, the FundSetup shell
  renders its `draft-uncertain` alert with "Check save status" and Fund Basics
  keeps its own `bootstrapError` text. Both stay during uncertainty; the local
  text is the step-level explanation and the shell alert is the recovery
  control.
- Stale local error after replay: `bootstrapError` renders unconditionally
  (around line 476) and is cleared only at the top of the Next handler (around
  line 139). A replay confirmed through the shell's "Check save status" goes
  through the hook, which sets `draftServerReady` and `synced` but cannot reach
  the step's local state, so the old "Save not confirmed" text would linger.
  Fix: clear `bootstrapError` when the store reports the draft server-ready (one
  effect keyed on `draftServerReady` becoming true; creation and bootstrap
  errors only exist while it is false, so nothing else is hidden). The
  saved-with-newer-edits arm sets `draftServerReady` false before its message,
  so that message is unaffected.

**File**: `client/src/components/workspace/FundWorkspace.tsx`

**Current state**: the `uncertain` arm (around line 389) only sets the dialog
error. The `saved` arm already writes `synced` before closing the dialog and
starting the new fund.

**Modifications**:

- In the `uncertain` arm, write the store's `uncertain` status first, then the
  existing dialog error. Nothing else in `confirmNewFund` changes.
- Out of scope, named on purpose: the `stale`, `rejected`, and `retry_same_key`
  arms of this dialog also leave global status untouched. They are terminal,
  dialog-scoped outcomes with `pendingCommand` already resolved where
  applicable, and none of them is a false-uncertainty defect. They stay as they
  are unless a demonstrated mismatch appears.
- Out of scope: the `hydrateThenStartNew` dead end recorded in
  `docs/3-code-review/CR_w10_draft-recovery-fence.md` (Minor 1). It is live code
  on the `saved` path, not dead code, and changing it alters dialog flow beyond
  this defect.

### 3. Mounted-shell bootstrap coverage

**File**: `tests/unit/pages/fund-basics-bootstrap.test.tsx`

**Current state**: the suite's file-local `vi.mock('wouter')` exports
`useLocation` only; it also mocks `FundContext`, `@/services/funds`, and
`@/services/fund-drafts` (the last exports only `saveFundDraft`), and renders
`FundBasicsStep` directly. The candidate adds a `BootstrapWithDraftSync` harness
that mounts the hook next to the step and proves the held live save is not
reported as uncertain, including same-actor rehydration.

**Modifications**:

- Extend the file-local `wouter` mock with `useSearch` and a normalized pathname
  so the real `FundSetup` shell can mount (the shell reads `useSearch` for the
  step key). The OMX diagnostic proved no additional guard mock is needed; do
  not add one speculatively.
- Add one mounted-shell regression that renders the real `FundSetup` shell, real
  `FundBasicsStep`, real `useFundDraftSync`, and real `fundStore`, reusing the
  suite's transport and context doubles:
  1. hold the bootstrap save pending; assert no `draft-uncertain` alert;
  2. reject with `FundWorkflowUncertainError`; assert `draftSyncStatus` is
     `uncertain` and the `draft-uncertain` alert with "Check save status" is
     present before any unmount; assert no navigation; capture the first
     transport arguments and the pending envelope;
  3. click "Check save status" without remounting; hold the replay
     acknowledgement; assert the second transport call reuses the original body,
     key, and ETag and that `pendingCommand` is still present;
  4. acknowledge success; assert `pendingCommand` cleared, status `synced`, the
     shell's `draft-uncertain` alert gone, the step's local bootstrap error
     alert gone, and no automatic navigation.
- Mechanism the acceptance relies on (verified on main): the shell's "Check save
  status" calls the hook's `retry`, which falls through to `persistCurrentDraft`
  because `draftServerReady` is still false and the identity is not yet
  verified; `persistCurrentDraft` proceeds when the pending command is a
  `save_draft` and replays it through `saveDraftAndSettle` with the same key and
  ETag. On `saved` the hook sets `draftServerReady` and `synced`. A fresh
  bootstrap session has `needsServerHydration` false, so the `hydrating` branch
  is not expected; if it is reached, patch the `@/services/fund-drafts` double
  to also export `fetchFundDraft` and `isMissingDraftError` rather than widening
  production code.
- Port the two diagnostic assertions first and observe them fail against the
  cherry-picked candidate before applying section 2 (red-first).
- Supersession control for the new status write: extend the existing "reenables
  Next without navigating when a save belongs to a replaced session" control so
  that, after `replaceDraftSession`, the replacement session's `draftSyncStatus`
  is asserted unchanged (seed a sentinel value on the replacement) and no
  `draft-uncertain` alert renders; add a sibling case where the held save
  rejects with `FundWorkflowUncertainError` after the replacement, with the same
  assertions. The helper's fence delivers a `superseded` outcome through the
  callback, and that caller arm performs no status write, so both must pass
  without further source change.
- Keep the candidate's separate held-live-save and same-actor rehydration cases,
  and the existing recovered-command and remount cases, as independent controls.
  Do not replace the suite-wide `FundBasics` mocks in
  `tests/unit/pages/fund-setup-draft-sync.test.tsx`; that suite keeps its own
  seams.
- The hook's local `error` may stay `null` on this path; the shell already falls
  back to "Could not confirm the save; it may have completed".

### 4. Workspace dialog coverage

**File**: `tests/unit/components/workspace/fund-workspace.test.tsx`

**Current state**: the test "offers Resume Draft for the local session and asks
before starting another fund" seeds `draftSyncStatus: 'saving'`, rejects the
save, and asserts only the dialog alert text.

**Modifications**:

- After the rejected save, assert `draftSyncStatus` becomes `uncertain` while
  the dialog stays mounted, local values and the pending command identity are
  intact, and the retry's second transport arguments equal the original body,
  key, and ETag before the replay is acknowledged.
- Extend the superseded-session and superseded-command negative controls ("does
  not reset a replacement session queued after the save resolves" and the
  old-rejection cases) to assert the replacement's `draftSyncStatus` is
  unchanged, and add one variant where the old save rejects with
  `FundWorkflowUncertainError` after replacement. Keep the identity-only
  recovered-save replay case unchanged.

## Technical Considerations

- **Pattern usage**: follows the F_1.15.0 settlement contract in `docs/ARCHI.md`
  section 5 (helper owns transport and fences, callers own status). The registry
  is a module-local `Set`, not store state, by design: a reload clears it, which
  is exactly the signal that distinguishes a recovered command from a live one.
- **Ordering**: the status write must be synchronous and precede the local error
  write so a test observing the store immediately after the callback sees
  `uncertain` with no intermediate render.
- **Supersession**: when session, fund, or command key changed, the helper
  delivers a `superseded` outcome through the callback; that arm performs no
  status write, so the new transition cannot touch a replacement session.
  Existing ordering regressions assert replacement state unchanged.
- **Preact production bundle**: no new React-specific API; `Set` iteration and a
  store setter only.
- **Edge cases**:
  - After a successful "Check save status" replay from the mounted shell, the
    hook's `saved` arm sets `draftServerReady` and `synced` and does not
    navigate. The expected user path is to press Next again; Fund Basics then
    skips the bootstrap save because `draftServerReady` is true and falls
    through to the plain navigate at the end of its handler (verified around
    line 273). This is intended and must not be "fixed" in TRIP-2.
  - Same-actor rehydration (role change) replaces `pendingCommand` with an
    equal-identity envelope; the registry matches on identity fields, not object
    reference, so the live save stays owned.
  - A real reload empties the registry; a persisted pending `save_draft` is then
    reported `uncertain` and replayable, as today.

## Files to Modify/Create

1. `client/src/services/fund-draft-settlement.ts` (modify, from candidate) -
   live-save registry and `isDraftSaveInFlight`.
2. `client/src/hooks/useFundDraftSync.ts` (modify, from candidate plus one
   deletion) - registry-guarded recovery effect; remove dead
   `STORAGE_UNAVAILABLE_MESSAGE` export.
3. `client/src/pages/FundBasicsStep.tsx` (modify) - store `uncertain` transition
   in the bootstrap `uncertain` arm; clear `bootstrapError` when the draft
   becomes server-ready.
4. `client/src/components/workspace/FundWorkspace.tsx` (modify) - store
   `uncertain` transition in the dialog `uncertain` arm.
5. `tests/unit/pages/fund-basics-bootstrap.test.tsx` (modify, from candidate
   plus mounted-shell case) - `useSearch`/pathname in the file-local wouter
   mock; real shell regression through acknowledgement.
6. `tests/unit/components/workspace/fund-workspace.test.tsx` (modify) - store
   status and retry identity assertions in the existing dialog test.
7. `TODOS.md` (modify, from candidate) - remove the bootstrap alert entry once
   acceptance passes.
8. `docs/1-plans/F_1.15.1_bootstrap-save-confirmation.plan.md` (new) - this
   plan. Verified: with the file staged, `npm run docs:routing:check` passes and
   `npm run docs:routing:generate` rewrites nothing under `docs/_generated/`, so
   no generated routing file changes.

## Type Definitions

None. `isDraftSaveInFlight(sessionId, command)` is the candidate's only new
export and its signature is unchanged.

## Backward Compatibility

No wire, persisted-envelope, schema, or exported-type change. The persisted
pending command format and the replay contract (same key, same `If-Match`) are
untouched. Autosave behavior is unchanged.

## Test Impact

- Five focused client suites (test-spec, run under `TZ=UTC` with
  `--configLoader native --project=client`):
  `tests/unit/pages/fund-basics-bootstrap.test.tsx`,
  `tests/unit/services/fund-draft-settlement.test.tsx`,
  `tests/unit/pages/fund-setup-draft-sync.test.tsx`,
  `tests/unit/components/workspace/fund-workspace.test.tsx`,
  `tests/unit/hooks/useFundDraftSync-canonical.test.tsx`. Baseline recorded in
  test-spec against the candidate (not rerun here): bootstrap+settlement 52
  passed; workspace+wizard-sync+canonical 69 passed.
- Two red-first assertions: mounted bootstrap expects `uncertain` but receives
  `idle`; workspace dialog leaves global status `saving`. Both must fail on the
  cherry-picked candidate and pass after section 2.
- New logic needing tests: the two caller transitions (sections 3 and 4), the
  `bootstrapError` clear on server-ready, and the supersession controls for the
  new status write (both suites). The registry already has the candidate's
  held-save and rehydration cases.
- Integrated checks under Node 22.23.2: `npm run check`, `npm run lint`,
  `npm run test:unit`, `npm run validate:core`. The wouter mock change is
  file-local (no shared fixture changes), but the pre-push hook skips
  "non-behavioral" runs, so run full `npm test` before push as the gate.
- Real-backend acceptance: `TZ=UTC npm run test:e2e:batch-b`
  (`tests/e2e/fund-workspace-real-backend.spec.ts`) against disposable
  PostgreSQL; with Colima,
  `DOCKER_HOST=unix:///Users/nikhil/.colima/default/docker.sock` and
  `TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock` after confirming
  the socket exists. Required evidence: distinct fund ids,
  `freshTabFirstEditPersisted`, `invalidLocalValuesRecovered`,
  `interruptedSaveReplayed.ifMatchReplayed`, `replayStatus=200`,
  `revisionAdvancedOnce`, zero unexpected browser or API errors. Local harness
  evidence only, not deployment proof.
- Before source admission: `git diff --check`, the changed-path classifier (no
  financial calculation is intended; follow any activated gate), explicit
  staging of owned files, green checkpoint commit, and hosted CI on the exact
  final head including heavy lanes. Old CI or old local browser evidence does
  not qualify the new head.

## Documentation Impact

- `TODOS.md` - the "False Save not confirmed alert during the Fund Basics
  bootstrap save" entry is removed by the candidate; only correct once
  acceptance passes.
- `docs/1-plans/F_1.15.0_fund-draft-command-settlement.plan.md` - the "Not
  closed by this plan" paragraph in the status banner should point at this plan
  once merged (one-line edit, TRIP-3).
- `docs/ARCHI.md` section 5 (fund draft command settlement note) - add one
  sentence that the settlement module also keeps a module-local live-save
  registry consulted by the hook's recovery effect, and that the two direct
  callers write `uncertain` themselves.
- `docs/3-code-review/CR_w10_fund-draft-command-settlement-audit.md` - its
  non-blocking note about the dead `STORAGE_UNAVAILABLE_MESSAGE` export becomes
  resolved; no edit required (historical record), but the TRIP-3 changelog line
  should mention the removal.
- None affected: `README.md`, `CLAUDE.md`, `DESIGN.md`, `DECISIONS.md`,
  `docs/_generated/*` (verified above). `CHANGELOG.md` gets its release line
  from TRIP-3, not from this plan.

## To-dos

### Phase 1: Reuse and red-first

- [ ] Fetch `origin/main`; confirm the candidate's parent is still an ancestor
      and intervening commits stay documentation-only.
- [ ] Create the isolated worktree and branch (no `command` substring in the
      path), cherry-pick the candidate, `npm ci`.
- [ ] Extend the bootstrap suite's `wouter` mock with `useSearch` and pathname;
      add the mounted-shell regression through acknowledgement.
- [ ] Extend the workspace dialog test with the store-status and retry-identity
      assertions.
- [ ] Run the five focused suites; record the two expected failures.

### Phase 2: Repair and verify

- [ ] Add the synchronous store `uncertain` write to the Fund Basics bootstrap
      `uncertain` arm and the workspace dialog `uncertain` arm; clear
      `bootstrapError` when `draftServerReady` becomes true.
- [ ] Extend the superseded-session/command controls in both suites with the
      replacement-status assertions and the uncertain-rejection variants.
- [ ] Remove the dead `STORAGE_UNAVAILABLE_MESSAGE` export.
- [ ] Re-run the five focused suites (all green), then `npm run check`,
      `npm run lint`, `npm run test:unit`, `npm run validate:core`, then full
      `npm test` as the pre-push gate.
- [ ] Run `npm run test:e2e:batch-b` against disposable PostgreSQL; retain the
      acceptance JSON.
- [ ] `git diff --check`, changed-path classifier, explicit staging, green
      checkpoint commit.

### Phase 3: Admission handoff

- [ ] Independent exact-diff review of the final head; hosted CI on that head
      with heavy lanes.
- [ ] Draft PR describing this bounded internal-workflow repair; owner merges.
      Merge, deploy, and production stay separate.
