# F2 First Public Selected-Lane Success Plan (F_2.0.1)

**Status:** APPROVED  
**Date:** 2026-08-24  
**Execution base:** `origin/main@f50b4f516ecce70a099cb627935ec5d6d4a79405`  
**Worktree:** `/tmp/updog-f2-first-success-20260824/worktree`

Independent plan review: `APPROVED`, no P1/P2 findings, thread
`01a03481-8581-7c50-b6d4-e69dfbcd388d`.

## 1. Supersession and authority

This file replaces the obsolete post-merge execution plan anchored to PR #1418
and `6f643798d`. Current `origin/main` already contains F1 conformance repair in
PR #1419 at `f50b4f516ecce70a099cb627935ec5d6d4a79405`; Tasks 1-5 are complete and
must not be replayed.

This plan authorizes only local, uncommitted source and test edits in the clean
detached worktree above. It authorizes no commit, push, pull request, merge,
deployment, provider action, schema or data mutation, tag, release, or
production dispatch. Plan and review results are evidence only.

## 2. Goal

Admit the first public V2 success for one complete opening-state-only,
fee-free, deal-by-deal capability envelope. Return the existing detached V2
receipt and preserve every F1 refusal outside that envelope.

Success criteria:

1. Named truth case `V2-S-0100` returns an exact receipt from strict
   `internal-economics-composite/2.0.1` input.
2. Ordinary derivation executes only `deal_by_deal` for this envelope.
3. Dual-lane certification remains disabled.
4. Nonzero management-fee entries still refuse
   `UNSUPPORTED_V2_MANAGEMENT_FEE/accrual`.
5. Empty and non-empty all-zero fee schedules follow ADR-086 exactly: only an
   empty schedule may enter F2; a non-empty all-zero schedule remains
   `UNSUPPORTED_V2_BASE_EVENT/admission`.
6. Inputs, receipts, hashes, and existing F1 truth corpus remain deterministic
   and unmutated.

## 3. Locked invariants

- V1 code and behavior remain unchanged.
- Repo-global `ROUND_HALF_UP` remains the only rounding authority.
- Ordinary runtime remains selected-lane-only.
- Dual-lane execution remains certification-only and is not enabled here.
- No event kind is admitted in F2.
- No management-fee, preferred-return, catch-up, recycling, reserve, or
  event-processing mechanics are added.
- No ownership, lot lineage, or accounting value is inferred.
- Existing normalization and refusal precedence remain fail closed.
- No dependency is added.
- `tests/helpers/v2-input-builder.ts` default remains unchanged because its
  non-empty all-zero schedule is shared by the F1 refusal corpus and multiple
  unit suites.
- Existing receipt contract, receipt version, component-version value, and
  normalized/result hash domains remain unchanged.

## 4. Exact F2 admission envelope

After successful normalization and existing specific capability refusals, the
public guard may return success only when all predicates below are true:

```ts
input.selectedLane === 'deal_by_deal'
input.events.length === 0
input.gpCashPreferredReturnTreatment === 'pari_passu'
input.lpClasses.every(({ feeProfile }) =>
  feeProfile.managementFeeSchedule.length === 0 &&
  feeProfile.feeRecyclingEnabled === false &&
  feeProfile.feeRecyclingCapUsd === undefined &&
  feeProfile.exitRecyclingEnabled === false &&
  feeProfile.exitRecyclingCapUsd === undefined
)
input.waterfallPolicy.length === 1
input.waterfallPolicy[0].kind === 'carry'
input.waterfallPolicy[0].priority === 1
new Decimal(input.openingState.openingCashClassification.recycling).isZero()
input.openingState.openingProvenance.cashLots.every(
  (lot) => lot.classification !== 'recycling'
)
(input.sourceRefs?.length ?? 0) === 0
(input.upstreamReceiptIds?.length ?? 0) === 0
```

`gpShare` remains a schema-valid input value; F2 does not hard-code an economic
rate. Opening recycling cash is excluded because current event-state
initialization does not hydrate opening provenance lots. All partner, class,
opening-state, and provenance values must already pass the strict normalizer
and its reconciliation checks.

Any normalized input missing one predicate returns
`UNSUPPORTED_V2_BASE_EVENT/admission`, unless an earlier specific refusal owns
the failure.

### Refusal precedence retained

| Input condition | Required result |
| --- | --- |
| Contract/schema/identity/reconciliation defect | Existing normalization refusal |
| Equalization | Existing `UNSUPPORTED_V2_EQUALIZATION/equalization` |
| Any nonzero fee-schedule entry | `UNSUPPORTED_V2_MANAGEMENT_FEE/accrual` |
| Contribution correction, write-off, or conversion | Existing specific admission refusal |
| Non-empty all-zero fee schedule | `UNSUPPORTED_V2_BASE_EVENT/admission` |
| Any event otherwise processable by current engine | `UNSUPPORTED_V2_BASE_EVENT/admission` |
| Whole-fund ordinary lane | `UNSUPPORTED_V2_BASE_EVENT/admission` |
| Preferred return, catch-up, ROC-plus-carry, recycling, or receipt refs | `UNSUPPORTED_V2_BASE_EVENT/admission` |
| Exact F2 envelope | Existing receipt success |

## 5. Minimal implementation

### Task 6.1: Add RED boundary and truth tests

Create a test-local `buildV2S0100Input()` from `buildMinimalV2Input()` using
explicit overrides. Do not change the shared builder default.

The fixture must set:

- `selectedLane: 'deal_by_deal'`;
- `events: []`;
- every `managementFeeSchedule: []`;
- fee and exit recycling disabled;
- fee and exit recycling caps absent;
- exactly one carry tier at priority 1 with `gpShare: '0.200000000000'`;
- `gpCashPreferredReturnTreatment: 'pari_passu'`;
- opening recycling classification `0.000000` and no recycling cash lots;
- no `sourceRefs` or `upstreamReceiptIds`.

Add `tests/unit/truth-cases/internal-economics-v2-first-success.test.ts` with a
named `V2-S-0100` assertion. Before production code changes, prove it fails
only with `UNSUPPORTED_V2_BASE_EVENT/admission`.

The truth test must hand-author the complete expected result payload and assert:

- receipt version `internal-economics-receipt/2.0.0`;
- `componentVersions: {}`;
- selected lane `deal_by_deal`;
- hash algorithm `canonical-json-sha256/1`;
- cash equation opening and ending cash `550000.000000`, all period flows zero;
- no tier allocations;
- exact LP and GP opening ledger values from the fixture;
- exact zero class ledger values;
- normalized-input hash from the independent test oracle over the untouched
  strict wire input;
- result hash from the independent test oracle over the hand-authored result
  payload;
- both hashes are lowercase 64-hex strings;
- input remains byte-for-byte equivalent after derivation;
- mutating caller-owned input after derivation cannot change the receipt;
- repeated derivation returns deep-equal receipts and hashes;
- cash and distribution conservation equations equal zero.

Update `tests/unit/internal-economics/v2/derive-composite-v2.test.ts` with
adjacent guard tests:

1. non-empty all-zero schedule on an otherwise valid F2 fixture returns base
   admission refusal;
2. nonzero schedule on that fixture returns management-fee accrual refusal;
3. each excluded sibling shape returns base admission refusal: one event,
   whole-fund lane, fee recycling, exit recycling, preferred-return policy,
   ROC-plus-carry policy, non-empty `sourceRefs`, non-empty
   `upstreamReceiptIds`;
4. explicit event refusal remains ahead of base admission;
5. certification called with the valid F2 fixture remains
   `UNSUPPORTED_V2_WHOLE_FUND_CERTIFICATION/waterfall` with no partial output.

Update `tests/unit/internal-economics/v2/reserve-funding-classifier-v2.test.ts`
with one isolation test: obtain a valid `V2-S-0100` economics receipt, run the
reserve classifier separately on a state that produces its own refusal, and
assert the existing economics receipt is unchanged and contains no reserve
result fields.

RED command, under pinned Node 22.23.2:

```bash
TZ=UTC npx vitest run \
  tests/unit/truth-cases/internal-economics-v2-first-success.test.ts \
  tests/unit/internal-economics/v2/derive-composite-v2.test.ts \
  tests/unit/internal-economics/v2/reserve-funding-classifier-v2.test.ts \
  --config vitest.config.mjs --configLoader native --project=server
```

Expected RED: only the new positive `V2-S-0100` assertion fails with
`UNSUPPORTED_V2_BASE_EVENT/admission`; existing F1 assertions remain green.

### Task 6.2: Retire the base guard only for the exact envelope

Edit only
`shared/lib/internal-economics/v2/derive-composite-v2.ts` unless RED evidence
proves another production file is necessary.

Add one private predicate for the exact envelope in section 4. Preserve guard
order:

1. `checkManagementFeeRefusal`;
2. `checkEventCapabilityRefusal`;
3. return `null` for the exact F2 envelope;
4. otherwise return `UNSUPPORTED_V2_BASE_EVENT/admission`.

Do not admit events. `processEvents()` currently discards refusals returned by
deployment, realization, and expense processors; eventful success is separate
work and remains fail closed.

Do not change receipt builder, receipt contract, normalizer, event engine,
waterfall implementations, reserve classifier, corpus adapter, historical
truth corpus, ADRs, changelog, review records, server routes, persistence, or
activation surfaces.

### Task 6.3: Targeted GREEN and delta review

Run the RED command again and require all selected tests green. Then run:

```bash
TZ=UTC npm run test:internal-economics-v2
TZ=UTC npm run phoenix:truth
npm run guard:decimal-string-laundering:check
```

Review the complete working-tree diff against `f50b4f516`. Reject scope outside:

- this plan;
- `shared/lib/internal-economics/v2/derive-composite-v2.ts`;
- the new F2 truth test;
- the two named existing unit-test files.

No commit is permitted.

## 6. Full verification

All verification uses Node `22.23.2`, npm `10.9.2`, and `TZ=UTC` for tests.
Host Node 26 evidence is non-authoritative for this change.

```bash
node --version
npm --version
TZ=UTC npm run test:internal-economics-v2
TZ=UTC npm run phoenix:truth
npm run guard:decimal-string-laundering:check
npm run check
npm run lint
TZ=UTC npm test
TZ=UTC npm run calc-gate
git diff --check
git status --short
```

Required results:

- Node reports `v22.23.2`; npm reports `10.9.2`.
- Every command exits zero.
- `phoenix:truth` includes named `V2-S-0100` expected-output proof.
- Existing `V2-TC-001` through `V2-TC-008` remain base refusals.
- Existing refusal corpus hashes remain unchanged.
- Financial path classifier remains true through the existing `shared/lib/`
  and truth-test paths; classifier implementation need not change.
- `git diff --check` reports no whitespace errors.
- Worktree remains uncommitted and root dirty worktree remains untouched.

## 7. Independent review gates

1. Plan review must end `APPROVED` before Task 6.1.
2. Implementation delta review must find no Important or Critical defects.
3. Final full-diff review must confirm exact admission boundary, refusal
   precedence, V1 freeze, selected-lane isolation, independent hashes, and
   financial-truth coverage.
4. Any Important or Critical finding is repaired and re-verified before handoff.

## 8. Stop condition and handoff

Stop with an uncommitted diff after all required checks pass. Report:

- exact base SHA;
- changed files;
- RED and GREEN evidence;
- full verification results;
- remaining limitations;
- commit authority `ABSENT`;
- merge eligibility `UNKNOWN` pending exact-SHA CI;
- merge authority `UNKNOWN` pending live `CI Gate Status` and owner action;
- deployment readiness `UNKNOWN`;
- production authority `ABSENT`.

## 9. Addendum A — Task 6.4 contract-hardening repairs (2026-08-24)

**Status:** approved for local, uncommitted execution by owner directive in
session. **Trigger:** `docs/3-code-review/CR_w6_v2.0.1.md` — verdict NEEDS
REVISION, two Major findings reproduced `ok: true` by the reviewer.

### 9.1 Findings being repaired

- **M1.** Tier schemas (`internal-economics-input-v2.contract.ts:207-237`) are
  the only four objects in the contract without `.strict()`. Unknown tier
  fields are silently stripped; distinct wire inputs collide to one
  `normalizedInputHash` (reproduced: baseline == extra-field,
  `8542190f…0f70ab`).
- **M2.** Ratio fields are format-only validated
  (`shared/lib/decimal-string.ts:5-10` regex permits any signed magnitude);
  `gpShare = -1` and `2` derive to success (reproduced `ok: true`). Same-class
  siblings: `gpAllocationRate`, `annualRate`.

### 9.2 Scope extension

Task 6.3's five-file whitelist is extended by exactly:

- `shared/contracts/internal-economics/internal-economics-input-v2.contract.ts`
  — add `.strict()` to the four tier schemas.
- `shared/lib/internal-economics/v2/normalize-input-v2.ts` — extend
  `validateTierPolicy` with ratio bounds.
- `tests/unit/internal-economics/v2/normalize-input-v2.test.ts` — refusal
  tests.
- `tests/unit/internal-economics/v2/derive-composite-v2.test.ts` —
  admission-ordering tests.
- this plan file (this addendum) and, later,
  `docs/3-code-review/CR_w6_v2.0.1.md` (re-review update).

Forbidden: `shared/lib/decimal-string.ts` (shared ratio schema; V1 freeze and
other contracts depend on current semantics), receipt builder/contract, event
engine, waterfalls, reserve classifier, corpus adapter, historical corpus,
ADRs, changelog, routes, persistence, activation surfaces. No commit.

### 9.3 Required behavior

1. Unknown field on any tier object refuses
   `SCHEMA_VALIDATION_FAILED/normalization` (same shape as existing schema
   violations).
2. `gpShare`, `gpAllocationRate`, `annualRate` outside `[0, 1]` inclusive
   refuse `INVALID_TIER_POLICY/normalization` via `validateTierPolicy`, using
   Decimal comparisons. Boundary values `0` and `1` remain valid;
   `-0.000000000000` remains valid (zero).
3. Valid inputs unchanged: normalized hashes, receipt hashes, the `V2-S-0100`
   expected receipt, and F1 refusal corpus codes/stages/hashes all byte-stable.
4. Refusal ordering: contract defects refuse at normalization, ahead of
   admission. An F2-envelope fixture with `gpShare '2.000000000000'` refuses
   `INVALID_TIER_POLICY/normalization`, never `UNSUPPORTED_V2_BASE_EVENT`; an
   F2 fixture with an unknown tier field refuses
   `SCHEMA_VALIDATION_FAILED/normalization`.

### 9.4 Tests (written with the change, RED-first within the batch)

- Unknown extra field on each of the four tier kinds refuses
  `SCHEMA_VALIDATION_FAILED/normalization`.
- `gpShare` `-1.000000000000` and `2.000000000000` refuse
  `INVALID_TIER_POLICY/normalization`; same bounds for `gpAllocationRate` on a
  `gp_catch_up` tier and `annualRate` on a `preferred_return` tier.
- Boundary acceptance: `0.000000000000` and `1.000000000000` pass
  normalization.
- Admission ordering: F2-envelope fixture with out-of-range `gpShare` refuses
  `INVALID_TIER_POLICY/normalization`; F2 fixture with unknown tier field
  refuses `SCHEMA_VALIDATION_FAILED/normalization`.
- Reviewer repro expectations invert: `/tmp/f2-repro.ts` cases A, B, D must
  refuse.
- Prove RED inside the batch: run the new tests before the production change
  and capture the failure reason; then implement and show green.

### 9.5 Verification

Same battery as section 6 with `TZ=UTC` (host provides Node `22.23.1`; pinned
`22.23.2` unavailable here — delta documented nonblocking in
`CR_w6_v2.0.1.md`): focused vitest (normalize + derive + reserve + truth),
`test:internal-economics-v2`, `phoenix:truth`,
`guard:decimal-string-laundering:check`, `check`, `lint`, full `npm test`,
`calc-gate`, `git diff --check`, `git status --short`. Every command exits
zero.

### 9.6 Authority reading

Sections 1 and 8 use "authorizes" as procedural scoping only; under
`docs/governance/solo-internal-change-and-production-policy.md` (resolved from
`origin/main`), a plan grants no authority. This addendum changes none:
commit authority `ABSENT`; merge eligibility `UNKNOWN` pending exact-SHA CI;
merge authority `UNKNOWN`; deployment readiness `UNKNOWN`; production
authority `ABSENT`. TRIP-review re-review follows implementation.
