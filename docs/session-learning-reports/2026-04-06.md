---
last_updated: 2026-05-11
---

# Session Learnings Report — 2026-04-06

**Session Window:** 2026-04-06 (Phase 4 close-out triage + dependabot sweep)
**Sources Analyzed:** conversation trace, git log, `.omx/plans/`, dependabot API

## Session Context

Initial request was "prioritized next development goals." The investigation
revealed every approved PRD in the planning queue had already shipped — most
within the previous 24–72 hours. The session shifted to close-out work: Phase 4
architect-review findings, planning-doc reconciliation, and a dependabot sweep
that took the dashboard from 21 open alerts to 0.

Commits landed on `origin/main` during or around this session:

```
0298a65c fix(deps): override lodash, lodash-es, path-to-regexp to patched versions
bd62ef08 fix(ml-service): bump scikit-learn and python-multipart to patched versions
2a8f1bac chore(phase-status): record Phase 4 completion in tracker
8c15257f Close the mounted Phase 4 truthfulness findings without widening the product surface
6d355fea chore(gitignore): ignore .omx/artifacts and .omx/drafts local state
a606761d Align dormant time-travel truth with the current restore boundary
```

## Learning Candidates

### Candidate 1: Planning docs drift from current `main` within hours

- **Score:** 4 (repeated pattern +3, DX friction +1)
- **Sources:**
  - `.omx/plans/prd-priority-next-development-goals.md` (APPROVED, but all 3
    queue items already shipped)
  - `.omx/drafts/variance-automation-remaining-risks-plan-draft.md` (DRAFT, but
    all 3 risks closed in commit `3e9a360c` on the same day the draft was
    written)
  - `.omx/plans/prd-phase-4-performance-truthfulness.md` (APPROVED, but
    implementation shipped 5 hours before the architect-review artifact was
    written)
  - `docs/plans/2026-04-05-todo-report-remediation-strategy.md` (Worktracks
    A/B/C1 all shipped in `6bb2a92d`, `dabc93be`, `72c7d834` without doc
    updates)
- **Category:** Process / Workflow
- **Anti-Pattern:** Treating a planning doc's `status:` frontmatter as
  authoritative about what work is open. The doc represents a point-in-time
  snapshot; commits can land faster than plans get reconciled.
- **Observation:** Three distinct instances of this pattern in a single session.
  Each cost meaningful investigation time.
- **Fix:** Before scoping any task from a planning doc, verify each claim
  against current `main` via grep/git log. Evidence commands beside each claim
  (which the todo-report remediation strategy doc does correctly) make this
  trivial.
- **Recommendation:** CREATE FEEDBACK MEMORY (user-personal)

### Candidate 2: Parallel sessions need planning-doc reconciliation FIRST

- **Score:** 3 (repeated pattern +1, DX friction +1, risk surface +1)
- **Source:** `.omx/context/time-hardening-20260406T081749Z.md` + conversation
  around Phase 4 close-out plan dispatch
- **Category:** Process / Workflow
- **Anti-Pattern:** Running two concurrent sessions on the same repo where one
  is doing planning research while another is about to execute code changes. The
  planning session reads stale PRDs and draws wrong conclusions about what's
  in-flight; the execution session may collide at the file level if the planned
  lane overlaps.
- **Fix:** When parallel sessions are expected, the first action on the
  execution branch should be planning-doc frontmatter reconciliation — mark
  COMPLETE/SUPERSEDED/ARCHIVED on anything already shipped. This takes minutes
  and unblocks the planning session with truthful ground-state. The code changes
  can wait.
- **Recommendation:** CREATE FEEDBACK MEMORY (user-personal) — fold into
  Candidate 1 as a second rule.

### Candidate 3: Dependabot dashboard lies about severity until lockfile rescans

- **Score:** 4 (security +2, DX friction +1, repeated pattern +1)
- **Source:** dependabot API response + `npm audit` comparison during the sweep
- **Category:** Infrastructure / Security
- **Anti-Pattern:** Taking the dependabot banner count ("21 vulnerabilities: 9
  high") at face value when planning remediation work. GitHub's scanner is
  asynchronous — if the lockfile already has a patched version but no new commit
  has been pushed, the dashboard keeps showing stale alerts indefinitely.
- **Observation:** In this session, **9 of 21 alerts were already-patched false
  positives** (rollup, picomatch, postcss, minimatch v10, vite). Another **4
  were scanner false positives against the wrong version range** (vite v7
  advisory applied to our v5). Only **8 alerts represented real actionable
  work** — and even those split into real code fixes (5) and dev-only
  accepted-risk transitives (3).
- **Fix Sequence:**
  1. Cross-reference every open dependabot alert against the current lockfile
     _before_ planning any remediation.
  2. Ship real patches first; pushing the updated lockfile triggers GitHub's
     rescan which auto-clears many stale false positives.
  3. For alerts that won't auto-clear (either because the vulnerability is real
     or the scanner is stuck), dismiss with evidence via
     `gh api --method PATCH repos/:owner/:repo/dependabot/alerts/:n -f state=dismissed -f dismissed_reason=<reason>`.
  4. Valid dismissal reasons: `fix_started`, `inaccurate`, `no_bandwidth`,
     `not_used`, `tolerable_risk`. `dismissed_comment` is limited to **280
     characters** (Twitter-length, not documented in obvious places).
- **Recommendation:** CREATE REFERENCE MEMORY (user-personal)

### Candidate 4: Nested overrides for parent-scoped transitive fixes

- **Score:** 2 (DX friction +1, reusable technique +1)
- **Source:** `package.json` override added in commit `0298a65c` to force
  `path-to-regexp@0.1.13` only under `@lhci/cli` without touching the global
  `path-to-regexp@8.4.0`
- **Category:** Build / Dependencies
- **Technique:**
  ```json
  "overrides": {
    "@lhci/cli": {
      "path-to-regexp": "^0.1.13"
    }
  }
  ```
  Forces the transitive version _only_ under a specific parent, preserving the
  higher version elsewhere in the tree. Useful when a legacy dev tool carries an
  old transitive but other packages rely on a newer major.
- **Recommendation:** FOLD INTO Candidate 3 reference memory

### Candidate 5: Stale session-start git status

- **Score:** 2 (DX friction +2)
- **Source:** Session-start `git status` snapshot showed files as "modified"
  when they had actually been committed by another session 4 hours earlier
- **Category:** Git / Tooling
- **Anti-Pattern:** Trusting the session-start `git status` dump without
  re-running `git status` after significant conversation time. The snapshot is
  frozen at session start and doesn't update as the conversation progresses.
- **Fix:** Re-run `git status` / `git log` immediately before any git action,
  especially when the initial state was captured minutes or hours earlier.
- **Recommendation:** SKIP — too context-specific to codify; captured implicitly
  in Candidate 1

## Actions

| #   | Action                                                                    | Target                                       |
| --- | ------------------------------------------------------------------------- | -------------------------------------------- |
| 1   | Create feedback memory: planning-doc drift + cross-session reconciliation | `memory/feedback_plandoc_drift_verify.md`    |
| 2   | Create reference memory: dependabot triage flow                           | `memory/reference_dependabot_triage_flow.md` |
| 3   | Update `MEMORY.md` index with both new entries                            | `memory/MEMORY.md`                           |

## Not Captured (Skipped)

- **Red-teaming your own plan catches real flaws** — workflow observation, not a
  codifiable rule
- **Architect review artifacts should be wired into follow-up plans** — already
  addressed implicitly by the Phase 4 close-out commit message and plan doc
- **Vercel plugin hook injection noise** — local tooling config issue, not a
  project learning

## Metrics

- 21 → 0 dependabot alerts resolved this session
- 3 instances of planning-doc drift observed
- 3903 → 3915 tests in the unit suite (Phase 4 close-out added 12 tests; lodash
  4.18.1 update introduced zero regressions)
- 5 commits shipped to `origin/main`

---

# Session 2 — sensitivity-refactor-and-polish (afternoon)

**Session Window:** 2026-04-06 afternoon (babysitter run
`01KNHRMYD8WHNBP1XYE8MKXG6W`) **Sources Analyzed:** babysitter run journal,
red-team review, integration verify output, pre-push hook output, conversation
trace **Outcome:** Single combined commit `2772dce9` on `origin/main`. Phase 1 +
Phase 2 + Phase 3 all green in 11 iterations. Pre-push full suite reported 4010
passed / 86 skipped / 0 failed (+28 new tests from this slice).

## Session Context

Started from a clean working tree on `main @ 33465586`. Initial brainstorm
identified two follow-on items from the previous sensitivity-foundation-one-way
run: (1) extract `OneWayPanel` internals into a shared directory so a future
TwoWayPanel can reuse them, and (2) replace the engine-error-to-status ternary
in `server/routes/sensitivity.ts` with a `STATUS_BY_CODE` table that fixes
`INVALID_PUBLISHED_CONFIG` (500 → 422) and `UNSUPPORTED_VARIABLE_PATH` (500 →
400). User chose to bundle them as a single combined commit. Process file was
designed via `/babysitter:plan`, then red-teamed via the
deliberation-debate-red-teaming skill which caught one showstopper and several
high/medium-priority issues BEFORE execution. All red-team patches landed in the
process file before `run:create`. The babysitter run executed cleanly with zero
re-runs, zero retries.

## Learning Candidates

### Candidate 1: `test.projects` per-project glob extension mismatch — silent skip risk

- **Score:** 7
- **Source:** Red-team T1 finding on
  `.a5c/processes/sensitivity-refactor-and-polish.red-team.md`, grounded against
  `vitest.config.ts:134`
- **Anti-Pattern:** Adding a `.test.ts` file under a directory whose vitest
  project glob is `*.test.tsx` only. The contract gate would have reported "0
  passed, 0 failed" — false-positive green.
- **Detection cost:** Zero (caught at planning time).
- **Fix:** Renamed `formatters.test.ts` → `formatters.test.tsx` in the process
  file BEFORE `run:create`.
- **Recommendation:** EXTENDED REFL-016 with a "Variant: per-project glob in
  `test.projects` mode" section.

### Candidate 2: Pre-push hook is warmer than cold full-suite — trust it over cold runs

- **Score:** 4
- **Source:** Integration verify reported 10 server + 5 client failures from
  cold full-suite runs; pre-push hook ran the SAME suite and reported 4010
  passing. Standalone re-runs of the failing files all passed.
- **Anti-Pattern:** Treating cold full-suite failures as regressions and failing
  the verification gate without re-running standalone first.
- **Recommendation:** CREATED
  `feedback_pre_push_warmer_than_cold_full_suite.md`.

### Candidate 3: `useFakeTimers` + setInterval-based hooks need `act()` wrapping

- **Score:** 3
- **Source:** Red-team T2 finding on the planned `useElapsedSeconds.test.tsx`.
- **Anti-Pattern:** Calling `vi.advanceTimersByTime(1000)` outside an `act()`
  boundary when the hook calls a state setter from inside `setInterval`.
- **Fix:** All timer advances wrapped in `act()`. Reference pattern at
  `tests/unit/hooks/use-graduation.test.tsx:80`.
- **Recommendation:** CREATED `feedback_use_fake_timers_act_wrapping.md`.

### Candidate 4: Babysitter `task:post --value` is path-relative to runDir

- **Score:** 2
- **Source:** Iteration 1 of the run — first `task:post` invocation failed with
  `ENOENT` on a doubled path because I passed an absolute value path.
- **Fix:** Always pass `--value tasks/<effectId>/output.json` (relative to
  runDir).
- **Recommendation:** CREATED `feedback_babysitter_task_post_relative_paths.md`
  (cheap; future sessions will hit it).

### Candidate 5: Refactor verification — grep symbol names, not line counts

- **Score:** 3
- **Source:** Red-team E1 finding.
- **Recommendation:** SKIP. Already covered by
  `feedback_verifier_agents_use_grep_not_line_numbers.md` (the principle
  generalizes naturally from line numbers to line counts).

### Candidate 6: Verbatim extraction discipline (don't "improve" tokens during refactor)

- **Score:** 3
- **Source:** Red-team E2 finding.
- **Fix:** Process file's Phase 1 instructions explicitly forbid token changes;
  verification gate greps for `border-gray-200`.
- **Recommendation:** SKIP. Captured in the process file itself.

## Actions

1. [x] EXTENDED
       `docs/skills/REFL-016-vitest-include-patterns-miss-new-test-directories.md`
       with a "Variant: per-project glob in `test.projects` mode" section.
2. [x] CREATED `memory/feedback_use_fake_timers_act_wrapping.md`.
3. [x] CREATED `memory/feedback_babysitter_task_post_relative_paths.md`.
4. [x] CREATED `memory/feedback_pre_push_warmer_than_cold_full_suite.md`.
5. [x] UPDATED `memory/MEMORY.md` index with links to the 3 new feedback files.
6. [x] UPDATED `.claude/memory/active-context.md` snapshot for the new commit.

## Skipped (already documented or not worth a separate entry)

- **Refactor grep proofs vs line counts:** existing
  `feedback_verifier_agents_use_grep_not_line_numbers.md` covers it.
- **Verbatim extraction discipline:** captured in the process file's red-team
  patch, not a project-wide pattern.
- **Vercel plugin hook injection noise:** still loud, still ignored. Same
  finding as session 1.

## Metrics

- 1 babysitter run, 11 iterations, 0 retries, 0 re-runs
- 1 commit shipped (`2772dce9`)
- 12 files changed (3 modified, 9 new), 468 insertions, 108 deletions
- Net `OneWayPanel.tsx`: -86 lines
- Pre-push full suite: 4010 passed / 86 skipped / 0 failed (300 test files)
- Tests added by this slice: 28 (formatters 12 + useElapsedSeconds 6 +
  SummaryCard 3 + error-mapping 7)
- Phoenix truth count: 258 → 258 (unchanged)
- Lint ratchet: 28 ≤ 29 (unchanged)
- TypeScript baseline: 0 → 0 errors
- Red-team patches applied before execution: 1 showstopper + 1 high-priority
  - 5 monitor-tier
- Cost of red-teaming the process before execution: ~10 minutes
- Cost saved (silent test skip → false-positive gate → eventual rework):
  unknowable but >> 10 minutes

---

# Session 3 — sensitivity-two-way-panel (evening)

**Session Window:** 2026-04-06 evening (IA decision arc + babysitter run
`01KNJ5BXCTDFQBR5S0DR8G4BWG`) **Sources Analyzed:** conversation trace, IA
decision rounds, defensive grep on `pages/monte-carlo.tsx`, parent plan lines
387/487/496, babysitter run journal, integration verify output, pre-push hook
output **Outcome:** Two commits landed on `origin/main` — `35b63a47`
(`chore(sensitivity): remove orphaned pages/monte-carlo.tsx wrapper`) and
`bc592b38`
(`feat(sensitivity): add two-way analysis tab with TwoWayPanel + engine + route`).
Pre-push full suite reported 4040 passed / 86 skipped / 0 failed (+30 from this
slice). Babysitter run executed cleanly across 11 iterations with zero retries.
The IA discussion that preceded the run is the most learning-rich part of this
session.

## Session Context

Started from a clean working tree on `main @ e4707353`. The user's "next
direction" question was answered via an AskUserQuestion menu (Phase 2
TwoWayPanel selected). Before the babysitter run started, the IA of the
`/sensitivity-analysis` page was analyzed via the information-architecture
thinking framework, which surfaced a 3-layer label mismatch (sidebar
"Backtesting" vs page header "Sensitivity Analysis" vs default tab "Monte Carlo
Backtesting"). The initial recommendation (Strategy A: split Monte Carlo into a
separate route) was retracted after the user asked for git alignment, then
re-recommended as Strategy B (rename only), then bias-checked via the
scout-mindset framework, then almost executed on a false premise about
`pages/monte-carlo.tsx` being "incomplete cleanup" — stopped at the last second
by one defensive grep that found explicit "do not delete until X" instructions
in the parent plan I had previously read only the top of. The plan's gate (slice
1b verification) was then confirmed met, the deletion was authorized
compliantly, and `35b63a47` landed. The babysitter run for the actual Phase 2
work (TwoWayPanel) ran cleanly afterward. The dominant lesson is process
discipline, not code.

## Learning Candidates

### Candidate 1: Defensive grep before destructive action — and "plan complete" ≠ "plan binding"

- **Score:** 8 (destructive consequence, recurrent failure mode, cheap fix)
- **Source:** Conversation trace; specifically the 4-round IA discussion that
  preceded commit `35b63a47`. Three rounds of self-corrected recommendations all
  driven by shallow plan reads, caught on the third round only by one defensive
  grep immediately before the deletion that turned up
  `docs/plans/2026-04-02-phase-2-scenario-comparison-consolidation-plan.md`
  lines 387/487/496 (which I had previously claimed didn't exist).
- **Anti-Pattern:** Terminating a search at the first match that ratifies the
  current working hypothesis. Confusing "plan exists and is recent" with "plan
  is binding architectural law." Executing destructive actions without one final
  defensive grep regardless of how thorough the prior search felt.
- **Fix:** Defensive grep immediately before any deletion. Three-property plan
  check (recency, implementation status, constraint binding) before citing a
  plan as authoritative. When self-correcting, search HARDER, not the same. Two
  orthogonal validation passes (git alignment + bias check) for non-trivial
  recommendations.
- **Recommendation:** CREATED
  `docs/skills/REFL-033-defensive-grep-before-destructive-action.md`.

### Candidate 2: Sidebar label drift after consolidation

- **Score:** 3
- **Source:** IA analysis of
  `client/src/components/layout/navigation-config.ts:79` vs the current state of
  `/sensitivity-analysis`.
- **Anti-Pattern:** When the Apr 3 consolidation routed `/sensitivity-analysis`
  and labeled the sidebar nav `'Backtesting'`, the label was correct AT THAT
  MOMENT (Monte Carlo Backtesting was the only live tab). Three days later,
  One-Way Analysis was also live, and the label had drifted from accuracy. The
  team didn't relabel during the One-Way landing because they were focused on a
  different scope. After this session's two-way slice, the label is stale by TWO
  slices.
- **Fix:** When adding a new live tab to a multi-tab page, include a "does the
  page-level label still cover the umbrella?" check in the slice's gate
  criteria. Or schedule periodic IA-cleanup slices that touch only labels.
- **Recommendation:** SKIP for a separate REFL — the lesson is captured in the
  sensitivity-two-way-panel session memory and the open-questions list. The next
  IA cleanup slice will fix the symptom; the broader pattern is too narrow to
  generalize without more cases.

### Candidate 3: Babysitter scaffold reuse converged on stable template

- **Score:** 4 (positive pattern, third validation case)
- **Source:** `.a5c/processes/sensitivity-two-way-panel.js` was scaffolded from
  `.a5c/processes/sensitivity-refactor-and-polish.js` with high fidelity: same
  task definitions (`scopeApproval`, `implementBatchTask`, `verifyBatchTask`,
  `integrationVerifyTask`, `commitBreakpoint`), same phase rhythm (scope →
  implement → verify → integration → commit), red-team patches from the previous
  slice carried over (`.test.tsx` for client tests, `act()` wrapping for
  setInterval hooks, `npm run baseline:check` in Phase 3, collateral check after
  every batch). The reuse made this slice substantially faster to scaffold than
  building from scratch.
- **Recommendation:** SKIP for REFL — this is a positive pattern, not an
  anti-pattern. Captured in the sensitivity-two-way-panel session memory. A
  future "babysitter sensitivity-slice template" meta-skill could codify the
  shape, but it's premature with a 3-slice n.

### Candidate 4: Two cold-run client-suite flakes (re-validation)

- **Score:** 4 (re-validates an existing memory entry)
- **Source:** Integration verifier reported 2 client-suite cold-run timeout
  failures in `tests/unit/pages/fund-basics-bootstrap.test.tsx` and
  `tests/unit/pages/review-step-draft-failure.test.tsx`. Both pass standalone in
  <1.5s. Neither file is in this slice's owned files; neither was modified. The
  pre-push hook then ran the same suite and reported 4040/4040 passing. This
  matches `feedback_pre_push_warmer_than_cold_full_suite.md` exactly — the
  previous slice's session also hit this with 10 server + 5 client failures cold
  vs 4010/4010 from the pre-push hook.
- **Recommendation:** SKIP for new memory entry — already documented. This
  session is the second validation case in a row. Worth noting in the existing
  feedback file that the pattern is now reproduced across 2+ sessions.

### Candidate 5: Babysitter `--harness claude-code` session-id resolution under multiple state files

- **Score:** 2
- **Source:** Two state files in
  `~/.claude/plugins/cache/a5c-ai/babysitter/4.0.144/skills/babysit/state/`
  modified at the same Apr 6 13:24 timestamp (`ea17f14e-...` and
  `80534c19-...`). Both empty `run_id`. Picked the most recent one
  alphabetically (`ea17f14e-...`) which turned out to be the correct active
  session — but this was a guess.
- **Fix:** No fix needed for now; the binding worked. If it had been wrong, the
  stop-hook wouldn't have fired and I'd have fallen back to manual iteration.
  The only code-level improvement would be a babysitter CLI command that reports
  the active Claude Code session id (e.g., `babysitter session:active`).
- **Recommendation:** SKIP for REFL or memory — single-session annoyance, not a
  recurring failure mode. Worth a 1-line note in the session memory.

### Candidate 6: Vercel-plugin auto-injection hook noise — 3rd consecutive session

- **Score:** 2
- **Source:** Every prompt in this session triggered the plugin's lexical-match
  best-practices auto-suggestions for irrelevant Vercel/Next.js skills. This is
  a Vite/Express/React project that has nothing to do with Vercel deployments or
  Next.js routing. Same observation as Sessions 1 and 2.
- **Recommendation:** SKIP for REFL — already noted in two prior sessions. The
  fix is configuration (disable the plugin for this repo), not a learning. Worth
  promoting to "actionable open question" if it persists into a 4th session.

### Candidate 7: Stop-hook driven iteration discipline (positive pattern)

- **Score:** 3
- **Source:** This session's babysitter run executed all 11 iterations correctly
  in the "STOP after every phase" discipline. After every iteration result was
  posted, I stopped and waited for the user's "proceed" to drive the next
  iteration. No iteration was prematurely chained, no breakpoint was
  auto-resolved, all AskUserQuestion responses came from the actual user.
- **Recommendation:** SKIP — already codified in the babysit skill itself. This
  session is the third consecutive validation case in a row. The pattern is
  stable.

## Actions

1. [x] CREATED
       `docs/skills/REFL-033-defensive-grep-before-destructive-action.md`
       covering candidate 1 (defensive grep + plan binding taxonomy) with the
       full 4-round trace from this session as the validation case.
2. [x] CREATED `.claude/memory/sessions/2026-04-06-sensitivity-two-way-panel.md`
       capturing the IA decision arc, the babysitter run summary, decisions
       made, context for the next session, and open questions.
3. [x] APPENDED Session 3 to `docs/session-learning-reports/2026-04-06.md` (this
       document).
4. [ ] UPDATE `.claude/memory/active-context.md` snapshot for the new commits
       and the new sensitivity surface state (3 live tabs + 1 disabled).

## Skipped (already documented or not worth a separate entry)

- **Sidebar label drift after consolidation (Candidate 2):** captured in the
  session memory's open-questions list. Will be fixed by the next IA cleanup
  slice; not a generalizable anti-pattern yet.
- **Babysitter scaffold reuse (Candidate 3):** positive pattern, not an
  anti-pattern. Captured in the session memory's "Context for Next Session"
  section. Worth promoting to a meta-skill if a 4th sensitivity-style slice ever
  appears.
- **Cold-run flake re-validation (Candidate 4):** already documented in
  `feedback_pre_push_warmer_than_cold_full_suite.md`. This session is the second
  consecutive validation case.
- **Babysitter session-id ambiguity (Candidate 5):** single-session annoyance,
  not recurring. Worked correctly via heuristic.
- **Vercel plugin hook noise (Candidate 6):** third consecutive session, still
  ignored, configuration fix not yet applied.
- **Stop-hook discipline (Candidate 7):** positive pattern, already codified.

## Metrics

- 1 babysitter run, 11 iterations, 0 retries, 0 re-runs
- 2 commits shipped (`35b63a47` chore + `bc592b38` feat)
- 12 files changed total: 1 deleted (monte-carlo.tsx, -8 lines), 11 in feat
  commit (4 new + 7 modified, +1756 / -21)
- Pre-push full suite: 4040 passed / 86 skipped / 0 failed (302 test files,
  69.41s)
- Tests added by this slice: ~30 (engine 8, route 5, hook 4, component 8, page
  extension)
- Phoenix truth count: 258 → 258 (unchanged)
- Lint ratchet: 28 (held)
- TypeScript baseline: 0 → 0 errors
- Net new TS LOC: ~700 (engine + component + tests, untracked-new files not
  counted in `git diff --stat`)
- IA decision rounds before correct verdict: 4 (initial → corrected → bias-
  checked → defensive-grep-halted → staleness-verified)
- Defensive grep tool calls that prevented violation: 1 (the one immediately
  before the planned `rm`)
- Cost of the IA discussion: ~30 minutes of conversation
- Cost saved by the defensive grep: unknowable but ≥ a force-revert
- Babysitter scaffold reuse savings vs scaffolding from scratch: ~estimated
  60-70% time reduction on the process file authoring step
