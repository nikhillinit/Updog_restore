# Session: 2026-02-16 -- P4 Pipeline UI Polish Planning

## Summary

Reviewed, critiqued, and Codex-validated the P4 Pipeline UI Polish plan through
3 rounds of iterative collaboration. Explored the full pipeline codebase (4
components, 1 page, 1 route file, schema, tests, Playwright config). Identified
10 issues in the original proposal. Codex steelman confirmed feasibility at
~19h, recommended deferring DnD, using non-unique lookup index for dedupe,
savepoint pattern for bulk partial failures, and 4 minimum viable E2E scenarios.
Round 3 uncovered 4 hidden dependencies: apiRequest loses Zod issues,
queryKey.join breaks with filter params, fundId not wired to modals, and 3
competing flag systems. All incorporated into final plan.

Also debugged Codex CLI invocation: `codex-wrapper` Go binary hangs on long
prompts; `codex exec` works reliably. Updated skill docs and auto memory.

## Work Completed

- Explored full pipeline codebase (6 files, ~3000 lines)
- Wrote codebase findings doc (.claude/plans/p4-review-findings.md)
- Produced 10-point plan critique with inversion defense
- Codex Round 1: Feasibility confirmed, dedupe/bulk/E2E flagged
- Codex Round 2: Resolved dedupe index (non-unique), savepoint pattern, 4 E2E
  scenarios
- Codex Round 3: Found 4 hidden dependencies (apiRequest, queryKey, fundId,
  flags)
- Finalized plan (.claude/plans/p4-final-plan.md) with 4.0a-d prerequisites
- Fixed codex skill docs for Windows (HEREDOC path mangling, wrapper vs exec)
- Updated auto memory with Codex invocation patterns

## Decisions Made

- DnD deferred to P4.5 (highest risk, lowest value for "hardening")
- Last-write-wins (no version column for solo GP tool)
- Cursor resets on sort change (no per-column keyset pagination)
- @dnd-kit/core selected for future DnD implementation
- Non-unique lookup index for import dedupe (not unique constraint)
- Savepoint pattern for bulk partial failure reporting
- useUnifiedFlag + registry for feature flags
- codex exec preferred over codex-wrapper for analysis tasks

## Key Patterns

- **Codex exec vs wrapper**: `codex exec "prompt" --sandbox read-only` responds
  in <1 min. `codex-wrapper` hangs on long prompts due to JSON stream parsing.
- **Iterative Codex critique**: 3 rounds with blocking/important/minor severity
  gates per maestro playbook. Round 3 found issues Rounds 1-2 missed.

## Artifacts

- `.claude/plans/p4-final-plan.md` (implementation plan)
- `.claude/plans/p4-review-findings.md` (codebase findings)
- Updated `skills/codex/skill.md` (Windows fixes)
- Updated auto memory MEMORY.md (Codex patterns)

## Open Questions

- None blocking. Plan is implementation-ready.

---

_Session duration: ~90 min (50 min lost to codex-wrapper hang)_
