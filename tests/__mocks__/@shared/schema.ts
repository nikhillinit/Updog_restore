// Structural mock for '@shared/schema'
// Ensures tests that vi.mock('@shared/schema') have expected exports.
export const performanceForecasts = {
  horizonMonths: 36,
  metrics: ['tvpi', 'dpi', 'nav'],
};

export default {};
